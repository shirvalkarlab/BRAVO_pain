# Monday and Friday research slides

**Required before every delivery:** follow [the scientific and visual review loop](biweekly-slide-review.md). Adapt settings and layout to each run, explicitly critique the actual rendered pair, fix problems, re-export and reinspect before sending it to Aditya. Record findings and corrections, and use prior feedback to prevent repeated mistakes. Historical template coordinates and prior warning exceptions do not replace current judgment.

**Current contract, September 5, 2026:** Monday/Friday updates are private review drafts delivered only to Aditya in Slack. He reviews and forwards them himself. This self-review workflow supersedes the earlier format-approval activation gate. “Biweekly” means twice weekly, not every other week. Historical preparation notes below document superseded layouts; the current contract in this section and the final section takes precedence.

Destination: [existing Google Slides deck](https://docs.google.com/presentation/d/1fst2deA7acDnJFJXOZz-bn6fBDPMn_YZbaXJPiN1Uqs/edit). Preserve its instruction slide (object `p`), existing pages, sharing and source instructions. Insert two pages per Pacific update date immediately after the instruction slide: newest Multimodal Summary first, then newest Left Leg VAS biomarkers, with all older pairs below. Check existing page identities and a local completion record before writing; resume or repair that date's pair instead of duplicating it. Record both page IDs and completion state after successful writes.

## Two-page content contract

1. **Multimodal Summary — past 28 dates.** Use the first All Data Streams tab and its `window: past28` response: 28 inclusive Pacific calendar dates ending at extraction time. Retain reviewed Left-leg VAS and MPQ, Oura steps and total sleep, bilateral neural power/amplitude, configured thresholds where supported, sensing contacts/frequencies and visible interval context. Preserve missing-data gaps, source units, raw markers and the default five-observation median overlay. A smoother is a display guide. See [summary semantics](multimodal-summary.md).
2. **Left Leg VAS — exploratory biomarker decoding.** Include the pain-metric panel’s live matched-sample tertile histogram and full-spectrum band performance, separating Spearman correlation and signed AUC into aligned panels for readability. Label the recorded channel/frequency explicitly as Current BrainSense and show its nearest scan-band numeric statistics without implying a newly selected deployment candidate. Use Biomarker Exploration, `/reports/biomarkers/<participant_uid>`, and its explicit Start exploratory analysis action. This is not the separate Closed-Loop Research Review, which requires a selected validated band. Show the returned full-spectrum correlation/signed CV-logistic-AUC scan, channel/target names, independent TD/PSD counts, FDR markers, chance reference and adaptive-band context. Do not invent a selected band, run click-triggered validation, commit a candidate or describe the scan as validated for programming. This analysis uses its default eligible data scope; the summary's 28-date display window does not silently restrict it.

The exact `POST /api/queryBiomarkerAnalysis` controls, with participant resolved from the authorized RCS08 context, are:

```json
{
  "source": "both",
  "LabelMetric": "left_leg_vas",
  "LabelStrategy": "tertile",
  "PercentileLow": 33.3,
  "PercentileHigh": 66.7,
  "MatchToleranceMin": 60,
  "MaxPerRating": 3,
  "RefractoryMin": 2,
  "MatchDirection": "pro_first",
  "UseLiveMatching": true,
  "MatchExtentSec": 30,
  "AllowWindowReuse": false,
  "SlidingWindow": false
}
```

Add `ParticipantId`; do not add `ForceRefresh`. Explicit controls prevent saved browser settings from changing the template. Server defaults currently apply the 5-MAD log-scale exclusion rule; record the returned rule and counts. Reuse a completed result only when request, data/QC/processing, permission and code identities match. A 202 response is pending, not an analysis result. Poll the identical request. The UI's 10–40-second estimate is not a runtime guarantee.

## Provenance and editability

Record extraction time and Pacific display window separately from actual represented source dates. Include source freshness and partial/unavailable disclosures: reviewed survey time, native JSON session boundary, PDF Session Date at minute precision, and latest valid Oura HR/HRV measurement (not app sync). Preserve source/QC exclusions and the returned input manifest, analysis identity and request alongside page IDs. See [source freshness](data-freshness.md).

Keep titles, labels, legends and context editable. Use linked native charts backed by their plotted source values, with durable source-sheet links, wherever the approved template supports the scientific encodings. If an encoding cannot be preserved by a native chart, flag that limitation during Saturday review rather than silently substituting an uneditable image. Native Plotly export can provide a fidelity reference without a screenshot; its source arrays/layout and provenance must remain available. Do not strip thresholds, gaps, units or inferential caveats to fit a slide. Formatting may be adjusted for readability without changing scientific meaning or source-deck instructions.

## Active nightly integration

The existing bravo-nightly-maintenance heartbeat was updated on September 5, 2026, retaining its daily 03:00 Pacific schedule, 05:00 hard cutoff, task attachment, prior integration and data-maintenance responsibilities, and notification defaults. Monday/Friday review drafts go only to Aditya's self-DM; every nightly review performs the conditional >48-hour freshness check. No new timer or Saturday Slack test was created. The current self-review and source-alert instructions below supersede the former activation gate and group-delivery proposal.

## September 5 preparation state

The default Left Leg VAS run completed successfully with six sensing channels, 96 frequency centers and 0/563 estimable rating-clustered tests surviving BH-FDR. The exact response and request, source manifest, summary plot arrays, verified workbook, and prepared native chart/slide requests are in the local `output/slides/2026-09-05/` working directory. This is preparation evidence, not publication or template approval.

The workbook importer failed at its local-file transport boundary. A private backing Sheet was created at `https://docs.google.com/spreadsheets/d/1dQUc5K_RjnMmQK9kZzDs8v58fEWcjNpEdEytUcOsQ7A/edit`. Automatic approval review initially rejected the participant-data write. The user subsequently approved this exact owner-only Sheet, and the data upload succeeded. Owner and sharing were rechecked afterward: aditya.behal02@gmail.com, not shared. No broader sharing was enabled.

Published page IDs are `bravo_20260905_summary` and `bravo_20260905_biomarkers`, after preserved instruction page `p`. Eight native linked charts were verified in the deck and rendered through a Google PDF export. To avoid Google's dense-chart series limit, static neural panels use unconnected raw points plus the ten configured threshold intervals per side; survey/Oura medians remain unchanged. Exact native Sheet readback confirmed 2,386 power and 2,466 amplitude points on each neural side. Google chooses secondary-axis bounds; score/measurement units are explicit in chart subtitles. Static axis labels were shortened to avoid clipping. Full settings remain in the backing Sheet.

The native chart specifications, page identities, readback checks, and approval state are saved in `output/slides/2026-09-05/publication-evidence.json`. The Monday/Friday automation has not been changed, pending layout feedback and consensus.

### Multimodal layout revision for user review

The user requested a much larger four-row composition: pain, Oura, left neural, then right neural. The existing summary page now uses four nearly full-width native linked charts stacked vertically. Slide/chart titles, subtitles, and all surrounding explanatory text were removed at the user's explicit request; source provenance and settings remain in the backing Sheet and local evidence. Only chart axis labels remain, including separate native right-axis labels for MPQ, sleep hours, and stimulation mA because Google's chart renderer omits those secondary-axis titles. Date axes are aligned; raw points, median overlays, settings observation guides, and threshold traces are retained. This layout revision does not alter the instruction or biomarker pages.

The latest revision specification is `output/slides/2026-09-05/stacked-summary-layout.json`. Following the user's warm/cool contrast request, REDCap uses teal VAS (#007F86) and coral MPQ (#D65A45); Oura uses violet steps (#7251A2) and gold sleep (#B58A16); both neural hemispheres retain blue power (#356E9B) and orange amplitude (#B86C2D). Y-axis titles, numbers, and vertical axis lines match their series. Settings observation guides remain red and thresholds gray. Median overlays match their raw-series colors.

VAS must use its complete 0–100 scale and MPQ its complete 0–45 scale. Live Google Sheets readback showed the secondary-axis settings and title colors were being discarded. To preserve native linked charts while enforcing the intended scales, separate helper columns map right-series values affinely into the primary-axis display coordinates; the original data columns remain intact. Editable native Slides axes replace the default y-axis titles and numbers. Right-axis display ranges are MPQ 0–45, sleep 0–12 hours, left stimulation 0–5 mA, and right stimulation 0–4 mA. The latter three are display ranges, not instrument limits. Each chart retains one invisible original right series to preserve the chart margins. Every represented numeric helper value was checked against its original value and mapping (58 REDCap, 52 Oura, and 2,466 amplitude values per hemisphere; zero mismatches).

Construction details, exact mappings, native axis object IDs, and verification are in `warm-cool-full-scale.json`. After changing source ranges, reapply the chart specification and verify live series styling: the first Google update reset new-series styles to defaults. On subsequent updates, recompute and verify the native axis annotations against actual exported chart bounds; do not reuse annotations after changing chart geometry or scale. These annotations must not mislabel untransformed data.

Every 28-date summary window must show all 28 dates on each of the four x-axes. Use MM/DD, angled 45 degrees, with the largest readable fonts that fit. The current date labels use 12 pt and y-axis titles/numbers use 15 pt. Label the final panel R MD Thal (LSB). Dates run August 9–September 5 and align to each Pacific midnight in the plotted window. Source chart positions and series are unchanged. Label construction and exact dates are in `daily-axis-labels.json`; 28 unique consecutive dates per panel were verified. Regenerate these labels for each update date and derive positions from the actual chart bounds. Cover only the superseded weekly x-axis labels, preserving the y-axis zero labels and plotted data.

Draw configured control thresholds on the last two neural subplots only during source-supported closed-loop intervals. Single mode shows its one threshold; dual mode shows both available thresholds. Missing lower-threshold evidence in a dual-mode record must not be relabeled single mode or filled by inference. The current draft has ten threshold segments per neural side, reinforced with dark dashed lower and dotted upper native lines at the original numeric heights and interval boundaries. Current dual values include 1/167 and 166/167 LSB; the latter nearly overlap at this overview scale. Both sides use the L GPe control signal during the shown closed-loop intervals. Preserve gaps and omit sensing-only/paused periods. See `final-axis-threshold-refinements.json` for exact values, intervals, geometry, and final typography.

The full exported deck is `renders/closed-loop-thresholds/presentation.pdf` with its summary preview at `renders/closed-loop-thresholds/slide-2.png`. All three rendered pages were inspected; `raw-final-dates-thresholds.json` preserves the final native deck and the issue checker reported no findings. This is a revised draft for review, not template approval or authorization to activate the recurring slide updates.

### Stimulation settings between observation guides

The user subsequently requested stimulation settings between transitions on deck slide 2 (the multimodal summary). Editable labels now sit within the corresponding intervals of the final neural panel: group, fixed/cycling/dual closed-loop mode, and left/right amplitude or configured closed-loop limits. A shared chart label states 55 Hz and left/right pulse widths of 100/150 µs. The cycling interval states 6 minutes on / 12 minutes off. These are observed configurations; guide boundaries must not imply exact confirmed transition times. Identical group D settings across the three closely spaced September 2–5 observations share one label, while the separately supported threshold intervals retain their original boundaries and gaps.

Regenerate labels from each update's source settings rather than hard-coding this example. Preserve unconnected raw neural data, threshold evidence, four-row chart geometry, full score scales, and every MM/DD date. Use open plot space without opaque labels covering data. Construction and seven source intervals are in `stim-settings-labels.json`; the final native deck is `raw-stim-settings-final.json`. The current reviewed render is `renders/stim-settings-final/presentation.pdf` and summary preview is `renders/stim-settings-final/slide-2.png`; all three exported pages were inspected. This remains a template draft awaiting user feedback, with recurring slide updates still inactive.

### Shared top settings axis (latest revision)

The user moved the stimulation annotations to a shared top axis above the third subplot, covering both hemispheres. This supersedes labels inside the fourth plot. Show unchanged contacts, 55 Hz frequency, and left/right 100/150 µs pulse widths once. Use contact notation C+2− on the left and C+9−10− on the right. Per the user, write C+ followed by each active ring number and polarity; reserve a/b/c suffixes for directional-only contact configurations. Do not expand complete rings into abc notation in the slide labels. Interval labels show group, no cycling or cycling durations, or single/dual closed-loop mode as source-supported; amplitudes are ordered L/R in mA. Closed loop uses explicitly labeled configured min–max ranges. Do not infer state amplitudes from those limits or paused amplitudes. Keep the actual threshold segments on both neural plots and include their numeric values in the closed-loop annotation, per the subsequent user request. Recompute all common/interval settings from each new source window.

The settings strip uses separate space above the neural panels. Plot y geometry is 85 percent of the previous layout, with the neural panels shifted down 60 pt; native annotation heights follow the same mapping. Editable date and axis font sizes remain unchanged. Neural target/unit titles use two lines to avoid overlaps. The final construction is `stim-top-axis-final.json`; current preview is `renders/stim-top-axis-final/slide-2.png`. All three exported pages were visually inspected. User format approval and activation of recurring updates remain pending.

### Invariant header and explicit BrainSense numbers (current)

Above the third subplot, use a per-side header containing target and only settings invariant across all supported records in the 28-date window. Current text: L GPe C+2−, 55 Hz, 100 µs; R MD Thal C+9−10−, 55 Hz, 150 µs. Frequency, contacts and pulse width were checked against every available settings record. Any changing quantity, including amplitude in this example, belongs in its respective interval instead. Do not assume invariance through unsupported records. Group B is labeled B, followed by 6m on / 12m off cycling, followed by its L/R amplitudes; do not repeat cycling next to B.

Closed-loop context explicitly includes the source BrainSense channel L GPe 1–3 and exact 23.44 Hz center, threshold pairs 1/167 and 166/167 LSB, and min–max L/R amplitudes 2–3 / 1.5–2.5 mA. Both closed-loop sides use the left control signal. Treat September 3 as the note-dated threshold update, with exact time unknown; the native snapshots confirm 1 before and 166 afterward. Do not label this a conflict from date-only ordering. See the subsequent native timing audit below. Source min–max limits are not assumed to define state-specific amplitudes. Regenerate values and any uncertainty label for each update.

Current specification: `invariant-brainsense-final.json`; native readback: `raw-brainsense-numbers-final.json`; preview: `renders/brainsense-numbers/slide-2.png`. Native verification confirmed exact labels, 112 daily date labels, 20 configured threshold segments, four linked summary charts, and unchanged instruction/biomarker pages after excluding temporary image URLs. All three exported pages were inspected. The checker has one size advisory for the compact 10 pt closed-loop annotation, accepted after visual inspection without clipping or overlap. No schedule activation or user template approval is implied.

### Native threshold-change timing correction

Read-only inspection of the actual stored Percept JSONs confirms lower/upper 1/167 at September 3 13:08:20 Pacific (Groups.Final) and 166/167 by September 4 16:56:12 Pacific (GroupHistory in the September 4 report). The clinical note dates the change to September 3 without a time. This is compatible with an update after the September 3 export; an exact programming-change timestamp was not found. The previous disputed annotation was unsupported and is removed. The date-only note currently becomes midnight in RedcapHomeAdjustments, producing a misleading same-day conflict label; this inquiry did not change that backend or infer exact threshold timing. Future plots must not use this midnight as an established activation time. Audit: `threshold-change-native-audit.json`; corrected preview: `renders/threshold-change-corrected/slide-2.png`. All three exported pages were inspected.

### Group date ranges and Brain Sense prefix

Each interval label includes its MM/DD date range in parentheses: A (08/09–08/12), A (08/12–08/18), B (08/18–08/23), A (08/23–09/02), and D (09/02–09/05) for the current window. Regenerate dates from displayed interval boundaries; same-day transitions legitimately share an end/start calendar date. These labels do not establish exact activation times. Prefix the closed-loop sensing line with Brain Sense:, currently Brain Sense: L GPe 1–3 · 23.44 Hz. Current specification: `group-dates-brain-sense.json`; current preview: `renders/group-dates-brain-sense/slide-2.png`. All three pages visually inspected.


## Current dated review-draft layout and delivery

Both update pages display the Pacific update date in MM/DD/YYYY, currently 24 pt. The multimodal page reserves a header under the date for “Latest data (PT)”: latest reviewed REDCap survey, native Percept JSON/PDF sessions, and valid Oura measurement, retaining unavailable/partial coverage. These are represented-data timestamps from the authenticated sync Status API, not upload, export, fetch or job times. Show years on source dates when needed to avoid ambiguity across a year boundary. September 5 example: REDCap 09/04 16:45 PDT; JSON 09/04 16:57 PDT (partial coverage); PDF 09/04 16:56 PDT; Oura 09/04 11:47 PDT. Evidence: latest-slide-source-times.json. Do not hard-code these for later runs.

Slide 3 now has two analytical sections: a matched-neural-sample pain histogram on the left, and separate aligned full-spectrum Spearman rho and signed grouped-CV AUC panels on the right. Native linked charts remain editable. Complete VAS 0–100, rho −1…1, AUC 0…1 scales; AUC chance 0.5; all six channel curves; current sensing band guide/points; and adaptive-range context remain visible. Use a clearly distinguished current-channel color/weight and a six-channel legend. Numeric axis labels are 12 pt, titles 12–15 pt, current values 20 pt, date 24 pt, settings 12 pt. Verify all labels at normal presentation size; never accept wrapped dates, overlapping scales, clipped numbers, or unlabeled units.

The user explicitly selected the existing Spearman result rather than a new Pearson calculation. Current BrainSense is L GPe 1–3 at 23.44 Hz; the closest existing 5-Hz scan band is centered at 23.5 Hz (21–26 Hz). This scan gives rho 0.4481592876, signed AUC 0.6735250737, rating-clustered BH-FDR q 0.4359489618. There are 0/563 FDR survivors for this Left Leg VAS run. Recompute the annotation from the current source and request; do not carry forward the numbers or confuse them with the NRS analysis. This is descriptive exploratory scan evidence, not click-triggered mixed-effects validation or a deployment commitment.

The visible settings block records: Left Leg VAS; all eligible records; both TD and PSD sources; aggregate all; sliding-window analysis off; PRO-first matching; ±60-minute tolerance; max 3 PSDs per rating/channel; 2-minute refractory setting explicitly inactive in PRO-first; live matching on; 30-second TD window (nearest ten 3-second tiles); no window reuse; PSD median within ±60 minutes; fixed tertile 33⅓/66⅔ cuts with middle excluded; 5-Hz bands at 1-Hz steps; calibrated LSB CS1–4; 5-MAD exclusions on log power and raw pain labels; log-power logistic fitting; rating-grouped CV up to five folds; rating-clustered BH-FDR family and adaptive/bridge-range context. Keep the exact request, input manifest and response alongside the derived charts. Histogram counts are matched neural samples from the live preview; they are not independent pain-report counts or every band’s fitted sample size. The September 5 histogram has 479 low, 451 middle-excluded and 481 high samples, cutoffs 51/66. Band inference uses its own channel-specific eligible records.

The current native chart-data readback verified all 2,037 cells of the split performance source, including 1,427 numeric values, without a discrepancy; histogram totals matched the live preview. Source plans and construction are in output/slides/2026-09-05/. Do not reuse an old transform with scaleX/scaleY set to 1: Google normalizes text boxes to a fixed intrinsic size, so translations must preserve the actual current linear transform. When moving timeline panels, preserve date-label dimensions and rotation to avoid MM/DD wrapping.

### Slack: Aditya only

Private review destination is Aditya’s verified existing Slack self-DM D076Z8T5EUX (user U077G9W0TL4). The earlier four-person group destination is superseded and must never receive automated posts. On Monday/Friday, after the actual deck pages pass data/visual checks, export those two pages and upload the multimodal PNG first, then the biomarker PNG, each titled with MM/DD/YYYY and Draft for review. Put a concise review notification and the Google Slides link with the first upload. Do not forward on Aditya’s behalf. Track each slide ID, PNG hash, Slack file/message ID and per-upload completion independently so retries resume without duplicates. Preserve instruction slide p at the front and prior pairs; newest pair occupies the next two positions. No Saturday test Slack post is implied.

### Nightly 48-hour source alerts

Check only during the existing nightly BRAVO review, after its normal sync attempt. Compare exact represented instants with the actual check time; threshold is strictly greater than 48 hours. REDCap uses latest reviewed survey; Oura uses latest valid measurement. Percept combined age uses the later valid native JSON/PDF session; include both timestamps in the message and disclose a missing source. If any stream is overdue or unavailable, send one concise alert to Aditya’s self-DM with all three stream dates, JSON/PDF shown separately, and the names of the overdue/unavailable streams. Missing or invalid timestamps are not zero age. Preserve partial-coverage notices. Record the dated nightly run and Slack message ID to prevent retry duplicates. No separate timer, continuous polling, daytime fetch or outside-window catch-up is authorized.


## Current reminder and schedule (September 5, 2026)

Both published update slides and actual exported PNGs must carry large bold red text: “Reminder for Aditya to ask Prasad about stim plan and Donna for any RCS08 medication updates.” This replaces the earlier Donna-only wording. Keep the plots, numeric labels, source dates and settings legible. See `weekly-review.md` for the active Monday 09:00 Pacific review, two-source visit evidence, conservative integration questions, and every-night unresolved freshness alerts. The nightly window is now 03:00–07:00 Pacific with a four-hour maximum, using GPT-6 Astra / High.

Final readback revision `DPQzYJBjA7jS0w`; actual published PDF and PNGs are in ignored `output/slides/2026-09-05/renders/stim-medication-final/`. Both changed pages were visually reviewed; instruction-page pixels are unchanged. Native checker reports no errors, with the previously accepted 10pt compact closed-loop annotation warning. All original analysis values, linked chart sources, daily date labels, scientific scales and settings are preserved. Reminder fonts are 18pt (multimodal) and 20pt (biomarker), bold red, with manual line breaks.

Metric-title correction: Spearman and Signed AUC headings moved 4pt upward, preserving their 15pt font and all chart/data geometry. Verified actual slide-3 export with clear separation from plot frames; revision `Rzp-4xmTeGaAhQ`. Current rendered PNGs: `output/slides/2026-09-05/renders/metric-title-spacing-final/`. Preserve this heading clearance in future scheduled pairs.
