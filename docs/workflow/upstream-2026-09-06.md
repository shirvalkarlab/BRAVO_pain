# Reviewed upstream integration — September 6, 2026

Aditya removed the 48-hour source-observation hold for this run and future nightly
maintenance. Current compatible source changes are reviewed immediately, Fixel
first and Prasad second. Routine execution needs no new manual approval; source,
scientific, CI and deployment checks still apply.

## Source review and selected changes

Fixel `ef7234dbaf9c2967b98c785656d7b4e797823574` is already an ancestor of the
accepted Aditya release; no incoming Fixel commits remain. Prasad was reviewed
from the last selected baseline `6f66905aa70ecfde40935f9d4c6b6f13cf290b06` through
`e8c200052f718701a301a5ca9a9a47270b3eeea4`. The candidate starts from accepted
Aditya `046e0f6357740c12dd3e2fa38c7d2fb349003a1e` in an isolated worktree.

The selected matcher changes reuse converted spectral matrices and vectorize
rating/window selection and medians. The existing scalar selection remains the
fallback when padded temporary arrays would exceed the bounded memory budget.
Differential tests compare values, assignment counts, ties, reuse, missing values,
quantity caps and empty/default cases with the existing selection rules and an
independent scalar oracle. Array-backed spectra are accepted in both paths.

The raw LSB tile cache is shared between worker processes while retaining
canonical cohort/QC/processing identity, producer code and exact decoded input
content in its keys. It stores no new scientific results and introduces no
background analysis. Incomplete inputs remain an error. Existing single-request
settings-stream reuse is retained; malformed supplied frames now fail explicitly
before settings attribution rather than reaching downstream calculations.

## Held scientific changes and excluded source content

The delay waiver does not establish scientific equivalence for new methods.
Prasad also changes ramp exclusion from 45 to 20 seconds, means versus medians,
slope versus AUC-based response, the calibrated evidence route, and adds a
band/time sweep and three-source comparison. These require separate scientific
acceptance against the canonical local inputs before adaptation; current Aditya
behavior is retained. The recommendation for weekly review is to validate these
as distinct method changes, with independent numerical evidence, before enabling
their new outputs. No clinical or programming interpretation is inferred.

Do not restore upstream direct REDCap pulls, unrestricted source selection,
recording-only keys for caches containing survey inputs, or the upstream
closed-loop wrapper that bypasses Aditya's research-only route. Preserve approved
stored surveys, exclusions, timestamp alignment, calibration assets, participant
facts and `include_planning=False`. Participant preregistration JSON, upstream
built browser bundles and research/handoff anecdotes are not part of this release.

## Acceptance

The existing portable gate remains required, including strict 95% true branch
coverage for changed decision logic. Synthetic matching equivalence, cache
roundtrip/invalidation and settings-frame tests supplement existing canonical
input and authorization regressions. Private live comparison, exact-candidate
GitHub Portable acceptance, deployed image identity and authenticated public
checks remain separate required release evidence. Dated private receipts record
actual results; this document does not assert unobserved CI or deployment success.
