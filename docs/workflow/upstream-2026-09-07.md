# Reviewed upstream integration — September 7, 2026

Fixel development remains at `ef7234dbaf9c2967b98c785656d7b4e797823574`,
already included in Aditya. Review then covered Prasad's changes from
`e8c200052f718701a301a5ca9a9a47270b3eeea4` through
`2a543b648f9e486525cd18ed7cbaa10bcc0ad717` without a source-age hold.

Adapt the Redis memory bound from upstream `b7036bf8`: cap cache memory at
512 MiB and use `allkeys-lru` eviction. Preserve the private backend network,
health check, existing image and restart policy. Redis contains disposable cache
state; authoritative records and research results remain in database/storage.
Review any future use for coordination or durable state before relying on keys
that this policy can evict. A memory limit is not a complete container RSS limit.

The new upstream CacheStore, DecodeCommon and scientific output migrations are
held for canonical-input and numerical validation. Aditya's cache already checks
canonical cohort, QC, processing, producer code and decoded content, with bounded
atomic storage. Do not replace those keys with a recording-only identity, restore
direct REDCap fetching, or import upstream planning/data artifacts. The new Redis
lock also places a context-manager yield inside its exception handler: caller
failures while building after a wait can be caught as Redis failures and cause a
second yield. Resolve that behavior before adapting the helper.

Preserve the distinct method holds recorded in the September 6 review. Cache
status UI depends on the new store and is not an independent patch. These are
content and acceptance holds, not the removed 48-hour observation delay.

Required release evidence remains the full portable gate, exact published-SHA CI,
isolated Redis configuration/eviction checks, actual deployed Redis configuration
and authenticated application checks. Private dated receipts own actual outcomes;
this document does not assert checks that have not yet run.
