# Reviewed upstream integration — September 5, 2026

The user explicitly waived the 48-hour stability hold for this run and requested Fixel first, then Prasad. The active dirty `aditya` checkout was preserved; no branch switch, stash, reset, index rewrite, Git commit, push, or source-line merge was performed.

## Ordered source review

1. **Fixel:** `origin/development` is `ef7234dbaf9c2967b98c785656d7b4e797823574`, already an ancestor of Aditya `0b1de6354ec0319aba85a650d0402646d130df25`. `git log aditya..origin/development` is empty. This stage is an already-integrated no-op.
2. **Prasad:** reviewed the 15-commit net delta from the previously adapted `d745360d898647048213c561d18e30e3064ad8e7` through `6f66905a`. Applied 20 source/test file patches on top of the existing Aditya adaptations, plus selected generic D30/D32 schema corrections. The preflight artifact is `output/upstream-review-2026-09-05/prasad-generic-reviewed.patch`; every selected hunk applied cleanly. It records the raw selected delta, before the additional Aditya fixes below.

## Integrated behavior

- Validation-only three-week record-anchored exclusion, before binarization and standardization; returned estimates identify their exclusion window. Other biomarker scans retain their previous population.
- Response-screen evidence uses the five most recent surviving eras, ordered by timestamp, with explicit exclusion/provenance fields. The capture-separation floor has one definition shared across screening and threshold placement.
- Pure clinic-step, within-visit evidence and band-cluster permutation research helpers; capture-span diagnostics. These do not independently authorize scientific recomputation or programming.
- Replay reports longest observed continuous excursions at amplitude limits. Duty-cycle summaries read the numeric `amplitude_mA` trajectory rather than the string-valued controller `state`.
- The staged live helper selects evidence after rate freezing and carries explicit refusal provenance. Existing report/API routing and research-only `include_planning=False` remain in place.
- D30 accepts a per-attempt rate commitment with backward compatibility; its displayed observer and explanation now match the predicate. D32 can read supplied participant hardware facts when candidate facts are absent.

## Aditya corrections and exclusions

- Fixed an incoming replay defect: removing nonfinite amplitudes would have joined excursions across missing data. Missing points now break runs. Across separate replay segments the longest duration is a maximum, never a sum across gaps.
- Fixed an incoming staged-helper defect: explicitly supplying a different evidence rate bypassed its frozen-rate check. Mismatched, nonfinite, absent or disagreeing frozen rates refuse evidence loading instead of attributing a different configuration's evidence to the selected one.
- Kept current `device_facts.py` and its reviewed caller/session-supplied evidence. **Did not import** upstream participant UUID-specific PI declarations, deployment rate defaults, or a global RCS08 programmed rate/pulse-width table. Upstream D31 consulted that table without participant identity, which would incorrectly grant other participants the same evidence.
- Kept existing D29 and D31 rules. The incoming D29 ring/segmented-contact interpretation was not independently established against this checkout's authoritative device evidence; D31's participant-specific shortcut is inappropriate without locally scoped provenance. Unknown facts remain unknown.
- Did not import `PREREG_RCS08_110Hz.json`: it is an upstream participant-specific research registration, not an Aditya registration or an authorization to collect/program anything.
- Did not copy upstream built browser assets or replace Aditya handoff documents. Source-branch numerical anecdotes remain historical claims, not current local recomputations. Canonical REDCap/Oura/neural source policies, timestamps, exclusions, credentials, permissions and data were preserved.

## Verification

The isolated existing Docker validation image ran without network, appliance volumes or deployment secrets. Initial broad diagnostic run: 824 passed, 47 skipped, 3 failures, all in local synthetic fixtures that incorrectly supplied amplitude numbers as controller states. Those fixtures now supply the actual ReplayResult fields and retain their original amplitude/duty assertions.

After correction, ClosedLoopDeployment and StimOptimizer suites: **709 passed, 42 skipped**. Optional Torch/GPyTorch/BoTorch and unavailable private fixtures account for skips. Concurrent development invalidated the whole-tree fingerprint of that diagnostic run; it is not the final release gate. Focused branch inspection measured new clinic-step code **10/10**, within-visit code **82/82**, window-selection helpers **6/6**, span helpers **16/16**, frozen-rate selector **6/6**, and missing-data excursion helper **6/6**. Prescription coverage was **104/106**.

Final replay/constraint regression subset: **162 passed**. The parent's stable full backend/frontend/coverage, build, deployment and authenticated browser acceptance supply the final release result. No live participant optimizer or closed-loop fit, data sync, database migration, or device programming was triggered by this integration.


## Final parent acceptance

Stable final gate: 1,538 backend tests passed with 47 optional/private-fixture skips; 557 frontend tests passed across 64 suites. Every declared critical unit passed the 95% true-branch threshold (Python 99.70%, JavaScript 99.05%). Candidate `sha256:da9dde16b14e99da67f798b62acb0b390bcf1f34a201a051f09512c198cc1b0d` is deployed as `bravo-local:aditya`; both app services match it, with unchanged migration files and passing database/storage/application checks. Authenticated isolated Chrome source-to-render, zoom/hover/control and responsive acceptance passed. See `multimodal-summary.md` for evidence and the expired native-GUI-session handoff limitation.


## Subsequent user decisions — September 5

- Intended RCS08 deployment is 55 Hz on both sides. This is an intended frequency, not a replacement label for prior 165 Hz results.
- User confirms no pocket adaptor. This is a participant hardware declaration, with provenance; candidate-specific facts retain precedence.
- Current intended configuration is ring stimulation. Verify each actual contact configuration from device logs: an isolated 2a without 2b/2c is not ring stimulation. Do not implement a permanent patient-level D29 bypass.
- The proposed 110 Hz in-clinic experiment is exploratory, still under development, and not adopted as a finalized protocol.
- User approved fixing historical configuration evidence: require the correct participant/device/side/configuration, distinguish saved active-group observations from verified delivery/sensing, and do not convert historical examples into general device limits or automatic compatibility passes.
- Paused-amplitude declarations remain unconfirmed; no new values were adopted.


### Scoped historical configuration evidence implementation

The new `historical_configurations` helper reads the shared native-settings cache without populating it or opening raw exports. Source-owner, device, source-type, exclusions and implant/time eligibility are checked. Only unambiguous active-group snapshots are used. Evidence is grouped by participant, device, hemisphere and the complete recorded group/side settings; source ID, hash, timestamp and Initial/Final phase remain attached. Repeated snapshots are not counted as separate programming events. Stimulation/sensing declarations remain explicit and actual delivery verification stays unknown. No historical evidence supplies or overrides D31 limits. The evidence is returned in the research API manifest; no model was run to inspect it.

Live read-only inspection found 590 eligible source records, 167 distinct configurations (81 Left, 86 Right), one device, 1,108 source snapshots, zero missing caches and 12 excluded sources. Of these configurations, 134 recorded stimulation On and 33 Off; sensing was Enabled in 106 and not recorded in 61. These counts establish cached snapshot coverage, not successful delivery. Compact evidence: `reports/validation/historical-configurations-live.json`.

Fresh deployed service import and participant-context checks confirm bilateral 55 Hz deployment intent and no pocket adaptor. Other D32 group facts remain unknown when not supplied; the hardware confirmation alone does not clear the whole rule.
