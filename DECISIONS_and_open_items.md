# Decisions and open items — the digest

**Compacted 2026-09-19 at the PI's direction.** Every decision row keeps its number here in one or two
lines. The full rows, with every proof, field count and verbatim quote, are in
`docs/decision_log_full_2026-09-19.md`; a decision number resolves there. PI quotes are paraphrased
here with his permission (2026-09-19). Struck rows stay struck: the mistake is the lesson. New
decisions are appended to Part 3 here AND as a full row in the full log.

---

## Part 1 — Standing rules, by subject (the log's current state)

**Signal, units, calibration**
- Pain-report timestamps are California wall-clock; device times are UTC; convert before matching (2). The chronic detector joins on the California calendar day (142). The session matcher caps nothing per report; reports claimed by more than one session are counted and warned about (221).
- Windows with >10% zero-filled samples are dropped (4); this rule now reaches every routine (57-59).
- 60 Hz notch off by default (13). Only the two 256-point FFT modes convert (14). Conversion from the voltage trace: the transform route at 349.10 since 209 (352.62 from 18; recipe and blocks in `routines/calibration.py`, 208); the composed constant is composed, never "measured" (33, house rules).
- Per-participant conversion model is a frozen stored asset with tiered fallback (11); 8.8 Hz R 0-3+ counts only from 2026-03-01 (16); no impedance term (17).
- **Log power enters no calculation, anywhere** (PI, 2026-09-19; 202). The E1 path (202), the pooled full-spectrum path and the pain correlation (204), the outlier rule and cross-check (205), the aperiodic fit (206, deleted) are done. The frozen log-log model was explored (207), the constant refit instead (208), the Biomarkers page's calibration panel draws the calibration in effect from the recipe (212), and the model and its asset were deleted (218; `DEVICE_percept_rc.md` §10 keeps its numbers).
- **Time is modelled nowhere**: drift is a current effect in a patient >3 years into disease; no age penalty, no time input (193-196).
- **Every view and product starts at the device's implant date** (RCS08: 2025-07-16 18:06 UTC, the device record): measurements before it are dropped where they are read, the setting in force at implant is kept and moved to it; the rows stay in the database (260). The tiles and PSDs are cut at it too, a guard that removes nothing on RCS08 today (289).
- Never say "spectrum" bare; name the quantity (115, house rules).

**Statistics and verdicts**
- Discrimination intervals are signed, never folded; the lower bound may fall below 0.5 (7, 8, 19). Validation is forward-chaining out-of-sample (12).
- Gates have three states; a gate that goes green on absence of evidence is unsafe (9). A not-assessed condition still blocks but is counted separately in the wording (176).
- "Established" means the point sign; intervals and p stay as caveats; verdict flagged provisional when any interval spans zero (147). D19 passes on point signs (134); D26 capture checks read the pooled slope and WARN (139).
- Every model fitted offline here holds its test rows out as blocks of TIME with the neighbouring rows dropped from training, the gap taken from the label's own decorrelation timescale, and reports each candidate plainly AND with the stimulation current taken out; an out-of-sample score is never folded, and none of this refuses anything (240). The current is taken out as a SHAPE, named on every answer with the flexibility it spent: a 3-knot spline in the two guards, a straight line in `partial_corr` and the adjusted grid, and a squared term, three kernels or one level per setting available; the removal is fitted on training rows only (241).
- Family-wise correction on the grid: Benjamini-Hochberg over 22 centres per grid, no autocorrelation adjustment (63); the cell interval is a block bootstrap sized like the shuffle (183).
- E1 (current-to-power) is the pooled titration slope, model B: one intercept per run, ramped side's current, single-side runs, other side held at ANY constant (126, 197, 198); both legs of a ladder count, and a streaming restart under 20 s with the current held does not split a run (213). Never pooled across visits at the per-run level (40); curvature tested pooled across visits, cluster-robust, before any switching value on a peaked band, and only on one side of the peak (55, 56, 124).
- The one-band rule: a sensing contact and rate is usable when ONE band falls with current AND rises with pain on the stored Biomarkers grid; no majority rules (199). A usable cell whose qualifying bands all sit on a stimulator harmonic carries a warning, never a refusal (220). The sensing pair must be the two contacts immediately flanking the lead's stimulating contact -- stimulate on 1 and sense 0-2, on 2 and sense 1-3, on 1 and 2 and sense 0-3; on 0 or 3 nothing (217; the device's three configurations per lead). "Rises with pain" means positive with the block-bootstrap interval wholly above zero (supported, 210); the grid's stricter "established" is shown beside it. On RCS08 today, under decision 217's sensing-pair rule, the only two allowed pairs are L 1-3+ and R 0-3+, neither has a band rising with pain, and 0 of 50 contact-and-rate combinations are usable.
- Honest current: a milliamp number is recommended only when three per-speed checks pass (not flat, beats today beyond scatter, ≥6 pairs each ≥5 ratings on ≥2 California days spanning ≥1 mA) (158, 184). Rate ≥55 Hz unless a stated reason (138). Left and right are modelled together (157). Pulse-width pairings are fitted separately by default; a pooled fit over every pairing, the pulse widths as inputs, sits behind the current-map card's toggle (222).
- ~~Reliable change: short-gap (≤1 h) pairwise SD per score, same-minute repeats removed, stim-off pairs kept; a warning, never a blocker (104, 111, 112).~~ Deleted (231): too few pairs, too much scatter to mean anything (RCS08 Left Leg VAS: 12 pairs, pooled SD 11.1, threshold 30.8 of 100).
- Post-move margin: 0 s by measurement; the 20 s switch stays OFF -- by the PI's ruling after the titration session was run: use as much data as we can (144, 178-179, 191, 196, 217).
- Safety ceiling is PI-stated: 4.5 mA both sides (145, 160). Side-effect score 2 is its own rung, cost 2.0 (165); moderate/severe steps never seed "tolerated" (164); the amplitude-severity statistic is recomputed each request (166).
- Thresholds are placed from the record: median averaged reading ± the design rule's minimum; the tablet's capture pair shown beside (180). Timing: averaging 3 s, onset 30 s (max enterable), blanking 30 s, transitions 30 s, startup 15 s (150, 169); ranges have one home, `DecodeCommon/device_ranges.py` (168).
- Ground truth per row: device band power passing the 99.5th-percentile ceiling table > calibrated trace > composed (tagged) > never uncalibrated; both values and fold ratio where both exist (33, 47, 52).

**The store**
- One store, `BRAVO/modules/CacheStore/`; a second implementation is a test failure (30). The saved 3 s tiles are read or written only for the one input set every page uses; any other request builds in memory (289). Key decides whether to write (26); key from database rows alone (24, 25); no pain rating in any recording-derived key or payload (23).
- Every derived write carries `writer=` and flattened `provenance=`; a consumer whose own output is in the chain is refused, and the refusal raises (31, 39). A refused entry is replaced, not skipped (41). The Closed-Loop `inputs` entry is keyed on the recording set AND the two calibration constants AND a rule version (215); it served a three-day-stale frame under the old constant until then.
- Recordings cached without expiry; pain reports fetched fresh on every build; the held table serves drill-downs only until the next build (22, 78). The Biomarkers page's top timeline is an acquisition timeline: keyed on the recording set and the constants, stored as a raw kind, nothing in it from a pain report; the rating-centred sample index has its own endpoint (216).
- Parquet+zstd tables, npz arrays, pickle otherwise; CSV/JSON excluded because the therapy timestamp is timezone-aware (29). Redis: 512 MB, allkeys-lru, locks/freshness/small values only, protocol 2 (27, 28).
- One entry per participant per kind by default; `KEEP_NEWEST_BY_KIND` names the kinds that keep more (sweep kinds 12; the stability answer 12, one per grid, 248; simulation, design rule 6; two Stim Optimizer responses) — six writes reporting success and one file on disk is the failure this prevents (107, 128, 146, 152).
- A test that touches the store clears or restores ONLY while its own override is in force, and never reaches a launcher that writes to the production root; three times a test has reached that root (96: real jobs started; 116, 129: the root cleared).
- Memos are keyed on the participant's current recording set, not the participant (130).

**Pages**
- Closed-loop figures draw once and restyle by trace index; rebuilding on interaction reintroduces the flash (5, hard). Plotly `doubleClick:false` and guarded purge on the Biomarkers heat maps (91).
- The Biomarkers module does not deal in what stimulation does to a biomarker (108). The heat maps are the headline; nine lengths, 1 s to 60 s (170); the Closed-Loop "Choose a band" card reads the SAME stored grid under the Biomarkers page's settings tag, the clinic-sheet switch included, and the stability answer filed for that grid (131, 248).
- The chosen closed-loop band is held on the server, append-only, naming who chose it; the browser keeps a mirror and says when it holds the only copy (249). A band picked on the grid carries its grid's pain score, split and window to the deployment summary (254); a red-outlined button adds the clinic-sheet ratings to it (258). The sign-off card is the last card (258). Both pages: no text under 11 px, no grey under 4.5:1 (258).
- Every band-to-pain reading on the Closed-Loop page (E2, E3, the stability card) is computed on the pain score chosen in its dropdown, NRS by default; E2 reads each piece's own settings period's ratings (290, 292).
- A page judges a band on its own side (141). Every card's text is pinned by a fixture render test asserting what a clinician must read and the retired words' absence (174).
- Stim contact notation is the clinic sheet's own, `L C+2-`; sensing pairs keep superscripts (181).

**Process**
- Commit identity: Prasad Shirvalkar, `prasad.shirvalkar@ucsf.edu`, inline `-c` (49). Default branch `PS_closedloop_deployment`; `v3.1.0` is a label (177). No worktrees: the container mounts only the main checkout (102, 177).
- Written record: superseded documents go to `docs/archive/<date>/` by `git mv`, never deleted; no suite count in any document (34). Plan kept with planning-with-files, inject-smart, committed (36); autonomous mode with attestation allowed per plan, gated mode never (223).
- Only code changes are open items; clinic notes live separately (open items, 2026-09-10).
- Every check ships with its live proof: field count and difference count, never a tolerance; alternating timings; the first live run of step 8 wrote nothing while every test passed (41).

---

## Part 2 — Open items

**On the PI**
- **30. Titration session**: RUN on 2026-09-16, both sides (the PI, 2026-09-20; 213): the record holds 8 settled currents on the left ladder and 10 on the right. The 20 s margin switch stays OFF by his ruling (217). Closed.
- Ruled 2026-09-21 and built (218-222): the frozen model deleted; the zero-caller chronic-detector routines deleted; the harmonic rule a warning, never blocking; the matcher's report cap stays out and the sharing warns; pooling across pulse widths behind the current-map card's toggle, default separate. The clinic implication of 217 (L 0-3+ needs L C+1-2-) stands; the full-spectrum matrix keeps rebuilding on a new report. The audit code leftovers were ruled on and built the same day (223-227): nothing of that list remains open.
- Item 15 remainder: the layout question is answered — decision 258(a) made the sign-off card the last card on the page, below the CL-DBS simulations, with the jump links following. Three of the seven June audit items in that batch are still open (found 2026-09-25, not fixed by 244): the odds ratio per time block prints with no interval, the stability result does not say when it was measured, and one rating can be split across two time blocks. [2026-09-26: all three are built -- when the stability result was measured (284), each state's odds ratio with its interval and one rating in one block (291); nothing of item 15 remains.]
- **Next visits (2026-09-26)**: the PI answered the visit-length and home-sequence questions -- a visit is at most 2 hours door to door; the multi-day home sequence is covered by research ethics; the ladder starts with a rise to 3.5 mA; the patient files the REDCap survey at every rating of a stimulation-off block; 60 Hz is covered. Still his: which of the four stimulation-off options, (a) to (d), since the off block as first designed cannot fit 2 hours; and one wording, the 2 mA home hold's condition now that the ladder delivers no 2.0 mA (`artifacts/protocol_2026-09-25_rate_swap_and_down_first_ladder.md` sections 6.1 and 11). Medtronic and clinical-team questions are listed there.
- **Numbers read from a short tile copy that were not re-measured (289)**. Tonight's (2026-09-25 20:03 to 09-26 03:35 UTC) are re-measured in 289-301 and the P-19 and P-12 corrections, except: P-12's band-power cells (its E3 numbers reproduce) and 285's mixed-model odds ratio. Two earlier windows: 2026-09-11 20:08 to 09-12 05:15 UTC (128's first simulation numbers, committed 41 minutes in; 130 and 131 were committed after it closed) and 2026-09-17 20:06 to 09-20 08:12 UTC (194-208; of these the heat-map counts of 199 and 204, and the titration-ladder points behind 196-198 and 202, are built from tiles; 208's recipe, 205's chronic detector and 194-195's pain-only fits read none). Those describe code since replaced, so they cannot be re-run as they were; their before/after comparisons stand. Today's full-tile ladder points hold 8,353 rows, as decision 213 counted.

**Rewritten, not open**
- 10. The 110 Hz "heat map" does not exist in the platform; the pre-registration (`PREREG_RCS08_110Hz.json`, read by nothing) is unrun and its own counts do not reproduce; the five left-only days carry one current each (109). Re-measure before reopening.

**Notes for the clinic, not items**: 55 Hz has one left-only visit day on the left electrode, nothing to confirm against; the device never computes its own FFT while current is stepped, so that source is empty for every ladder.

**Resolved by consolidation, do not re-open**: disk reads are 4.8% of the page (the cost was 72 million channel-name normalisations, since fixed); the module README edits are absorbed; matching is 0.2-1.7% of Recompute (the costs are the 1,000-shuffle test and the 90-band scan).

Closed items 1-9, 11-14, 16-29, 31 are one line each in the full log's Part 2 with their resolving decision.

---

## Part 3 — Every decision, one line (number, date where it matters)

Refs are commits or PRs; `→N` means superseded by N.

1. R interface converter built without the frame converter (`33f45a5`).
2. Pain-report timestamp is California local; parsing as UTC smears matches 7-8 h (`a4e4e68`).
3. Concatenation repair kept; re-decode matched 67 of 67.
4. Windows >10% missing samples dropped; zero-fill deflated band power.
5. Figures draw once, restyle by trace index; memoised params — hard constraint (`255e0ef`).
6. Net-benefit cut-point rule removed; it equals the cost rule (PR #3).
7. Discrimination interval not folded (`c50be37`).
8. Per-epoch AUC signed; orientation fixed once on the pooled result (PR #5).
9. Three-state gates; stability gate abstains when its test cannot run (PR #5).
10. Recommendation single-sourced; recommended vs programmed stated; abstain on ramp guidance (`b0597f8`).
11. Frozen per-participant conversion model with tiered fallback; modelled switching value flagged (`771f3c2`).
12. Forward-chaining out-of-sample validation; in-sample hid reversals 0.55→0.24 (`d9d58a4`).
13. Mains notch off by default (`f915257`).
14. Only 256-point modes convert (`f915257`).
15. Direct spectrum-to-device route; round trip adds nothing, 0.8% (`f915257`).
16. 8.8 Hz R 0-3+ from 2026-03-01; earlier data sit in a settling transient (`e9d7a80`).
17. Impedance term rejected, p 0.26 with grouping (`a9c3a01`).
18. Transform route at 352.62 is the primary trace conversion (r 0.9927) (→209 →211: 345.59).
19. Moving-block bootstrap, effective n, de-folded lower bound as gate.
~~20~~. →21. Cut-point converted through the frozen model: a units error (z-scored log fed as linear).
21. Fallback models the device power line off the raw trace at the cut-point's own centre; never converts the switching value (`09798f7`).
~~22~~. Amended by 78. Recordings cached without expiry stands; "reports never cached" now has one exception, the read-only drill-downs; no cheap freshness check exists.
23. No pain rating in tile file or key; report change alters 19,464 values and causes 0 writes.
24. File key from database rows alone, 0.33 s.
25. Key carries event metadata, decode stamping, every constant.
26. Key decides whether to write, not the caller.
27. Redis for locks and small values; big products stay files (0.05 vs 0.14 s).
28. Redis 512 MB, allkeys-lru, both compose files (`b7036bf`).
29. Parquet+zstd tables, npz arrays; CSV/JSON lose the timezone.
30. One store implementation; duplicate deleted.
31. Provenance chain and constructed-cycle proof before any write-back.
32. Band range is all 22 centres 8.5-29.5 Hz; "8-20" was a slip.
33. Ground-truth rule with ceiling check and fold ratio.
34. Record consolidated; 50 documents archived by `git mv`; no suite counts in documents.
35. Pain-report snapshot keyed on content, read by no page, history kept.
36. Plan in planning-with-files, inject-smart, committed, no attestation or gate.
37. `therapy_settings` raw, keyed on source-file rows; `therapy_pain_matched` raw-derived; 0.01 vs 33 s.
38. Sweep writes two tidy tables and serves its response on key match.
39. Store refuses derived kind without writer; one module object under both import spellings; `exploration_ladder`.
40. Amplitude effect per run and band, never pooled across visits at this level.
41. Stim Optimizer reads as consumer, writes five products, refused entry replaced, code digest in key; first live run wrote nothing while tests passed.
42. DecodeCommon is real code on both runners; naive start time mirrors platform.
43. per_pro readers use channel_index behind a switch; 0 differences; 54 vs 65 s.
44. Tile builder reads prepared traces; under 1 s saved of 37.
45. Deployment report accepts the calibrated frame (had raised on every candidate).
46. Redis build lock on the tile key: four cold requests, one build, 41 vs 368 s.
47. Device route ceiling (provisional →52); `ground_truth_verdict` written and read.
48. `cache_status` on every module response; one line under each recompute control.
49. Commit identity: the PI's own name and UCSF address.
~~50~~. →51. Per-recording spectra into the store: "read by no page" was true and beside the point.
51. Assembled matrix into the store; per-recording directory stays, stamp-served (0.35 vs 3.1 s).
52. Device ceiling = 99.5th-percentile table per (electrode, centre) from full history.
53. Track E done: `biomarker_psd_matrix` raw kind; row order follows collection order.
54. Result cache bounded by bytes and resident participants; do-not-edit lifted once.
55. Switching value on a peaked band only on the one-to-one side; pooled cluster-robust curvature test first.
56. Pooled curvature test built and run live; mixed result, one contact 13 points over 4 visits.
57. Entangled routine never applied the zero-fill rule (review).
58. Fixed: missing mask carried through the adapter.
59. Proof: 31 of 386 recordings excluded; 29,704 values moved; a best band moved.
60. Recompute fires one request per press, watched live.
61. Do not fold the entangled routine into the sweep; four axes differ.
62. Heat-map redesign: Option 2, search-first.
63. Family-wise correction: Benjamini-Hochberg, no autocorrelation adjustment.
64. Match direction wired into the sweep; BH built; a missed rule-version bump served a stale response.
65. Track D go-ahead: BH label on the CL grid; forbidden bands stay selectable, greyed.
66. Track A built (SVG grids, hover/click, contact strip); drill-down outlier bug caught.
67. Track D built; device-rules column not built (rules need amplitude etc.); bare import broke the container.
68. Track D leftovers closed; one `MatchDirection` helper.
69. All-None family test; `bh_fdr` matches statsmodels to 1e-10.
70. RCS08 had zero study links; Join Study, grid seen live, grant revoked.
71. `native_lsb_by_channel` added; the two live spectrum builders left unmerged.
72. Merging the builders not recommended: same DSP, different sampling unit, four treatments.
73. Shared matching layer designed; two of four matchers had no independence rule; crossover trial is what the field has and we cannot build.
74. Direction consistency check built (chain-rule signs); contact filter bug caught.
~~75~~. →231. Reliable-change floor built; not assessable on RCS08 then.
76. `DecodeCommon/matching.py` ported from the richest matcher; 600-trial equality.
77. Older scatter/violin panel removed; hover 2.9 s → ~1 s via recordings memo.
78. Amends 22: report table held for drill-downs until the next build.
79. Six backend efficiency fixes, each with a 0-difference proof.
80. Frontend dead code removed; purge-on-update and defeated memo bugs fixed.
81. Four Plotly render-manager findings evaluated, none worth 58-file blast radius.
82a. CL module audit: sign-off card read the retired `stim_stable` flag; two checks unwired.
82b. Biomarkers grid on the shared cache; one Recompute control; background prefetch of other scores.
83. `stim_stable` gate reads the three-way verdict; `adaptive_band` checks edges; two dead files deleted.
84. Host suite run in the container: one failure was an environment mismatch; the test wiped the cache (→116).
85. Participant id passed to the store; cross-participant eviction fixed.
86. One pain-score dropdown; Y-axis shows delivered lengths; Medtronic labels; gunicorn needed SIGHUP.
87. Plotly heat maps with side panels; requested vs delivered length carried separately.
88. Gridlines off; shared `heatmapHeight`; modebar off.
89. Sizing regression in 88 corrected.
90. Title and stats rows; plots fill width; thumbnails L then R; pooled warning hidden.
91. Native Plotly scatter and violin; click listener and double-click crash fixed.
92. Availability endpoint cached on recording set and report digest: 8.4 s → 0.6 s.
93. "How to read this" drawer deduped and reordered, 669 → 436 words.
94. Sweep outliers: fixed per-(contact, centre) ceilings applied per 3 s piece with backfill; 0.436% excluded.
95. Dead export button → "Open this grid in Closed-Loop".
96. Stability grid in a detached process after the grid lands; page key vs run key mismatch found only live.
97. Daily stability precompute loop; stopped-early run refused.
98. Stability column proof: 5,148 fields, 0 differing; page 291 s → 10.6 s.
99. "Cannot tell" example re-anchored to 17.5 Hz; the p depends on band width.
100. CL module: cache status on every return; wrong-key handler; zero logger calls fixed.
101. CL `bravo_service.py`; catch-all logs (the 5-day silent outage); note/reason mismatch fixed.
102. Naive timestamps are UTC on every machine; gitignore `_agent_bridge/_*`; agent worktrees were months stale.
103. Consistency check wired; `within_visit_pooled_shape` stored so cold and warm agree.
~~104~~. →231. Reliable change wired as a warning; wrong-frame defect caught by reading values (threshold from 0.93 spread is 2.6).
105. `within_visit_band_scores` deleted after a live run showed nothing unique.
106. Device-snapshot share marked per cell; R 0-3+ 79% snapshot-served, length axis 7x flatter.
107. Every score precomputed; store kept one entry per kind → `KEEP_NEWEST_BY_KIND`; fan-out guard.
108. Item 10 closed: Biomarkers does not model stimulation effects.
109. No 110 Hz figure exists; pre-registration unrun; counts do not reproduce.
110. Fake patient and per-rating overlay deleted; neighbouring-line trap noted.
~~111~~. →231. Reliable change = 1 h pairwise SD per score, repeats removed; panel added; NRS threshold 1.01.
~~112~~. →231. Stim-off pairs stay in the floor.
113. Sign-off card embeds browser-side figure snapshots; grid cache slot collision; stale workers.
114. Timeline circle equals the many-centre reader at its centre, 240 of 240.
115. Many-centre reader deleted; never write "spectrum" bare.
116. Host suite's one failure fixed; it had been clearing the production cache every run.
117. Pooled-PSD builder on the shared matcher; 0 differences; dead branch crashes.
118. `align_pros` already shared; two selectors are not matchers; `max_per_rating` open.
119. Histogram caption names the score and the record total.
120. Timeline's own match window removed.
121. Dash markers removed; snapshot route honours 30 s per snapshot on the length axis.
122. CL grid is a 22-row heat map with radios; hemisphere commit bug fixed.
123. CL prose folded; ledger to counts strip; "Sign agreement", "Full parameter recommendation", three column names.
124. E1 stays a straight-line slope; literature and RCS08 support no peak yet; titration protocol → item 30.
125. Three-source panel pooled across visits from stored per-run points; prefetched.
126. E1 is the pooled titration slope; historical estimate kept beside it.
127. No row; numbering gap in the original log.
128. CL-DBS simulations card, M0-M3, 3 s pieces on the device clock; per-candidate entries.
129. A test emptied the production store; clear only under the override.
130. Memos keyed on recording set identity.
131. CL grid reads the Biomarkers page's own stored grid by settings tag; built on demand.
132. Device rules read rate and pulse width from programmed settings when the candidate lacks them.
133. D16 impedance from a fixed-current test; automatic low-current reads are spurious fails.
134. D19 passes on point signs.
135. D30 answered from the device's active group.
136. Device facts rebuilt daily from ingested reports; two scanner misreads fixed.
137. 123 dead tests deleted; two-stage path wired behind a flag.
138. Stage 1 recommends ≥55 Hz unless a stated reason.
139. D26 checks read the pooled slope and warn.
140. Stim Optimizer first build 51 → 10 s; BLAS to one thread; no GPU in the container.
141. CL page judges a band on its own side; 20 s margin first wired.
142. Chronic detector on the California day; 26.2% of ratings moved; memo and key fixes.
143. Stim Optimizer review: own-side pulse width; one module object per package; stream anchors off.
144. 20 s margin behind a switch, OFF: two removed points flipped a verdict.
145. Ceiling PI-stated; 3,696 lines of dead modules deleted; unread Biomarkers blocks removed.
146. Titration session card designed from the record (amended by 160); two Stim Optimizer responses kept.
147. "Established" = point sign; provisional flag.
148. Timing parameter ranges found (FDA table 2); record-derived recommendations.
149. Ranges wired into both modules; "programmed today" shown.
150. Six-method contest; Kalman design rule adopted; averaging 3 s for response time.
151. Simulation card replays programmed and recommended timing; undone switches counted.
152. T3 design rule built; its kind added to `KEEP_NEWEST_BY_KIND`.
153. T4 occupancy check; a sign error caught against the contest's report.
154. T6 startup dip, two methods shown, not reconciled.
155. T5 block bootstrap, vectorised by precompute; 36-90 s reproduced bit for bit.
156. T7 gain wiring confirmed on constructed data; waits on the titration session.
157. Left and right modelled together; arm strip removed; injected "coordinator" messages ignored.
158. Honest-current rule; home titration schedule; two claims in 157 corrected.
159. Current map cards on the page; surfaces on the response.
160. Ceiling 4.5 mA; ladder redesign: ramp+test rows, 1.0 mA down legs, joint corners, sheet rows.
161. Clinic sheets ingested as their own stream; Excel turned "8/10" into a date.
162. Titration card and clinic section drawn.
163. "Make Google sheet" button; openpyxl `value=None` is a no-op.
164. Moderate/severe steps excluded from tolerated anchors.
165. Score 2 is its own rung, cost 2.0.
166. Amplitude-severity statistic recomputed per request; typed number retired.
167. Clinician review: 19 findings; root cause "computed, stored, not on the page".
168. Three Criticals fixed; device ranges one home in DecodeCommon.
169. Ranges reconciled with the tablet; onset max 30 s; 5-minute rows beyond the device.
170. Onset grids capped at 30 s; sweep ends at 60 s, nine lengths.
171. Heat-map text and layout to the PI's wording.
172. Retired-table notes reworded for the heat map.
173. Corrected-statistic line beside the violin; no repeated n.
174. Referent audit executed: 10 of 12 fixed; render test per card.
175. "What would change this" item reworded; second in-clinic plan table removed; sign-off duplicate.
176. Four leftovers: capital, stale comments, dead builders, gate wording (both copies).
177. Default branch `PS_closedloop_deployment`.
178. 20 s margin ON.
179. 178 reversed the same night; OFF until a titration session decides it.
180. Thresholds placed from the record; capture pair beside them.
181. Google Sheets via the PI's OAuth token; `L C+2-` notation; centred cells.
182. Clinic sheets synced from Drive daily.
183. Block bootstrap for cell intervals.
184. Coverage counts occasions: ≥2 California days per pair.
185. Stability answers on the Biomarkers grid; one home for the words.
186. Clinic-sheet ratings in heat maps behind a switch, default off.
187. T1 was a misinterpretation; the unread powerdomain block deleted.
188. Hover three lines; Pearson and Mann-Whitney p on the backend via scipy; browser stats deleted.
189. Descriptions folded; pairing line; pulse-width pooling plan (A behind toggle) not built.
190. Duration weighting measured: no recommendation changes.
191. Margin sweep: 0 of 30 settled values change; margin is a min-hold filter.
192. Current map absolute ratings, colour centred on today's setting.
193. Age penalty inert and unsupported; log drift ≠ raw drift.
194. Time as fitted input (→196).
195. Clinic stream without time input (→196).
196. Time modelled nowhere; S7 settled at 0 s; run finder needed the other-side rule.
197. Runs count whatever the other side is held at; 11 → 17 runs.
198. Pooled slope model B kept.
199. One-band rule; usable cells 6 → 4, all on harmonics. [2026-09-26, decision 289: measured while the saved tiles were a short copy (2026-09-17 20:06 to 09-20 08:12 UTC); the counts read the stored heat-map grid built from it and were not re-measured; the usable count has since been set on full tiles by 210 and 217.]
200. Review leftovers C3, C4, C6, C7, T2, T3 built.
201. C6 off-label line removed.
202. No log power on the E1 path; remaining sites listed for the PI.
203. Context compaction: this digest, the full log in `docs/`, shorter CLAUDE.md, house rules and store architecture; handoffs, worker reports, completed plans and generic rules deleted.
204. No log power on the pooled full-spectrum path (the matrix stores raw power under `X`; the stability test, mixed model, per-era ROC and band feature read it raw) or in the pain correlation (`transform="raw"`); 10 of 132 stability verdicts moved, the chosen band unchanged; the correlation's MAD rule on raw power drops more samples (106 -> 93) -- item 3. [2026-09-26, decision 289: measured inside the short-copy window of 2026-09-17 to 09-20; the moved-verdict and sample counts were not re-measured; the stability test's own sample set reads the same on the short and full copies tonight (482 on L 1-3+, NRS), so it may not have depended on the tiles.]
205. The outlier rule and the heat map's logistic cross-check on raw power, 5 MAD unchanged; the log-scale option refused; one-sided trimming of a multiplicative feature accepted with the rule; chronic detector sample set and summary move (AUC 0.554 -> 0.555).
206. The aperiodic (1/f) fit deleted with its `fooof` transform; reached by no page; the device cannot threshold a peak's prominence.
207. Calibration exploration (no code): the frozen model's curvature is between-band gain pooled into one slope; within-band slopes about 1; raw fits agree with the median ratio; the frozen-model rewrite was scratched in favour of refitting the constant on the new data.
208. The June anchor reproduced with the lab's benchmark (n 131, r 0.9927) and extended to 2026-09-03 (n 133 gated, k 345.59, r 0.992); block gate (3 s, 6 readings) and 5-MAD ratio rule adopted; bridge ratio 4.789 refit as raw median 4.755, kept; recipe and de-identified blocks and pairs in the repository.
~~209~~. →211. The transform constant as the midpoint of 352.62 and 345.59, 349.10; composed bridge 72.90; page labels read the served constant; every calibrated number scales by 0.9900, verdicts unchanged; heat-map correlations move at most 0.044 through the ceiling exclusion.
210. The one-band rule's pain leg loosened to "supported" (positive, block-bootstrap interval wholly above zero); "established" reported beside it; usable cells 0 -> 11 on today's data, L 0-3+ Left 125 Hz selected.
211. The transform constant is 345.59 everywhere: the adopted recipe's one median over every block, not a midpoint of eras (209 superseded); composed bridge 72.16; every calibrated number scales by 0.9899, verdicts and the 11 usable cells unchanged.
212. The Biomarkers page's bottom-right calibration panel draws the calibration in effect (the transform constant over every paired block with the recipe's gate and rule, the June reference from the same table, the bridge ratio per centre and contact pair, both constants read from the server) instead of the frozen June log-log model; raw axes; the committed band's own refit draws the constant beside its fit; no source file carries a constant.

---

## Part 4 — Lineage, in brief

PRs #3-#8 built the engine, figures, audit, validation; #9 (`39dfb2f`, 2026-06-29) merged 61 commits into `v3.1.0`; #10-#12 followed; since 2026-09-15 work lands on the default branch directly. Landmarks: `90eb109` calibrated closed-loop band power (verdict 2 → 6 of 50, sign of current-to-power reversed); `958cc89` matcher vectorised 21.4x, 0 differences over 1,080 configurations; `688a185` withdrew an invented constant; `6c3c9f2` corrected brain-side labels by amendment. Two pushed messages (`b700717`, `7ab2d1b`) wrongly say the container path is not a live mount. Full lineage: the full log, Part 3.
213. The 2026-09-16 titration session was run in full but under-counted: the run finder never joined two streaming recordings (the tablet restarted twice, once for 12 s with the current held), and only a RISE into a setting counted, so the first setting of every recording and every down-leg step were discarded. Now recordings separated by less than the 20 s move window with the current unchanged are one record; each setting carries the device's own current before its move; a setting reached by a rise OR a fall is measured and tagged by leg (the PI: pool both legs). Left 9/16 ladder 1+4 -> 8 settled currents in one run; 4 of 15 runs hold 8 or more; run points 4,845 -> 8,353 rows (1,588 falling); E1 on L 1-3+ n 18 -> 33, p 0.21 -> 0.72; verdicts unchanged.
214. The Closed-Loop "LSB & power" card's µV²/LSB cross-check read the stored voltage trace as ADC counts (×0.146) and took a segment-averaged band power; now unscaled microvolts, the transform band power, compared with the constant in effect instead of a 0.01 rule of thumb. On RCS08 the independent pairing (218 streaming sessions against the device's own readings at ~0 mA) gives 0.00283 µV² per LSB = 1/352.7, 0.98× the constant in effect: confidence low -> high; nothing else on the response moves.
215. The Closed-Loop `inputs` store entry (the evidence frame with its calibrated LSB columns) is keyed on the recording set, the two calibration constants in effect and a rule version, not the recording set alone; live, the entry on disk had been written on 2026-09-17 under the old constant and was still being served -- the rebuild has 13,617 more tiles and 90 report fields move (prescriptions, thresholds, E1), verdict unchanged.
216. The Biomarkers page's top timeline is an acquisition timeline: the endpoint reads no pain report, the per-report matched circles are gone, the payload (records, stim, calibrated overview, events, samples) is keyed on the recording set, the constants and a rule version and stored as the raw kind `acquisition_timeline`; the rating-centred sample index the Binarization card reads has its own endpoint `/queryPsdScanIndex`. Measured first: the matching step was 0.6 s of a 5.0 s build; the cost was the per-worker memo keyed on the report digest. Served: memo hit 1.2 s -> 0.015 s, from the store 0.02-0.05 s; a filed report causes zero builds and zero writes; 430,108 retained fields, 1 differing (the span no longer runs to the newest report; the page widens the axis from the live pain series).
217. The readiness screen applies the device's sensing rule: the sensing pair must be the two contacts immediately flanking the lead's stimulating contact (three configurations per lead -- BrainSense tip card pp. 7-8, white paper p. 8, A610 p. 36), read per lead from the cathode in force; a cell on any other pair is refused with the allowed pair named. On RCS08 (left C+2-, right C+1-2-) the allowed pairs are L 1-3+ and R 0-3+, neither of which has a band rising with pain: usable cells 11 -> 0 of 50. The 20 s margin switch stays OFF by the PI's ruling (use as much data as we can).
218. The frozen June log-log conversion model deleted (module, asset, tests); DEVICE §10 keeps its numbers; the calibration endpoint unchanged (3,007 fields).
219. The four chronic-detector routines with no caller deleted (336 lines, 9 tests); `roc_analysis` and `lfp_distribution` stay with their pipeline caller.
220. The harmonic rule on the readiness screen is a warning, never a refusal: a usable cell resting on harmonic bands alone is flagged and its sentence printed; `deployable` never written; on RCS08 today 0 usable so none fires.
221. The session matcher keeps no cap per report; the sharing is counted and warned about (RCS08, 60 min: 41 of 76 matched reports shared, at most 24 sessions); 0 fields differing.
222. Pooling across pulse widths (189's option A) behind the current-map card's toggle, default separate: one surface per speed over every pairing, pulse widths as inputs, read at the pairing in force; on RCS08 coverage passes at 55 and 110 Hz where the separate fits could not, every pooled surface reads flat, nothing resolves; separate rows untouched.
223. Amends 36: `.mode` may carry `inject-smart autonomous`; the plan is attested at init and re-attested after every edit; `gate` stays off.
224. The Binarization card laid out as option C (the PI, 2026-09-21): a top band with the coverage sentence, the match window, the split rule and the match direction, and a timing histogram (each neural sample's signed minutes to its nearest report, one translucent series per source overlaid, the window's 25% tails and any side the direction excludes in grey); the left column restyled, the preview untouched. Backend review: the Compute response's unread per-report band-power value (2.5 MB, 0.35 s) and its 60-minute tolerance helper deleted, 0 of 249,322 fields differing. Report-first stays two-sided; only "Before the report" is one-sided.
225. The per-report band-power reader deleted, both copies and their tests (the PI, 2026-09-21): `availability.per_pro_lsb` with its reference scan and `DecodeCommon.per_pro_lsb_indexed`, 26 tests; nothing on any page read its answer since decision 216; the tier names and the tile-cache matcher stay. Host 1434 -> 1419, container 699 -> 674, 0 failing.
226. The Compute response no longer carries a second copy of the timeline payload (the PI, 2026-09-21): it was a fallback for the timeline and sample-index endpoints (2.7 s and about 7 MB per Compute on RCS08); the builder has no report-dependent path left; the page draws the timeline from its own endpoint only, so a failed timeline request shows as one. Compute 36,570 fields in common, 0 differing, 212,752 removed; timeline 430,108 fields, 0 differing.
227. The audit leftovers ruled on (the PI, 2026-09-21). B1: the Biomarkers page's left refit panel (Welch-based, log-space check, a percentage of the constant in effect from a different recipe) deleted with its endpoint and routine as redundant; the calibration-in-effect panel takes the row. C1: the recipe reports a 95% bootstrap interval on the median ratio, 1 MAD of the raw ratio and a raw proportionality check (RCS08: 345.59, 339.4-350.7, 1 MAD 18.6 = 5.4%, Spearman -0.02 p 0.78 over 133 blocks), drawn and printed on the panel. A2: the band either side of a MODELLED threshold on the Closed-Loop card is that scatter per participant, never the deleted model's log-space 1.26 (L 1-3+ 22.5 Hz: 851-1351 -> 1014-1130); no blocks, no band. D: 49 stale-constant comment lines reworded to name the constant in effect; ten dated historical lines stay. E: the two uncalled statistics helpers deleted (101 lines). Host 1419 / 2 / 0, container 673 / 0.
228. The heat-map cell's scatter and violin come from the same ratings the grid correlated (the PI, 2026-09-21: r printed negative, line drawn rising): the drill-down never read the clinic-sheet switch, so with sheets on it matched the REDCap ratings alone (L 1-3+ 22.5 Hz 45 s: 97 points, r +0.104, against the grid's 172 and -0.033); it now merges the sheet ratings as the grid does and tags each point, drawn hollow; live test RED (97 vs 172) -> GREEN (r equal to 2e-15, slope sign = sign of r). Also: the calibration panel's jest suite had failed to parse since bd46556d, which claimed jest passed; repaired.
229. Exploratory search on L 1-3+ (the PI, 2026-09-21: is any band positive with pain under any window or knob?): 252 settings (windows 2-120 min, three directions, caps 1/3/10, reuse, clinic sheets) through the page's grid routine, store reads on and writes off; 0 positive rows with q < 0.05 out of 5,544; sheets off, the one "established" cell is 24.5 Hz at 60 s, 120-min window, pre-report (r 0.33, 0.17-0.48, n 59, q 0.23), and 21.5-25.5 Hz "supported" in 1-8 settings per window against 3-9 negative by chance; sheets on, 11-22 of 22 bands per setting fall with pain (1,545 negative rows with q < 0.05). Read as a lead for the next titration session, not a band to program. The five headline lines head the heat maps' "How to read this" drawer in bold, for RCS08 only. A first run with the store off had no clinic sheets (they are read from the store) and was discarded.
230. The exploratory ladder for L C+1-2- on the Stim Optimizer's titration card (the PI, 2026-09-21: a ladder "very helpful for deciding how the biomarker moves and its acute effect on pain"): when the readiness screen's best sensing pair on a side needs other stimulating contacts than the ones in force (L 0-3+ needs C+1-2-, the flanking rule inverted), the plan carries `proposed[side]`: the configuration, its rate from that cell (125 Hz; in force 55, whose half-rate harmonic sits on the watched 24.5-27.5 Hz bands), the pain-positive bands to watch and their harmonic clearance, the record for that configuration (L C+1-2-: 14 epochs 2025-07-18 to 2025-11-12 at 0.0 mA only; one day on part of a ring at 1 mA, counted apart), a first-exposure stop rule (side-effect score 2, decision 165), part A the 15-step up/down ladder for the biomarker's slope with the other side held, part B three 5-minute holds off / on / off at the top current tolerated with a rating every minute and the patient blind to the current; 33 clinic-sheet rows appended after the joint corners, in the Google-sheet export. Live RCS08: 8,821 fields in common, 8 differing (bookkeeping), 1,630 added, 2 removed (bookkeeping); host 1424 / 2 / 0, container 673 / 0, jest 31 across the seven Stim Optimizer suites. The card's new section waits on a live watch (the pane logged out at the session restart).
231. The reliable-change index deleted (the PI, 2026-09-22: too much variance; delete the decision that kept it): the module (`ClosedLoopDeployment/reliable_change.py`), its test, the adapter's block on the Closed-Loop response (`reliable_change`), the page's card (`ReliableChangePanel.js`, "How big a change in this patient's own rating means anything?") and the jest pins; decisions 75, 104, 111, 112 superseded. On RCS08 the index rested on 12 short-gap pairs of Left Leg VAS (pooled SD 11.1 on a 0-100 scale, a threshold of 30.8) and gated nothing. Also: a Closed-Loop smoke test still imported the panel deleted in 227 and had failed to run since; repaired. Panel A's proposal to print the index beside grid cells is dropped by this ruling.
232. Research batch and panels (the PI, 2026-09-22, overnight): four reports (the Biomarkers pipeline against the literature; alternative detection and symptom-tracking methods; the Stim Optimizer's Bayesian algorithm end to end; the Closed-Loop page's integration) each debated by a three-reviewer panel on a shared board to consensus (A round 4, B 3, C 4, D 3), every adopted action with an elicited probability and range and, where the record allowed, a live measurement with its interval; `artifacts/research_2026-09-22_*` and the synthesis. The finding that runs through all four: on the left lead the bands that rise with pain rest on the current in force (every "supported" 21.5-26.5 Hz cell on L 1-3+ and the ladder's 24.5-27.5 Hz watch bands on L 0-3+ lose their wholly-positive interval once the left current is partialled out; the grid and E2 carry no current term). Also measured: the Stim Optimizer's surrogate fails its own pre-registered calibration check on the live strata (55 Hz quarter-out coverage 0.37) and nothing runs it; the clinic-stream fit hard-codes the left leg; a two-state model of the ratings is not identifiable; the right-lead closed-loop candidate reads the right lead's own record. No code changed by the panels; eight open questions for the PI are collected in the synthesis §4.
233. The eight questions answered (the PI, 2026-09-22, on the panels' synthesis §4). (1) The ladder's pain reading is the current-stepped ramp WITH the current term, and he wants the fixed-current holds' reading beside it to decide finally: both are built and reported, the ramp's is the one to read. (2) "Supported" does NOT require a band to stay positive once the current in force is partialled out; the adjusted value is reported descriptively beside the plain one and never re-selects a band or moves a verdict (decision 210 stands). (3) The exploratory ladder runs at 55 Hz, the rate in force, not the 125 Hz of the cell where the bands were found: decision 230's rate rationale is overridden. Consequence, stated and kept as a warning, never a refusal (decision 220): at 55 Hz the half-rate harmonic is 27.5 Hz and a band is 5 Hz wide, so the harmonic falls INSIDE 25.5, 26.5 and 27.5 Hz and only 24.5 Hz stays clear (corrected on measurement in 236; this line first said three of them were clear, comparing centres to the harmonic and forgetting the band's own width). (4) The back site gets its parallel fit wired once the clinic-stream site defect is fixed (C-9 then C-1 becomes wire, not retire). (5) The next titration session runs 55 Hz at the pulse-width pairing in force (100/150 us), and its data are merged with the 60/160 us record for the analysis (pooling for that stratum, decision 222's toggle). (6) The surrogate's calibration check is reported as a WARNING, never blocking. (7) The current-adjusted grid sits behind a switch, the plain grid the default. (8) The chosen closed-loop band is recorded on the server and updated whenever a new band is chosen.
234. The heat map's correlation with the stimulation current taken out of it, behind a switch (the PI, 2026-09-22, rulings 2 and 7; panel A's item 1, the first step of the cross-cutting build order). `AdjustForStimCurrent` off by default: with it on, every cell of a pair's grid also carries the correlation computed with the current in force on that pair's own side taken out of both the band power and the pain score (`adjusted_correlation_grid`, `pearson_r_adjusted` and the change on each selected row, `covariate_adjustment` saying whether it could be done and why not). The plain value still selects the band, carries the interval and sets the verdict -- descriptive, never a gate. The current per pain report is read from the dated settings the device's own files carry, the raw store kind `therapy_settings`, as consumer "biomarkers" (`routines/stim_current.py`); Biomarkers does not import StimOptimizer and no second parser exists. The switch is in the store KEY, not in the cross-page settings tag (decision 131 untouched), so a grid built with the switch off is the same product it always was and is still served, while a switch-on request cannot be answered from one. Live RCS08 (NRS, the daily defaults): 33,919 fields in common, 0 correlation values moved (1,904 differing = 293 bookkeeping, 1,851 the stability column, which is a background product keyed on the grid's own key and so unbuilt for the new key, 24 the covariate block, 1 a wall-clock sentence), 2,952 added, 1 removed (a timestamp). What it says: on L 1-3+ the 22.5-27.5 Hz bands read -0.13 to -0.18 against NRS plainly and -0.23 to -0.28 with the current out; on L 0-3+, the ladder's own pair, +0.08 to +0.20 plainly and +0.01 to +0.12 with it out, the positive readings shrinking by 0.06 to 0.11. Six tests first, watched RED. Host 1406 / 2 / 0, container 679 / 0 (was 673).
235. The three interim caveats, on the pages read today (step 2 of the panels' cross-cutting build order; panel D item 9, panel B item 8, panel A item 5). (a) Closed-Loop evidence triangle: one sentence under the band-power-to-pain edge naming the current in force as an unadjusted confound, for a LEFT candidate whose centre is 21.5-27.5 Hz only (`CURRENT_CONFOUND_NOTE`, `currentConfoundApplies`), and the module's own "PROVISIONAL: ... rests on the point signs alone" half of the coherence note pulled out of its fold into the open, the method half left in it. (b) Stim Optimizer current-map card: the pooled "higher current, lower pain" association is stated as not holding out of sample (area under the curve 0.41 left alone, 0.49 both sides, against 0.57 for shuffled data, 611 ratings, folds separated in time), in the open, never behind the descriptions button. (c) The heat maps' RCS08 lines rewritten from five to six: q defined in plain words, "established" and the 22-band correction told apart in adjoining sentences (the contradiction the clinician found), the direction names and "reuse" explained, the pair named in the line that states the negative result, and a new line carrying decision 234's measurement (L 0-3+ 22.5-27.5 Hz +0.08 to +0.20 plainly, +0.01 to +0.12 adjusted; L 1-3+ -0.13 to -0.18 becomes -0.23 to -0.28). Tests first, watched RED: 5 new jest on the triangle (present for a left in-family band, absent for the right lead and for an out-of-family centre, the provisional sentence outside a CLOSED fold rather than merely mounted, the method half still inside), 2 on the current-map card, the drawer's referent test extended. Jest across the three pages 153 / 155, the two failures the known pair (decision 147). Chunks 732.075fce90, 100.2afc23f7, 63.8c13dd7e carry the three sentences; workers reloaded. Not yet watched live: the browser pane needs the PI's login.
236. The exploratory ladder runs at the rate in force, 55 Hz (the PI's ruling 3 of decision 233, built): `configuration_plan` holds `rate_to_hold(rate_in_force_hz)` instead of the cell's rate, reports the cell's own rate beside it (`cell_rate_hz`), and says both in one sentence. The harmonic consequence is named rather than merely flagged: at 55 Hz the half-rate harmonic (27.5 Hz) falls inside the 5 Hz width of 25.5, 26.5 and 27.5 Hz, leaving 24.5 Hz -- the near-hit cell of decision 229 -- the one clean band to watch; `watch_on_harmonic_hz` and `watch_clear_hz` carry the two lists and the card's sentence names them. Live: the ladder is 55 Hz at the pairing in force (100/150 us, the PI's ruling 5), 33 sheet rows, 15 steps and three holds unchanged. Host 1406 / 2 / 0, container 679 / 0; 50 / 50 in the plan's own tests. Decision 230's rate rationale is superseded.
237. The two readings of one titration session, and which is the reading (the PI's ruling 1 of decision 233; step 3 of the cross-cutting build order; panel B item 1 with panel A item 8). `StimOptimizer/routines/titration_readings.py`: `ramp_reading` gives, per watched band across the ladder's settled steps, the plain correlation between band power and pain and the one with the step's own current taken out, each with a resampled interval, plus the band's correlation with the current itself; `holds_reading` gives, for pain and for each band, the ON block against the two OFF blocks with an interval; `session_reading` carries both side by side with the ruling named on it -- the ramp with the current term is the reading, the holds are beside it for comparison. Two refusals are stated rather than computed around: a band that moves almost exactly with the current (98% of its movement or more) gets no adjusted number, because taking the current out of it leaves only noise, and that is itself the finding; a block with no ratings, a constant current or too few steps is a reason, not a crash. **What a ladder cannot settle, measured while building it**: on 15 settled steps a band with NO pain relationship still reads beyond 0.4 once the current is out about one draw in eight, and clears zero about one in fourteen (120 constructed ladders), so every reading carries the sentence that one visit is a lead to repeat, never an established result (METHODS §7). The mirror case is true too and pinned by the tests: a real relationship can read weak plainly and strong adjusted, because the current's own variance dilutes it -- the direction the live left-lead record already shows. Eleven tests, RED first; host 1417 / 2 / 0, container 679 / 0. The card's wiring waits on the visit's own data.
238. Step 4 of the cross-cutting build order, the PI's rulings 4 and 6 (panel C items 9, 1 and 2). (a) The clinic-stream fit follows the site it is asked for: `clinic_pain.fit_clinic_rate_strata(..., primary_item=)` reaches `run_stage1` instead of the hard-coded "left_leg", and `epoch_frame_from_steps`'s `primary_item` parameter -- read by nothing, which is what hid the bug -- is gone, since the frame carries every site and the site is chosen at the fit. (b) The back site is WIRED, his ruling 4: every site after the first gets its own open-loop fit (`two_stage.parallel_sites[site]`) with its own per-rate rows and its own clinic-stream fit, and says in `scope` that the gate, Stage 2 and the closed-loop question stay on the primary site. (c) The pre-registered calibration check (`OBJECTIVE_SPEC` §6) runs where each surface is fitted (`stage1_openloop.stratum_calibration`, behind `calibration_check=True`) and is reported per stratum as a WARNING, his ruling 6: `blocking` is False, the consequence is in words, and a fold that cannot be trained on says "not computable" rather than passing or failing. Live RCS08: the back site fits and differs from the left leg (110 Hz not flat against flat; the clinic 55 Hz gain fails where the left leg's passes; 12 clinic epochs against 15), built in 28 s; the calibration answers per stratum -- the clinic 55 Hz surface passes the epoch-out and coverage criteria and FAILS the block-of-time one, which is the non-stationarity the panel diagnosed. The first live run showed the check computed and reaching no field, because the rows are built from the summary frame and not from the stratum object; it is attached in the serialiser now. 16 tests first, RED; host 1423 / 2 / 0, container 679 / 0.
239. What the next session must deliver, named (step 5 of the cross-cutting build order; panel C item 10 with the PI's ruling 5). `stage1_openloop.current_coverage` returns the per-pair table it already built and discarded -- each (left, right) current pair with its ratings, its days, whether it qualifies and what it is short OF (ratings, days, or both, which a visit fixes differently) -- and `coverage_gap` turns the refusal into an instruction: how many pairs are missing, which to top up, which to add, what each needs, and the cheapest way to close it, never above the safety ceiling. **Two things the record taught that the constructed tests had not.** (a) A ladder that steps one side and holds the other can NEVER pass this rule: it needs at least 1 mA of range on both sides, so the joint corners are not an extra, they are the only way the span on the held side is ever earned; the gap now names corner pairs when the other side has not moved. (b) On the real record almost every pair at the held current has already been delivered ONCE on ONE day, so the cheapest sixth pair is a repeat, not a new setting: the gap proposes top-ups first ("repeat L3/R3, 1 more rating") and says so. **Live, at 55 Hz on the clinic stream**: at 60/160 us alone, 2 of 6 qualifying pairs, 4 missing, as panel C measured; **merged with the pairing in force (100/150), the PI's ruling 5 exactly: 5 of 6, one pair short, and one more rating at L3/R3 closes it** [corrected in 255: 3 of 6 on the Left Leg ratings the fit uses]; merged across all fourteen pairings at that rate (wider than the ruling), 10 of 6 and coverage passes. Six tests, RED first; host 1429 / 2 / 0, container 679 / 0.
240. The two guards every offline model here has to pass through, and what the first live run of them says (step 6 of the cross-cutting build order; panel B items 2 and 7). (a) Held-out blocks of TIME with the neighbouring rows dropped from training, the gap taken from the label's own decorrelation timescale rather than typed in (`stats_utils.purged_time_blocked_folds`): on constructed data a model that does nothing but copy its nearest training row scores 0.83 with the neighbours present and 0.19 with them dropped, and the gap matters most where the held-out blocks are short, which is the regime a record this size forces. (b) A current-confound gate (`stats_utils.confound_gate`): every candidate reported plainly AND with the stimulation current taken out, an interval on each, and "cannot be judged" rather than a number where the current never moves or the candidate is almost the current itself. (c) The check the panel made the condition on building any decoder (`Biomarkers/routines/confound_diagnostic.py`): the pain label read out of sample from the current alone, from every band, and from every band with the current taken out of each one inside the training rows, against a rotation null that keeps pain's own persistence. Out-of-sample scores are never folded -- a model that reads 0.30 has failed, and printing 0.70 for it would invert the finding. Nothing refuses anything (the PI, 2026-09-22: a warning, never blocking), and none of it is on any page today. Live on RCS08 at 1, 10, 30 and 60 s of signal on the two sensing pairs with enough reports: NO set of bands beats its own null out of sample anywhere -- the best is L 1-3+ at 30 s, 0.701 against a null 95th of 0.713 (p 0.075, 53 reports, uncorrected for the eight combinations tried) -- and taking the current out never lifts a reading above its null. Every band on both sides together cannot be read at all: 46 reports carry all 44 bands, so holding a block of time out leaves fewer training rows than bands, and the refusal is printed instead of a number. The one strong number is the stimulation current alone on L 1-3+, 0.879, and it is an era as much as a dose: 19 of those 53 reports sit at zero current, all between 2025-07-23 and 2025-10-14, mean pain 8.1 against about 6.1 afterwards; on R 0-3+, where the subset is large enough to test, dropping the zero-current reports takes the current's own out-of-sample skill from 0.411 to 0.242. Twelve tests, watched RED first; container 679 -> 691 / 0, host 1429 / 2 / 0.
241. The stimulation current is given a SHAPE when it is taken out of something, not only a straight line (the PI, 2026-09-22: "I think sometimes we see an upside-down U-shaped curve in the current-pain relationship, so linear might actually mask a useful measure"; then: model it with two or three other common kernel functions). `stats_utils.CovariateShape` offers seven: a straight line, a squared term, a 3-knot natural cubic spline, three kernels (squared exponential, Matern 3/2, rational quadratic, each fitted by kernel ridge with the length scale read off the delivered settings) and one level per delivered setting. Every answer names the shape it used and how much flexibility that cost (effective degrees of freedom: 1 for a line, 2 for the spline, 4-9 for the kernels), falls back and says why where a shape cannot be built, and refuses an unknown shape by name. The removal is fitted on the training rows and applied to the held-out ones, kernels included. **The two new guards default to the spline; `partial_corr` and `partial_corr_columns` keep the straight line**, so decision 234's adjusted grid and every other published number are untouched, byte for byte, until someone asks otherwise. WHY THE SPLINE AND NOT A KERNEL, measured on the live record (L 1-3+, 53 reports, 11 delivered currents): a straight line explains 25.7% of pain's scatter and is still falling at the highest current tested; the squared term and the spline explain 30.6% and 29.4% and put the lowest pain at 3.11 and 3.21 mA; the three kernels explain 36.9-42.8% and move it to 1.81-1.93 mA, chasing two ratings at 1.6 mA; one level per setting explains 62.0% with 10 degrees of freedom on 53 reports. The flexible shapes do not agree with each other about where the turn is, so the default is the cheapest shape that can turn over at all. The turn-over itself is suggestive and not established: the squared term is +0.214 with a 95% interval of -0.079 to +0.486. On R 0-3+ (96 reports) there is no turn-over under any shape -- every one puts the lowest pain at the highest current tested, and the line, the squared term and the spline are identical to three decimals. WHAT THE SHAPE CHANGES, live: nothing that matters. Under all seven shapes, on both pairs, no set of bands beats its own null out of sample and the adjusted reading stays inside it (L 1-3+ 0.478 after a line, 0.526 after the spline, 0.501 after one level per setting, against a null 95th of 0.713; R 0-3+ 0.579, 0.524, 0.431 against 0.622). Decision 240's live numbers stand as measured with a line and are now reported beside the spline's. The shape DOES change how the current itself reads: on a constructed record where the current turns over, a straight line reads it at 0.622, close to chance -- the answer that would have said the current is not the story -- and the spline reads the same current at 0.977; and a curved confound left in the bands reads 0.959 after a straight-line removal, 0.570 after the spline and 0.495 after one level per setting. One thing found while building and pinned as its own test: how much a removal leaves depends on how large the confound is, not only on the shape -- a spline fits 99.0% of an exact parabola, and where that parabola is ten times the noise the 1.0% left still correlates the two series at +0.82. Eleven tests, watched RED first; container 691 -> 702 / 0, host 1429 / 2 / 0.
242. The Closed-Loop page says what its own answer rests on (step 7 of the cross-cutting build order; panel D items 1, 2, 3, 5 and 4). (a) ONE VERDICT ON THE SIGN-OFF SHEET: the older statistical-gate endpoint's "Summary verdict: ..." sentence is gone from the sheet, its gate counts and gate rows kept as the checklist they are; the header's own copy stays where it is labelled as coming from a separate summary and as evidence, not permission. (b) "What would change this answer" reads the band-stability answer and ranks one item for it between the device rules and the evidence findings, quoting the stability card's own four words and naming who can resolve it ("measurement -- more stimulation states", or "band selection -- not more measurement" when the band behaves differently); nothing is added when the band was shown to behave the same. (c) THE CAVEATS LIST (`adapter.caveats_for_report`): one flat list of severity, sentence and the card each is about, assembled per request and stored nowhere -- the report's own warnings word for word, the point-sign caveat naming which edges, the band-power-to-pain reading's missing current term, the two switching values and the design rule's smallest allowed gap, the timing values, the simulation fractions (only M3 resamples runs) and the stability answer. It is printed on the sign-off sheet and counted in the header; it gates nothing. (d) The stability answer is printed INSIDE the coherence note, so a reader of the note is not left taking a coherent sign pattern for a settled finding about a band whose meaning has never been shown to hold still; the wording has one home (`consistency.note_with_stability`) and is appended by the adapter, where the answer exists, rather than through a parameter `pipeline.run` could not fill (decision 238's lesson), and appending twice changes nothing. (e) E2 READ AGAIN WITH THE CURRENT IN FORCE TAKEN OUT of the band power, the pain scores untouched: one optional argument on the shared estimator (`band_pain_auc_from_table(covariate_column=)`, default off), the partial correlation beside it, both intervals resampling whole pain reports, carried on `EdgeEstimate.adjusted` and printed beside the plain reading on the triangle in place of decision 235's interim sentence. Three refusals are stated rather than computed around: a band whose power is 98% the current gets no adjusted number (one home for that rule now, `stats_utils.NEARLY_THE_COVARIATE_R2`, which the titration ladder reads too), a current that never moves has nothing to take out, and the exported-table route says it cannot make the adjustment because it holds no per-sample current. Descriptive throughout: the plain reading still resolves the edge and sets the verdict (the PI, 2026-09-22, decision 233 answer 2). Measured on constructed records: a current driving both the band and the pain reads 1.000 plainly and 0.477 adjusted; a band with its own pain relationship reads 0.917 plainly and 0.909 adjusted (partial correlation +0.657); on a table of the live record's shape (32,069 samples, 32 pain reports) the estimator takes 3.07 s and 5.53 s with the second reading (medians of alternating rounds), and asking for the adjustment moves none of the plain answer's 23 fields. 21 new host tests and 12 new page tests, all watched RED first; host 1409 / 5 / 0; jest 61 of 63 on this page, the two failures the known pair (decision 147); chunk 732.487c2ca2 carries the new sentences. PROVED LIVE on RCS08 afterwards, on the PI's own machine (2026-09-22): the page's own request through its own endpoint, store writes off, the saved evidence inputs read on both sides (step 7 changes no stored key). L 1-3+ 24.5 Hz: 60,189 fields in common, 5 differing, 47 added, 0 removed; R 0-3+ 24.5 Hz: 60,203 in common, 5 differing, 44 added, 0 removed. The five differing fields are three timing fields and two notes that only GAIN text at their end, not a character of the old text moved: the coherence note gains the stability sentence (left "cannot tell"; right "behaves differently", the interaction test rejects), and the E2 note gains one sentence restating the adjusted reading -- the handoff listed the first and not the second. Added: the caveats list (8 entries left, 7 right) and the adjusted reading (21 fields; E1 and E3 carry an empty `adjusted`). The same code run twice differs in the three timing fields only. The adjusted reading, live: left 0.469 (0.348 to 0.596, 30 pain reports, partial correlation -0.114, -0.239 to +0.043) against 0.559 (0.420 to 0.697) plainly; right 0.386 (0.270 to 0.517, 48 reports) against 0.429 (0.307 to 0.574). On the left the adjusted reading runs on 18,992 of the plain reading's 32,069 samples, because the left current is missing on 13 days (2025-08-05 to 2025-08-21); the plain reading restricted to those same samples is 0.552 (0.406 to 0.688), so the change of sample moves it 0.007 and taking the current out moves it 0.083. On the right both readings use the same 37,517 samples. Cost, whole report, alternating rounds before / after / before / after: left 99.0 / 112.0 / 105.3 / 114.0 s (+10.9 s), right 118.3 / 123.0 / 111.3 / 125.7 s (+9.6 s) -- about four times the 2.5 s estimated on the constructed table, and the before-rounds alone spread 4 to 7 s, so about 10 s give or take 5. Container 702 / 0 (the first run of that suite on this code), host 1450 / 2 / 0; jest 61 of 63 on this page, the known pair; every sentence the new page code displays found in the served chunk 732.487c2ca2; workers reloaded. [CORRECTED 2026-09-26, decision 290: every E2 number in this row was computed on the ratings of the settings period BEFORE each piece's own. Re-measured on the full tiles with the join fixed, under NRS, the score this row read: left 0.564 (0.438 to 0.678, 43 pain reports, 39,953 samples) plainly and 0.553 (0.441 to 0.673, 42 reports, 26,416 samples) with the current out, partial correlation -0.133 (-0.231 to +0.059); right 0.401 (0.288 to 0.509, 59 reports) and 0.431 (0.322 to 0.549). The old join run today on the same full tiles gives left 0.562 / 0.470 and right 0.430 / 0.388, within 0.003 of this row's 0.559 / 0.469 and 0.429 / 0.386, so the change is the join, not the weeks since. The left adjusted reading's sample count is now 26,416 of 39,953 (was 18,992 of 32,069). The field counts, the timing rounds and the equality of the plain answer's 23 fields stand.]
243. The Stim Optimizer page reads in the order a decision is made and says what its numbers rest on (step 8 of the cross-cutting build order; panel C items 5, 6, 4, 7 and 8). (a) ORDER: the readiness table first, then the decision strip, the current map, the next clinic session and the home schedule together, then the plan card with its four checks; the evidence base is a one-line footer, since no decision on the page reads it. The two readiness cards stay TWO cards, each pointing at the other, per the clinician's correction of the report's merge. (b) WORDING: the decision card's title is computed from the per-side verdicts ("No side has a setting proven better than today's"; a side whose comparison could not be formed is not called unresolved); "resolved" is defined once, at the top of the strip; the recording-site check reads "Does a usable recording site's power change with current? (a band inside 8–30 Hz)"; the readiness table OPENS with the device's sensing rule and the count it explains (`closed_loop.sensing_rule`, decision 217), which was printed only row by row; its fold no longer says the pain leg needs an "established" correlation (decision 210 made it "supported"). The current-map legend stays folded: the report asked to open it, but the PI's ruling of 2026-09-17 (S5) folded every description on that card, and it is his to reverse. (c) STILL POSITIVE WITH THE CURRENT TAKEN OUT (item 6, decision 233 answer 2): beside each band that rises with pain, yes / no / not assessed with both values, the adjusted POINT value only (the grid carries no interval on it), moving no verdict. Read from a STORED current-adjusted grid (`adapter.stored_current_adjusted_grid`, read-only; the grid's sidecar now records decision 234's switch, which its cross-page tag does not), never built on the request path; "not assessed" says what would produce one. The response key carries the adjusted grid's key only when one exists. (d) EXPOSURE (item 4): how many "proven better" comparisons ran and what 1 standard deviation exposes across them (0.159 per comparison where nothing is truly better, an upper bound on at least one lucky pass), on `stage1.audit.resolution_exposure` and printed under the strip; visibility only, nothing corrected. (e) THE RATE PIN (item 7): `OBJECTIVE_SPEC.md` states the one-octave pin (0.823) as an assumption with the bimodal evidence (modes 0.219 and 2.562, 2.789 nats apart; the pin in the valley) and that it acts only on the joint rate-by-currents surface, never on a current the page could recommend; `stage1.audit.frequency_length_scale` carries it. (f) THE LABELLING PASS (item 8): the README names what the page reaches and what only research reaches; the stopping rule is "computed, not read" (it runs on the live path; panel C's "unreached" was wrong); two stale statements corrected (an age term "in place" that decisions 193-196 removed; "no endpoint and no page yet"). 22 new host tests and 16 new page tests, all watched RED except the one pinning the rate field; host 1431 / 5 / 0; jest 49 of 49 across the page's 10 suites; chunk 100.ce250192 carries every new page sentence. PROVED LIVE on RCS08 afterwards, on the PI's machine (2026-09-22), the page's own two requests through its own endpoint, store writes off, the saved response bypassed on both sides, the background launchers switched off. Main request: 10,788 fields in common, 1 differing, 82 added, 0 removed. Two-stage request (`TwoStage`, the page's second request): 39,694 in common, 2 differing, 102 added, 0 removed. Added, exactly the blocks named: `closed_loop.sensing_rule` (19 fields; it reads "one sensing pair per lead: L 1-3+ and R 0-3+. Neither has a usable band, so 0 of 50 contact-and-rate combinations are usable"), `closed_loop.pain_relationship.still_positive_without_current` (63 fields; "not assessed" for every band, since no current-adjusted grid is stored under the daily settings, and saying what would produce one), and on the two-stage request `two_stage.stage1.audit` (10: the "proven better" comparison ran 2 times, 0.159 chance of a lucky pass each, 0.292 of at least one; the rate length scale pinned at 0.823) plus the same two blocks inside the back site's own fit, `two_stage.parallel_sites.back.stage1.audit` (10), which this row did not name. Differing: `two_stage.seconds` (timing) and `store.response_key`, and the line above about that key was wrong: it changes on ANY change to the module's code, because the module's own code digest is part of it (decision 41), not only once an adjusted grid is stored; no value moved. The same code run twice: 0 fields differing. Build time, alternating rounds: 9.3 / 9.1 / 10.3 / 9.4 s main, 30.1 / 30.6 s two-stage -- no cost. Container 709 / 0 (the first run of that suite on this code), host 1475 / 2 / 0; jest 52 of 52 across the page's 10 suites (243 and 245); every sentence the new page code displays found in the served chunk 100.a13447a0; workers reloaded.
244. The Closed-Loop page's jump links listed two of its sections in the wrong order (item 15 remainder, closed; first written as decision 231 on the branch ps_closedloop_deployment-zm4nwg, a number already taken, and renumbered on 2026-09-23 when rebased onto decision 243). The links at the top right are a table of contents; they offered "CL-DBS simulations" above "Sign-off", but the page file builds the sign-off card first and the simulations card below it, so a reader following the list downward was sent back up the page. The sign-off label also carried a down-arrow, which reads as "this is the bottom" and was not true. Both fixed. The list is a second copy of an order the page already carries, which is why it drifted, so a test now reads the page file itself and compares the two orders; it fails whether someone reorders the cards or the links (three jest tests, the order one watched RED then GREEN). Checked at the same time and found already correct: the print stylesheet hides the whole page and opts three blocks back in by name, so every card added since it was written is kept off the printed record by construction, and all six links point at sections the page really marks. Host 1423 passed / 0 failed / 3 skipped; jest 48 across the eight Closed-Loop suites, the same 2 failing before and after (the two known-failing page tests CI already excludes). Chunk 603.e66d20d2, order read back out of the built file. NOT watched on screen: this session had no browser. On the PI's machine the Closed-Loop page's jest suites read 64 of 66, the two failures the known pair. Left for the PI: whether the sign-off card should be the last card on the page.
245. Two amendments to decision 243 (the PI, 2026-09-23). (a) The current map's LEGEND is open on load -- what the colours, the black cross, the dots and the star mean -- amending his S5 ruling of 2026-09-17 for that one paragraph; every other description on the card still folds behind the button. (b) The search's own STOPPING RULE is shown: the decision strip prints, per side, "When to stop searching" with stop, keep searching (with the count of untried combinations still worth trying) or not assessable, and why. On RCS08 every surface reads not assessable -- the rule's "has the best stopped improving" half needs a history of completed batches, and this platform proposes batches without running them in turn -- with 3,184 untried combinations still worth trying on the chosen pulse-width pairings. The README row says so; its false statements (the age term "in place", "no endpoint and no page yet", a test count) were corrected in 243. Four jest tests watched RED first (one changed, three new); jest 52 of 52 across the Stim Optimizer page's 10 suites; chunk 100.a13447a0. No backend change: the fields were already on the response.
246. Housekeeping, step 9 of the cross-cutting build order (panel A items 4, 3, 9, 2; panel B item 6; panel D item 10) -- the last of the panels' nine steps. (a) D-10: three field groups nobody read leave the Closed-Loop response -- `protocol` (a titration plan built from the two capture currents; the page's titration plan is the Stim Optimizer's card), `edges_historical` (the setting-epoch E1 the pooled slope replaced, decision 126; kept on the report object) and three duty-cycle fields (`predicted_failure_mode`, `qualified_transitions`, `unqualified_excursions`); a whole-tree search found no reader outside the module, its tests and fixtures; the two completeness guards in `test_core.py` name them as withheld with the reason. (b) A-4: each circled heat-map cell carries `n_pain_reports_effective`, the lag-1 effective count of independent ratings (`stats_utils.effective_n` on the pairs the cell correlated), printed inside the ratings phrase of the hover and the panel line ("117 ratings (about 98 independent), q = 0.0022"), the PI's hover format otherwise kept; grid rule v22 so stored grids rebuild. (c) A-3: each grid carries `null_family_reconciliation` -- the shuffle null's own arithmetic run once on the UNshuffled scores and compared with the grid cell for cell, the older search's F8 fields (`perm_family_reconciled`, max deviation, cells selectable outside the null, and the reverse), for the correlation and the AUC grid; a measurement, never a gate; the null itself unchanged (three constructed grids, 4,281 values before and after, 0 differing). (d) A-2: the whole 252-setting search's answer as a count, one line in the heat maps' drawer for RCS08 -- a positive p < 0.05 cell in 6 of 252 settings against about 106 if the settings were independent tests of 22 bands (they are not; the true expectation is lower, but not near 6); 4.1% of cells under p 0.05 with sheets off against 5% by chance, 61% with them on, all negative; "a count, not a calibrated p-value for the search as a whole"; the full shuffle of the search stays deferred. (e) B-6: report B's "under 2% native coverage" now names both numbers and their denominators -- under 2% of the continuous 3-second windows, 79% of the matched pain-report rows on R 0-3+ (decision 106) -- and cites the device document's §8, not §6 or §4. (f) A-9: `BRAVO/_agent_bridge/step9_time_of_day_diagnostic.py`, a one-off check of whether band power follows the California clock (a once-a-day cycle, R with a block-bootstrap interval against a shuffled-hours reference) or the weekend (r with its interval), per contact and band, on the stored raw-power matrix; writes nothing to the store and feeds nothing (193-196 hold); its self-test finds a planted 15:00 cycle at 20.5 Hz and nothing where none was planted. NOT RUN on the record -- this session had no container. 13 new tests and 4 jest cases, all watched RED first; host 1434 / 5 / 0; Closed-Loop tests 698 / 2 skipped; the Biomarkers tests runnable here: 422 passed, the same 28 failures and 22 collection errors before and after (Django absent here; one exact-product check fails on this machine's numpy before and after); jest 74 of 74 on the Biomarkers page; chunk 63.257cb08b. PROVED LIVE on RCS08 afterwards, on the PI's machine (2026-09-22), each response through its own endpoint, store writes off, the saved answer bypassed, the background launchers switched off. CLOSED-LOOP, both candidates: L 1-3+ 24.5 Hz 58,937 fields in common, 3 differing (timing), 0 added, 1,299 removed; R 0-3+ 58,948 / 3 / 0 / 1,299 -- and every removed field is one of the three groups named (1,273 in `protocol`, 17 in `edges_historical`, the three duty-cycle fields in their 3 places each). HEAT-MAP GRID (daily settings, 6 sensing pairs): 33,913 in common, 216 added, 0 removed, 1,869 differing, and NO correlation, area, q or interval value among them: 21 are timing and the grid's own key (rule v22), 1,848 are the stability column (7 fields on each of 264 rows). That column is filed under the grid's key, so the new key finds none yet and every row reads "not tested: the cross-setting stability answer has not been computed for this grid yet" where it read e.g. "cannot tell" -- on the live page it stays that way until the background stability job rebuilds it for the new key, which the page starts the first time it loads each grid. Added: 132 `n_pain_reports_effective` (22 correlation rows on each pair) and 84 fields of `null_family_reconciliation` (14 per pair). The same code run twice differs only in timing (3 and 19 fields). Build time, alternating rounds before / after / before / after: Closed-Loop left 111.3 / 128.6 / 118.6 / 117.8 s, right 131.1 / 133.9 / 127.5 / 129.3 s, grid 11.9 / 12.1 / 11.8 / 11.6 s -- no cost distinguishable from the spread between rounds. THE TIME-OF-DAY CHECK, run on RCS08 on the PI's machine (its self-test finds the cycle planted at 15:00 and nothing where none was planted): band power against the California clock and against weekends, per sensing pair, 22 bands each. The two pairs that can drive closed loop today show NO daily cycle above chance -- L 1-3+ (1,223 recordings) 0 of 22 bands, R 0-3+ (3,815) 0 of 22 -- but L 1-3+ runs slightly higher at weekends in 15 bands, 13.5 to 27.5 Hz (r +0.10 to +0.13, each interval wholly above zero), which covers the band family that rises with pain on the left; pain turned out to differ at weekends too, checked afterwards on the PI's request (2026-09-22): comparing weekend with weekday reports within the same week, over 58 weeks, pain runs higher at weekends on NRS (+0.30 of 10, +0.12 to +0.50, p 0.001), VAS (+3.9 of 100, p 0.003), McGill (+1.9, p 0.003), back VAS (+3.4, p 0.022) and the composite (p 0.016); only left leg VAS's interval covers zero (+2.9, -0.7 to +6.3). So the weekend is a shared cause of both, and it was measured directly: taking the weekend out of L 1-3+'s band-to-NRS correlation, 13.5 to 29.5 Hz at 10, 30, 45 and 60 s of signal, moves it by at most 0.019 (median -0.005 to +0.007) -- real in direction, negligible in size, partly because only 8 of the 53 matched ratings (6 of 41 at the longer lengths) fall on a weekend. Two other pairs carry a strong daily cycle: L 0-2+ (418) in all 22 bands, strength 0.34 to 0.49 against a shuffled-hours 95th of about 0.12, power peaking between 22:40 and 23:40; R 1-3+ (364) in 21 of 22, peaking around midnight. Neither pair is one the device allows with today's contacts (decision 217), and the table cannot say whether the cycle is sleep, when those recordings happened to be made, or both. L 0-3+ and R 0-2+ show no daily cycle and weekend differences in 7 and 9 bands; with 132 cells checked twice, a few such bands are expected by chance. A fact to weigh, not an input: time is still modelled nowhere (decisions 193-196). The full table is in `_agent_bridge/_step9_time_of_day.csv` (scratch). Container 709 / 0 (the first run of that suite on this code), host 1475 / 2 / 0; jest 74 of 74 on the Biomarkers page, 64 of 66 on the Closed-Loop page (the known pair); every sentence the new page code displays found in the served chunks 63.257cb08b, 100.a13447a0 and 732.1a02d12d; workers reloaded.
247. Three things found by watching the pages live, fixed (the PI, 2026-09-22: "fix all three"). (a) THE CLOSED-LOOP PAGE NOW APPLIES THE DEVICE'S SENSING-PAIR RULE, as device rule D52 (A610 p. 36; WP p. 8; BrainSense Tip Cards pp. 7-8, the documents decision 217 cites): the sensing pair must be the two contacts immediately flanking the lead's stimulating contacts. It had said "The device permits this configuration" for L 0-2+ at 24.5 Hz, the band chosen on the page, while the left lead stimulates on contact 2 (settings in force since 2026-09-03), where the device allows L 1-3+ only; the Stim Optimizer's readiness card already said so. The contacts are read off the settings row in force for the sensing lead, the row that already supplies the rate and pulse width (`programmed_settings_from_epochs`, parsed by the Stim Optimizer's own `stim_rings`), and the rule's arithmetic is the Stim Optimizer's `lfp_evidence` helpers -- one home. Blocking; not determinable, never a pass, without contacts; a failure names the pair the device allows and the contacts the chosen pair would need; a pass records what it was checked against, like D30. THE LIVE RUN CAUGHT WHAT THE TESTS COULD NOT: the first build read D52 "not supplied" on every candidate and turned L 1-3+ from supported to blocked, because the pipeline routes a device fact to the rules only when `PARTICIPANT_KEYS` lists it and the tests had built the facts by hand; listed, and a test now goes through the pipeline's own routing. Live RCS08, before and after, the page's own request, store writes off: L 0-2+ 24.5 Hz goes from "supported (point signs only)" to "blocked" -- "NOT eligible: 1 blocking ... of 52 rules", observed "sensing on 0-2; this lead stimulates on contact 2 (programmed cathode 2a-2b-2c), where the device allows sensing on 1-3 only; sensing on 0-2 needs stimulation on contact 1" -- and the point-sign caveat leaves the list with the provisional verdict; 59,130 fields in common, 33 differing (18 timing), 12 added, 4 removed. L 1-3+ and R 0-3+ keep their verdicts (supported; blocked for its two existing reasons), D52 passing on each (contact 2 allows 1-3; contacts 1 and 2 allow 0-3): 59,138 and 59,149 in common, 24 differing each (21 timing, the rule count, the summary, the caveat text), 12 and 13 added, 0 removed. On the page: "This configuration cannot be programmed: the device refuses it", with D52 first in "What would change this answer". The rule table's own tests moved with it on purpose (52 rules, 21 blocking; the fully declared test participant now declares its contacts), including the test named for fifty-one rules, renamed for fifty-two because the table really grew. (b) The sign-off sheet's switching-values caveat printed raw floats ("172.2737406083742"); it now prints four decimals, as the parameter card prints the same two values (`fmtPower`), and a test forbids any caveat number with more. (c) The Biomarkers heat maps were drawn at a fixed 750 pixels inside 527-pixel boxes: the extra 223 spilled right, unseen until a cell was picked, when the side panel covered them -- the columns from about 25 Hz up, the 24.5-27.5 Hz family among them -- and cut the hover label mid-word. The layout no longer fixes a width, so Plotly draws at the box's width, and 750 stays as the box's cap. Live: 527 of 527 on both maps with the panel open, the axis to 30 Hz, the far-right cell's hover label wholly inside the plot; chunk 63.7cbd7c76. Tests RED first: 15 host, 2 jest; container 709 / 0, host 1490 / 2 / 0; jest 76 / 76 on the Biomarkers page, 64 / 66 on the Closed-Loop page (the known pair); workers reloaded.
248. The heat maps' stability column was empty, and the Closed-Loop card's was the wrong grid's (2026-09-23, the first item of the PI's outstanding list). (a) The answer for each grid is filed under that grid's key, and the store kept ONE entry of this kind per participant, so every background run deleted the one before it: the log said "stored 132 of 132 points" dozens of times that day, one entry was on disk, and every other grid -- the Biomarkers page's own (Left Leg VAS, clinic sheets on) and the daily NRS default among them -- read "not tested" on all 132 rows. The decision-107 failure a fourth time; `biomarker_band_stability_grid` now keeps 12, like the grid kinds (about 0.13 MB an entry). (b) The answer depends on the grid's pain score (on RCS08, 3,123 of 5,148 stored values differ between the NRS and Left Leg VAS answers), and the Closed-Loop "Choose a band" card read the NEWEST answer of any grid; each answer's sidecar now names its grid's key and the card reads only the answer for the grid it shows, "not tested" otherwise, never a borrowed one. (c) Found while proving (b): the Closed-Loop page sends the Biomarkers page's clinic-sheet switch, and the server's list of grid settings (`adapter.GRID_SETTING_KEYS`) dropped it, so with sheets on the card read the sheets-off grid -- decision 131 broken since decision 186 added the switch; added. Live RCS08, the card before and after, store writes off, no grid built: daily defaults 37,522 fields in common, 0 differing; the Biomarkers page's settings 37,522 in common, 27,667 differing, all from reading the other grid -- before, the sheets-off Left Leg VAS grid with the NRS grid's stability answers (22 behaves differently, 110 cannot tell); after, the sheets-on grid with its own (26, 103, 3 behaves the same), the counts the Biomarkers page shows. Both Biomarkers grids now read their own answers from the store (132 of 132 rows each). Tests RED first: 1 store, 3 Closed-Loop; host 1494 / 2 / 0, container 710 / 0; workers reloaded. Also: the exploratory-ladder plan's live-watch step ticked (watched 2026-09-22).
249. The chosen closed-loop band is recorded on the server (the PI's ruling 8 of decision 233), and the sign-off sheet names the band it signs and the grid it came from (panel D item 8). Until now the band lived only in the browser that chose it: another browser, or a cleared one, opened on "No band has been committed", and nothing recorded which band, when, or by whom. (a) `ClosedLoopDeployment/chosen_band.py`: an append-only table, `closed_loop_chosen_band`, created on first use like the store's ledger (no migration); a choice or a clear adds a row, nothing rewrites one, the current band is the newest row, every row names the account that made it and where it came from (the grid, an uploaded file, or a browser's own copy carried over). Unlike the ledger a failed save is reported, never swallowed. One endpoint, `/api/queryClosedLoopChosenBand`, actions read, choose and clear, behind the same participant-access check as the page's other requests. (b) The page draws from the browser's copy at once and then takes the server's record, which wins; a clear on the server clears the browser too; a band held only in a browser from before is sent once, keeping the time it was chosen, and the page and the sheet then say it was carried over and by whom -- never that that account chose it (the first live look printed "by demo@bravo.local" as the chooser; corrected and pinned). When the server cannot be asked or a save does not land, the page says the band is held in this browser only, with the reason, in the warning colour. (c) A band chosen on the grid now carries the grid's own settings tag; it reaches no request (the report is built from named fields of the band). The sheet's new "THE BAND SIGNED FOR" block prints the band, when and by whom it was chosen, where the record is held, and the grid's settings line (which now says when clinic-sheet ratings are included), or that the grid was not recorded. FOUND, NOT CHANGED, for the PI: a band chosen on the grid carries an empty label, so the deployment summary rebuilds its pain score and split from nothing and uses the defaults -- the sheet's "PRO metric / binarization" line reads nrs / tertile whatever grid the band was picked from; the new block now prints the grid beside it. Live, RCS08: the endpoint as a real account on MySQL inside a transaction rolled back (choose, read, a malformed band refused by name, an unknown action 400, clear; 0 rows after the rollback); then the page in the browser pane carried over its own copy, L 0-2+ at 24.5 Hz chosen 2026-09-16 03:44 UTC, as one row, and on reload read it back without a second one. Tests RED first: 10 host, 13 jest; jest 77 of 79 on the Closed-Loop page, the known pair; host 1504 / 2 / 0, container 710 / 0; chunk 732.8eb74a0b; workers reloaded.
250. The parameter card's check notes get one heading, and the two long-failing Closed-Loop page tests are repaired (panel D item 7; the known pair of decision 147). (a) Each row of the "Full parameter recommendation" card can carry up to four notes -- the noise-only design rule, where the readings sit against the thresholds, the start-of-stretch check and the robustness interval -- printed one after another with nothing saying they are separate checks. The panel asked for one heading, "Four independent checks on this number"; on the record no row carries four (the two threshold rows carry two, the two onset rows and the start-up row one each), so the heading counts its own row's notes ("Two separate checks on this number, each measured on this participant's own record:" / "A check on this number, ..."), and says "separate", not "independent", which would read as a statistical claim. Live in the planning view: 2 rows with two, 3 with one. (b) `panels.payload.test.js`: one test looked for "NOT COHERENT", renamed "SIGNS DISAGREE" at the PI's request on 2026-09-11, and had failed since; its name also claimed the cell was lit while it only checked the words were drawn -- it now checks the lit cell's own explanation is printed and the other's is not. The other looked for "1 of them can be resolved at the programmer" where the page now says "1 resolvable at the programmer" and "3 need a change to the analysis" -- the same counts. Checked on the way, because the two fixtures disagree about the signs: each is internally consistent (the 2026-09-15 fixture's signs agree, this file's disagree), and the live captures of 2026-09-22 agree edge by edge with their own coherence block on both sides. Jest: 80 of 80 on the Closed-Loop page, 226 of 226 across all 34 suites -- no known-failing page test is left, so CI could now run jest (not changed; CI is the PI's). Chunk 732.ca440c59. No backend change.
251. What the next visit must deliver is on the Stim Optimizer page (decision 239's `coverage_gap`, built, tested and reached by nothing until now). Every per-rate row whose coverage check fails -- the separate fits, the fits pooled over pulse widths, and the clinic-stream fits -- carries `coverage_gap`: how many current pairs are missing, which settings already on the record to repeat and what each needs (more ratings, another day, or both), the new settings that would also count, and why, under the PI-stated safe ceiling (4.5 mA each side on RCS08) with the side not being stepped held at its current in force. The left side is stepped unless it already spans enough and the right does not. A row that passes, or was not fitted, carries none. The current-map card prints it in the open under "Enough combinations tried?" ("What the next visit must deliver: repeat L3.5/R3.5 (on 1 more day), ..."). Live RCS08, the page's own two requests, store writes off, the saved answer bypassed: main request 10,870 fields, 0 differing; two-stage request 39,796 in common, 1 differing (its own timing), 668 added, all `coverage_gap`, 0 removed; the same code twice differs in 1 field (timing). Build time, alternating before / after / before / after: 35.1 / 32.1 / 31.5 / 32.0 s, no cost. What it says: the clinic stream at 55 Hz, 60/160 us, is 4 pairs short (as decision 239 measured), cheapest first repeating L3.5/R3.5 on one more day; at 100/150 us, 5 short; the separate 110 Hz 140/180 fit, 4 short. FOUND, FOR THE PI: decision 239's ruling-5 answer -- the clinic stream at 55 Hz with the two pairings merged is one pair short, closed by one more rating at L3/R3 [corrected in 255: 3 of 6 on the Left Leg ratings] -- is on no response, because the clinic stream is fitted with the pairings separate only (the pooled-across-pulse-widths toggle covers the REDCap stream); so the card shows the 4- and 5-short answers of the separate fits. Tests RED first: 4 host, 2 jest (one fixture corrected on the way: it had the left already spanning 1 mA, so the right was the side to step); jest 54 of 54 on the Stim Optimizer page; host 1508 / 2 / 0, container 710 / 0; chunk 100.6a4e7cee; workers reloaded; watched live on the page: 4 rows carry the line, the passing 55 Hz 60/160 row none.
252. Does the left chronic band power step when the left settings change? A one-off report (panel B item 3; built 2026-09-23 in the order agreed with the PI), `BRAVO/_agent_bridge/stepB3_chronic_level_shift.py`, writing nothing to the store and read by no page. The device's own chronic log (one band-power reading every 10 minutes per side, in its units, with the current it delivered) is compared across each pair of consecutive left-side settings -- current, pulse width, rate, stimulating contacts, from the raw kind `therapy_settings` -- each held 7 days or longer: the median of California-day medians over the last 30 days of the first against the first 30 days of the second, a moving-block bootstrap interval over days, and the size against the first stretch's 10th-90th percentile day-to-day scatter; both stretches must have 7 or more days at the SAME band centre and contacts, since the device moves those on its own schedule. No drift term, no calendar-time covariate (193-196). THE FIRST VERSION WAS WRONG IN A WAY THE DATA SHOWED: it compared against the setting just before each held one, which on RCS08 was usually a clinic visit's intermediate step lasting minutes to hours (0.1-0.8 h), so the stretch before was nearly empty and 1 of 21 changes read; the brief steps are now left out of both stretches (the self-test plants a one-hour step just before its planted shift). Self-test: a planted +40 step reads +39.1 (+35.4 to +44.0), a change with no step -2.2 (-6.9 to +3.3), a change coinciding with a band move is refused. Live RCS08: the left chronic log is 28,890 readings on 223 California days, with no left readings in January to March 2026 or June 2026 (the right log has them); 107 runs of one left setting, 22 held 7+ days, 21 transitions; 4 readable, the other 17 in the gaps or across a move of band or contacts -- panel B's "93 of 112 changes with 7+ chronic days both sides" does not reproduce under these conditions. The three on L 1-3+ at 23.44 Hz, the three most recent current changes, all point the same way, power falling as current rises: 2026-07-22 4.0 -> 4.5 mA, 166.5 -> 143.8, -22.8 (-51.0 to +21.3), -0.25 x scatter; 2026-08-12 4.5 -> 3.5 mA, 143.8 -> 232.0, +88.2 (+44.5 to +120.8), +1.31 x scatter; 2026-09-03 3.5 -> 3.0 mA, 232.0 -> 261.0, +29.0 (-75.5 to +85.5), +0.23 x scatter. Only 2026-08-12's interval excludes zero (uncorrected for 4 tested). The fourth, L 0-3+ at 26.37 Hz on 2026-04-17 (1.6 -> 0 mA), reads -4.2 (-22.2 to +6.5). A fact to weigh beside E1 (decision 213), not an input: nothing reads it. No product code changed.
253. Why the Stim Optimizer's pain maps fail their calibration check (panel C item 3: the diagnosis the panel made a condition on any boundary-avoiding kernel). `stage1_openloop.calibration_diagnosis`, carried inside each surface's calibration block on the response (on no page, like the check itself, decision 238; a warning, it refuses nothing): on the leave-one-block-of-time-out fold, each held-out error in units of the model's own stated uncertainty is split into the part its whole block shares and the rest, and named -- intervals that cover: "calibrated", or "honest but uninformative: thin data" when the surface does not beat the training-fold mean (the one case a boundary-avoiding kernel could help); intervals that miss mostly by whole blocks (over half the squared miss shared by blocks, beyond chance): "moves between blocks of time"; otherwise "too confident within blocks: the shape or the noise model". Beside it, the reference no model choice explains away: the objective at the setting delivered most often, per block, and whether it moved beyond its own noise (precision-weighted test). Nothing about time enters any model (193-196); this names the movement, it does not model it. The block fold's held-out predictions are now made once and shared by the check and the diagnosis. Live RCS08, the page's two-stage request, the old check restored verbatim for "before": 40,464 fields in common, 1 differing (its own timing), 298 added (the diagnosis), 0 removed -- no calibration number moved; the same code twice differs in timing only; build time alternating before / after / before / after 31.3 / 31.2 / 30.6 / 30.4 s. WHAT IT SAYS. The left-leg surface at 55 Hz, 60/160 us (19 epochs), the one the current map's verdict rests on, MOVES BETWEEN BLOCKS OF TIME: held-out coverage 0.74, 62% of the squared miss shared by whole blocks (p 1e-7), the first block missed by +2.12 of its own standard deviations; and the objective at the setting delivered most often there (L1.6/R1.2 mA, 8 epochs) fell block by block, 1.51 (+-0.12) -> 0.53 (+-0.24) -> -0.06 (+-1.50), p 0.0006 -- the setting did not change and the patient's response to it did. So, by the panel's own condition, no boundary-avoiding kernel is built: it would answer thin data at the edges, and this surface's problem is that it moves. Every other fitted surface reads "honest but uninformative: thin data" (left leg 110 Hz; the clinic stream at 55 Hz 60/160 and 100/150 and 110 Hz; the back site at 110 Hz): intervals that cover (0.90-1.00) around a surface no better than the mean. The back site at 55 Hz reads "too confident within blocks" (coverage 0.84), and its most-delivered setting moved too (0.98 -> 2.32 -> 0.54, p 3e-15). Tests RED first: 6 host; host 1514 / 2 / 0, container 710 / 0; workers reloaded.
254. A band chosen on the grid carries its grid's pain score, split and match window to the Closed-Loop deployment summary (the PI, 2026-09-23 night: "solve everything you can without my decisions"; found in 249). The page's own helper rebuilds the summary's settings from the chosen band "so the deployment ROC defines the band feature with the SAME binarization the candidate was validated under", and read only the band's label -- empty for a band picked on the "Choose a band" grid -- so the summary used NRS and a tertile split whatever grid the band came from. It now fills what the label leaves out from the grid's own settings tag (carried on the band since 249): the pain score, the split and its percentiles, the match window. Not carried, on purpose: the match direction (the summary takes it from the chosen cut-point, and a value here would override it) and the clinic-sheet switch (the summary reads REDCap ratings only and has no way to include the sheets -- a limitation, stated in the helper). The helper moved out of the page file into `ClosedLoopSim/candidateRequestParams.js` so it can be tested. Measured live on RCS08, L 1-3+ at 24.5 Hz, the summary with the old empty settings against the grid's (Left Leg VAS, tertile, 60 min): area under the curve 0.505 (0.342 to 0.679) under NRS becomes 0.623 (0.463 to 0.731) under Left Leg VAS; the verdict and every gate's state unchanged. Four jest tests, 2 watched RED on the old logic.
255. The clinic stream's own fit pooled across pulse widths, and the PI's ruling 5 answer, on the Stim Optimizer page -- and a correction to decision 239 (the PI's ruling 5 of decision 233; found in 251). (a) The clinic-stream fit has always built its surfaces pooled over pulse widths (the fitting routine pools by default); the serialiser dropped them, so the current map's "Pool pulse widths" toggle swapped the REDCap section only. `two_stage.stage1.pulse_width_pooling_clinic` now carries them (and on the back site's parallel fit), and the toggle swaps the clinic section too, saying so where none is available. (b) Ruling 5 exactly -- the next session at the rate and pairing in force, its ratings merged with the earlier clinic record at that rate, the pairing holding the most clinic epochs there -- is `clinic_stream.next_session_coverage` (`clinic_pain.next_session_coverage`): the pairings merged, the coverage check and the gap under the safe ceiling, printed in the open at the head of the clinic section. Live RCS08: it merges 100/150 us (in force, 10 epochs) with 60/160 us (24), never the other nine pairings at 55 Hz: **3 of 6 qualifying pairs; the next session must repeat L3.5/R3.5 on one more day, L3.5/R2.5 with 2 more ratings and L2/R1.6 with 3 more.** **CORRECTION TO DECISION 239**, which reported this merge as 5 of 6, closed by one more rating at L3/R3: its probe counted every clinic epoch, 70 at 55 Hz on the two pairings, and 36 of them carry no Left Leg rating at all (steps that scored other sites); the fit, the card's coverage check and this answer count the 34 that do. Rerunning 239's own probe with that one filter gives 3 of 6. (Within a kept epoch the rating count is still the epoch's step count, a pre-existing detail of `epoch_frame_from_steps` left as it is: changing it would change the fit.) Live, the page's two-stage request, store writes off, the saved answer bypassed: 40,762 fields in common, 1 differing (its timing), 14,976 added (8,174 the pooled clinic fit, 6,729 the back site's, 73 the ruling-5 block), 0 removed; the same code twice differs in timing only; build time before / after / before / after 34.7 / 34.3 / 33.9 / 33.5 s. On the page: the ruling-5 line at the head of the clinic section; the toggle shows "pooled over 5 pulse-width pairings" on the REDCap section and 11 on the clinic one. 4 host tests and 3 jest, RED first. Host 1518 / 2 / 0, container 711 / 0 (254-257 together); jest 235 / 235 across 37 suites; chunks 100.d7f2af35, 153.4bf14d3c; workers reloaded.
256. The stability run's stopped-early rule protects its own grid's answer, not the newest of any grid (follows 248). The rule keeps a run that stopped after repeated failures from replacing a complete stored answer; it asked for the newest answer of ANY grid, which under one entry per kind was the same thing, and under one per grid (248) threw away every partial run for a grid with no answer of its own, leaving its rows "not tested" where they had always been written. It now looks up this grid's own entry. The existing test (the same grid rerun, its complete answer kept) passes unchanged; one new test, RED first, in the container.
257. The CI runs the page tests Jest was left out of CI (`.github/workflows/ci.yml`) because two page tests had been failing since 2026-09-11 (decision 147) and a step that starts red teaches everyone to ignore it; both were repaired in 250, so the frontend job now runs every jest suite once before the build. CLAUDE.md's CI line says so. Local: 235 of 235 across 37 suites (Node 24); the first CI run with it (35846470031, Node 16) passed every job, the page tests 235 of 235 across 37 suites.
258. The PI's rulings of 2026-09-24, built. (a) THE SIGN-OFF CARD IS THE LAST CARD on the Closed-Loop page, below the CL-DBS simulations, and the jump links follow (decision 244's order test extended: the last section and the last link are the sign-off). (b) A RED-OUTLINED BUTTON puts the clinic-sheet ratings in the deployment summary, or not ("Clinic-sheet ratings in the summary: on / off", in the page header; off by default; remembered per participant in this browser). On, the summary's request -- shared by the deployment ROC and the other sign-off panels -- carries the switch, and the one setup behind them (`_band_validation_setup`, which read REDCap only) merges the sheet ratings that carry the chosen score, through ONE helper the heat-map grid and its drill-down now call too (`_merge_clinic_sheet_ratings`; the two copies before it are gone). The sign-off sheet prints which ratings were used ("Pain ratings used: REDCap reports plus 207 clinic-sheet ratings"). Because the stability grid shares that setup, a sheets-on grid's stability answers were computed without the sheets; they now include them, and the stability rule version is v4 so every stored answer rebuilds. Live RCS08, L 1-3+ at 24.5 Hz, Left Leg VAS: off, the summary is unchanged (0.623, 0.463 to 0.731); on, 207 clinic-sheet Left Leg ratings are merged and it reads 0.564 (0.396 to 0.684); verdict and every gate's state the same either way. (c) CLINIC EPOCHS COUNT ONLY THE RATINGS THAT CARRY THE SCORE BEING FITTED -- the Left Leg for the leg fit, the back for the back fit, the NRS (the sheets' overall item) for an NRS fit: each setting carries per site how many steps scored it and on which days (`n_<site>`, `rating_days_<site>`), and the fit narrows to its site (`clinic_pain.epochs_for_item`). Found on the way: the fit kept only settings with a Left Leg score WHATEVER site it was fitting, so the back site's fit had dropped every setting scored on the back alone. Live RCS08, the page's two-stage request, the old clinic module loaded from git for "before": left leg, the same 124 settings, ratings 102 -> 73 at 55 Hz 60/160 us, 27 -> 11 at 100/150, 40 -> 28 at 110 Hz; the back site 124 -> 135 settings (11 back-only settings kept), ratings 102 -> 76, 27 -> 12, 36 -> 25; ruling 5's merged answer 3 -> 2 of 6 qualifying pairs (next session: repeat L3.5/R3.5 on one more day, L3.5/R3 with 3 more ratings, L3/R3 with 2 more and another day, ...); no speed changes whether it can recommend a current. 11,749 of 51,088 fields differing, all in the clinic blocks; the same code twice differs in timing only; build time before / after / before / after 28.7 / 34.7 / 28.5 / 35.1 s (the back fit now has more settings). (d) LEGIBILITY, measured on the live pages: the Closed-Loop page had 79 of 117 visible text elements under 11 px (8.5 px at the smallest in the source) and captions in greys at about 3.5:1 on white. Every text and figure font on it is now 11 px or more (149 text sizes and 51 figure fonts raised; 10.3-10.9 px became 11.5), the light greys (45) and the de-emphasis grey #9A9A9A (struck-through rows, unlit state cells, separators, the empty-cell placeholder) are #5E5E5E and #6E6E6E, and both pages' content takes a darker theme text colour (#5E5E5E, about 6.4:1, for the app theme's #7b809a, 3.9:1: 40 elements on the Stim Optimizer page, 5 on the Closed-Loop page) without changing the rest of the app. Live after: 0 elements under 11 px, no light grey left, the same overflow count as before (8). A source test pins the floor on both pages. Left alone: the recompute bar's two small lines, which are `RecomputeBar.js` (the PI's file, rule 7). Tests RED first: 5 host, 2 container, 6 jest; host 1523 / 2 / 0, container 713 / 0, jest 244 of 244 across 39 suites; workers reloaded; stability answers restarted for both grids under v4.
259. The recompute bar enlarged (the PI, 2026-09-24: "yes, enlarge the recompute bar too", lifting rule 7 for this one change, as decision 54 did once). `RecomputeBar.js`, shown at the top of the analysis pages, printed its state line at 9.5 px, its explanation at 10.5 px and its stale reasons at 10 px, and drew the resting state in the palette's neutral chart grey; it now uses the Stim Optimizer's type scale -- state 12 px bold, explanation 13 px, reasons 12.5 px, the not-kept note 12 px -- and #5E5E5E for the resting state (the stale amber unchanged). Live on the Stim Optimizer page: 12 and 13 px, and no text under 11 px left on the page. The legibility test (258) now reads this file too, watched failing on the old one. Jest 244 of 244; no backend change.
260. Every view and product starts at the implant date (the PI, 2026-09-24: "remove all data from the chronic view or whatever before the implant date"; he confirmed July 16 2025). One helper, `DecodeCommon/data_start.py` (the earliest dated device row; RCS08 2025-07-16 18:06 UTC); measurements before it are dropped where they are read, and a setting is clamped: the last change before it is kept and moved to it. Applied at the therapy history (`Therapy.queryTherapyHistory`), the chronic extraction and its saved derived recording (stamped with the start, rebuilt once), the event query (`Event.queryDBSEvents`), the Biomarkers loader, its five patient-event reads and its recording-set label, the settings stream (rule v3) and the Biomarkers current reader; the Closed-Loop inputs entry's rule is v3. What went: 29 patient events (from 2025-06-18), 3,778 chronic timestamps (from 2025-05-26) and 793 therapy timestamps (from 2024-11-16) -- the January-April 2025 snapshots name the leads "Right STN", SenSight B33005, where every record from implant names them "Right VIM", B33015 -- and 14 settings-stream rows. Live RCS08, the committed tree against the new: events 20,310 -> 20,281, the rest identical; the heat-map grid 34,129 fields, 21 differing, all timing and the grid's key; the Stim Optimizer two-stage request 57,381 in common, 308 differing: 137 epoch numbers (3 unrated pre-implant settings gone; the 92 rated settings, their currents and 776 ratings identical), 160 readiness-audit counts (29 bench patient-event rows gone; four 130 Hz cells lose their one bench amplitude, unusable before and after) and bookkeeping; the Closed-Loop report (L 1-3+ 24.5 Hz) 55,714 fields, 53 differing: 21 timing, keys, the band-stability test (stimulation-off measurements 92 -> 87, p 0.043 -> 0.010, verdict the same) and E2's interval (p 0.61 -> 0.53), every verdict the same. No pre-implant timestamp is left in the timeline, the chronic view or the therapy history. Tests RED first where the rule is new (settings stream 3, Closed-Loop inputs 1); the helper's and the Biomarkers container tests were written with the code. [CORRECTED 2026-09-26, decision 290: E2's p-values here (0.61 -> 0.53) were read on the previous settings period's ratings; with the join fixed, on the full tiles, E2 for L 1-3+ at 24.5 Hz under NRS reads 0.564 (0.438 to 0.678), p 0.29, 43 pain reports. The before/after comparison, both sides on the same join, stands.]
261. The coverage check counted a stored table's rating days wrongly (found proving 260). A matched therapy-and-pain table read back from the store carries each epoch's days as a numpy array; `stage1_openloop.current_coverage` recognised only tuples, lists and sets, so a SERVED table's days fell back to a sum of per-epoch counts (a day shared by two settings counted twice: 81 days at L1.6/R1.2 where the union is 78) or, on the home schedule's "what this buys", to none (pairs with 16 and 19 real days read 0 and "does not qualify"). Every page load after the first read the stored table. Arrays are now read as days; the stored and the rebuilt table give the same coverage at 55 Hz 60/160 (9 pairs, identical days, passes). Two tests RED first (4 and 0 days where the union is 3).
262. The PI's two analyses, run 2026-09-24 (read-only; `artifacts/research_2026-09-24_SYNTHESIS_state_washin_threshold_offperiods.md` §4). (a) The 0 mA test WITHIN each stretch with both sides off: in 2025-07-16 to 08-22, matched to the same day's recordings (75 of 96 reports, 25 days), the left 21.5-25.5 Hz family rises with VAS pain -- L 1-3+ 21.5 and 22.5 Hz +0.38 and +0.35, L 0-2+ +0.36 and +0.40, q 0.022-0.044 -- and not detectably with NRS (spread 0.49); in the next eight weeks (left off, right on) the same bands fall with pain under opposite trends; the 2026 off stretches have device snapshots only, on R 0-3+. A lead to confirm. (b) A current with memory (time constants 1 h to 14 d) never predicts pain better than the current in force, out of sample; decision 253's drift is not explained by it. (c) CORRECTION TO 240: its probe matched reports within 60 SECONDS (the longest length of signal passed as the match window) and dropped the middle third; at the page's 60 minutes L 1-3+ has 201 reports (30 s) and 187 (60 s), the current alone 0.600 / 0.708, every band 0.722 / 0.743 against null 95ths of 0.716 / 0.673, and with the current taken out 0.681 (inside) / 0.683 (outside, p 0.04, uncorrected for 12 combinations). 240's 0.879 / 0.701 / 0.526 and the 19 zero-current reports were the 60-second window's.
263. The implant-date cut reaches the export, the raw recordings lists, the participant-context service and the custom-analysis pipeline too (the PI, 2026-09-24: "yes, cut the export and custom analyses too and raw recordings list"). Two helpers join `DecodeCommon/data_start.py`: `trim_segment` (samples before the start cut on a time axis) and `listed_date` (a row wholly before the start is not listed; a chronic file that runs past it is listed from the start). The export leaves out therapy changes, annotations, events, recordings and therapies dated before the start and re-encodes a chronic file that spans it; the raw recordings list (`queryAllRecordings`) and the custom-analysis listing drop rows before it and list the spanning chronic files from the implant date, flagged `TrimmedAtImplantDate`; `Database.listRecordings` and the participant context filter the same way; the custom-analysis pipeline trims its chronic input and rebuilds any cached input that carries no start. Live RCS08, the previous commit against this one: the raw recordings list 1,590 rows both, the four chronic files dated 2025-06-18 now listed from 2025-07-16 18:06:21, no row before it; the export 131,634 sections -> 131,120 (184 therapy changes, 93 therapies with their settings, 29 events gone; 4 chronic files trimmed), no section before implant, 1,384,393,052 -> 1,383,932,768 bytes. NOT PROVED LIVE: the participant-context service fails for RCS08 before and after on a source file with no `hashed_id` (`Database.py`, the `check_files` test), a fault this change leaves alone; RCS08 has no custom analyses. Six helper tests; host 1535 / 2 / 0, container 723 / 0.
264. Control analyses, saved and shown where they belong (the PI, 2026-09-24: saved results; 1, 2 and 4 first with the literature link, then 3 and 5). A new package, `modules/ControlAnalyses/`: a registry (each analysis's page, title, what it does, and links to the literature reports of 2026-09-24), a record of saved runs (`snapshots.py`: one JSON file per run under the data server path, `BRAVOStorage/control_analyses/<participant>/<analysis>/`, written whole or not at all, every run kept, the page reads the newest; not the cache store, which keeps one entry and can be switched off), the statistics as tested functions (`stats.py`: Spearman within stretches, intervals resampling whole California days, Benjamini-Hochberg, the exact remembered current, the held-out memory curve), and runners that reuse the modules' own matching (`runners.py`; `python3 -m modules.ControlAnalyses.run <key|all>`, offline only; a page never starts one). Decision 246's clock-and-weekend check moved into the package (`time_of_day.py`), and the script now imports it (one copy; its self-test passes). One endpoint, `/api/queryControlAnalyses` (Page biomarkers or stim_optimizer), behind the participant-access check. One card, `ControlAnalysesCard`, at the foot of the Biomarkers and Stim Optimizer pages: a dropdown, the run's date and data span and how many runs are kept, a figure or table per analysis, the reading, the literature links; an analysis never run says so. Biomarkers: (1) band against pain with the current off, stretches found from the settings stream (a visit under a day does not split one); (2) what the current explains, at the page's 60-minute window; (3) time of day and weekends. Stim Optimizer: (4) a current with memory with decision 253's drift re-read; (5) pain around each on/off switch, each described by the side that changed. Live RCS08, run 2026-09-24 on data 2025-07-16 to 2026-09-23: (1) both off 2025-07-16 to 08-22, 75 ratings on 25 days: L 1-3+ 21.5 and 22.5 Hz rise with VAS (+0.38, +0.34), L 0-2+ 21.5 Hz (+0.36), q < 0.05; (2) and (4) reproduce decision 262 to the third decimal; (3) see 265; (5) 15 switches. CORRECTED ON THE WAY: the first stretch finder split the left-off stretch of 2025-08-22 to 10-21 at clinic visits (merged under a day); a switch that ran one stretch into another was printed as an end and a start (now one switch named by what changed); the figures' inks were undefined, drawing black (jest now requires a colour on every mark, watched failing on the old import). Tests: 17 in the package (both runners), 6 jest; host and container suites below.
265. CORRECTION TO 246: R 1-3+ has no daily cycle. Decision 246 found a strong daily cycle on R 1-3+ (21 of 22 bands, peaking around midnight); run from the implant date on (decision 260), R 1-3+ has 335 recordings, not 364, and a daily cycle above chance in 0 of 22 bands. The 29 recordings removed are the patient events from before implant, the device on the bench. L 0-2+ keeps its cycle (18 of 22, was 22 of 22). L 1-3+ still runs higher at weekends in 12 bands (was 15); pain still runs higher at weekends within the week (NRS +0.30, 0.12 to 0.48, p 0.003, 58 weeks; VAS +3.86; McGill +1.89; back +3.43; left leg +2.91, interval across zero).
266. Speed-up item 1: the three-second tiles are built in batches (the PI, 2026-09-25, "go on all", on the handover from the grant session). The tile build called the transform once per 3 s piece (299,593 calls on RCS08) and made one Python row per piece: 25.6 s and 18.2 s of a 47.8 s profiled build. Now the per-piece gates (enough finite signal, no sample at the rail, missing packets within 10%) are computed for every piece of a recording at once, the pieces that pass and are whole and all-finite are transformed together (`analytics.td_transform_band_power_batch`, 4,096 pieces a batch), the rest take the single call, and the rows are made in one pass (`availability._td_tiles_batched`). THE FFT IS STILL CALLED ONCE PER PIECE: the container's FFT library (numpy 2.1.3, aarch64) does rows in pairs and a leftover row alone and rounds the two differently in the last digit (up to 1.6e-14), so batching the FFT across pieces changed values; keeping each piece's own call keeps them identical. Live RCS08, the six sensing pairs, 302,415 tiles: 32,310,237 fields, 0 differing (the build run twice under the old code also differs in 0); alternating rounds before / after / before / after 35.8 / 12.4 / 37.0 / 12.5 s. The saved tiles are the same product, so their key is unchanged. Two container tests, the old loop kept as the reference; host 1552 / 2 / 0, container 742 / 0.
267. Speed-up items 2 and 4: work repeated inside one request is done once (the PI, 2026-09-25). Measured first with the store writing to a scratch folder, which is how a live first request behaves (the earlier probes blocked writes): a cold Closed-Loop request built the tile table ONCE -- asked for three times, the other two read the saved file -- so item 2, "the tile table is built twice", was the probe's blocked writes and needs no change. The repeats that were real: the tile key 15 times (8.1 s), the recordings decoded 9 times (6.5 s), REDCap pulled 3 times (5.0 s). The within-request scope that held the pain reports (`pro_request_scope`) now also holds the tile rows' digest and each decoded recording list (`_request_memo`; a list comes back as a fresh list), and the Closed-Loop and Stim Optimizer requests now run inside it; it still ends with the request, so a new recording or report is seen by the next one. After: 0.5 s, 2.3 s, 1.2 s. Live RCS08, the committed tree against the new, store writes blocked: Closed-Loop left 59,166 fields, 3 differing (timing); Stim Optimizer two-stage 57,539 fields, 3 differing (timing, the response key, which carries the module's code digest, and a bookkeeping flag); heat-map grid 34,129 fields, 19 differing (all timing). Cold Closed-Loop requests with a scratch store, alternating before / after / before / after: 172.9 / 171.5 / 147.4 / 129.9 s; the second request in the same process 134.8 / 29.5 / 37.2 / 29.8 s (the first is an outlier). Two container tests, RED first; host 1552 / 2 / 0, container 744 / 0.
268. Speed-up item 5: the E2 interval with the current taken out runs its fits on one linear-algebra thread (the PI, 2026-09-25). Profiled on the inputs of a live Closed-Loop request (53,246 samples, 30 pain reports): 10.5 s of the step was 1,003 two-column least-squares fits at 9.6 ms each, the container's 16 OpenBLAS threads spending longer coordinating than fitting (40 fits: 0.36 s on 16 threads, 0.03 s on one). The fits' coefficients were identical at 1, 2, 4, 8 and 16 threads, so `_partial_corr_report_bootstrap` caps the library at one thread for its loop (threadpoolctl; without it, as before); nothing else's thread count changes. Batching the fits into one computation was not done: it changes the arithmetic. Live RCS08: the step before / after / before / after 10.63 / 1.11 / 10.42 / 1.08 s with identical printed values; the Closed-Loop left report 59,166 fields and the right 59,178, 3 differing each (timing). One container test (the interval with the cap equals it without, line and spline shapes); host 1552 / 2 / 0, container 745 / 0. [CORRECTED 2026-09-26, decision 290: the values printed identically before and after were E2's current-removed interval on the previous period's ratings; the equality and the timings stand, and the step reads the fixed join since 290.]
269. Speed-up item 3: the design rule's two-component filter runs compiled, every value unchanged (the PI, 2026-09-25: "add numba and do option 1"). numba 0.61.2 and llvmlite 0.44.0 (the line that supports numpy 2.1) are installed in the container and pinned in `requirements.txt`; numpy stays 2.1.3. The filter did about 30 small array operations per time step over 349 stretches x 1,499 steps, 29 ms a call and about 1,600 calls a fit, nearly all numpy's per-call overhead. The compiled loop (`design_rule._filter_2comp_kernel`) performs the same operations on each stretch in the same order: add, subtract, multiply and divide are exactly rounded whichever code runs them; the log is the same library function (2,000 arrays, 0 differing); each step's terms are summed in numpy's own pairwise order over the whole row (3,000 arrays, 0 differing -- a left-to-right sum and "first element plus the rest" both differ). The numpy loop stays as `_filter_2comp_numpy`, used where numba is absent (CI) and by the proof. NO ON-DISK CACHE: the summation is recursive, and a cached recursive function segfaulted when loaded under the server's libraries; each worker compiles on its first fit, about a second. Live RCS08: the fit before / after / before / after 26.0 / 4.0 / 26.6 / 3.8 s, the fitted model and the choice identical in both rounds; the Closed-Loop left report 59,166 fields and the right 59,178, 3 differing each (timing: the design rule 25.6 -> 3.1 s and 32.3 -> 3.8 s; the whole report, store writes blocked, 86.4 -> 41.8 s and 105.5 -> 49.1 s). Two tests (the filters equal over 160 parameter draws on panels with gaps and a missing first reading; the whole fit equal), RED first, skipped where numba is absent; host 1554 / 2 / 0, container 745 / 0.
270. The participant-context service (`/service/queryParticipantContext`, the tablet's sync) works for RCS08 again. One of RCS08's session files carries no `hashed_id`, and the check for "this session is already on the tablet" read it with [] and raised KeyError before any session was returned, since before decision 263 (found proving it). A file with no `hashed_id` cannot be in the tablet's list, so `Database._already_on_tablet` answers no and the file gets its full entry, which already handled the missing field. Live RCS08: 569 sessions returned; the chronic arrays start at 2025-07-16 18:10 (after the implant date) and no therapy change before it -- the part of decision 263 that could not be proved live. One container test, RED first; host 1554 / 2 / 0, container 746 / 0.
271. CI installs numba 0.61.2 and llvmlite 0.44.0 (the PI, 2026-09-25), so the host job runs the design rule's compiled filter and its equality test against the numpy loop (decision 269) instead of skipping it. `.github/workflows/ci.yml` only.
272. The carry-over test, saved (the PI, 2026-09-25, item 3 of "do 1, 2 and 3"): does pain at one current depend on whether the current was reached going up or coming down? `ControlAnalyses/carry_over.py` (free of the database, 11 host tests, RED first) and the saved analysis `carry_over_ladder`, third on the Stim Optimizer page's control-analysis card (one jest test, RED first). Within one visit of the clinic and at-home testing sheets, a current reached by a rise and the same current reached by a fall, the rate, pulse widths, contacts and the other side's current unchanged. The sheets write each ladder step twice (as it is reached and a minute later while it is held), so a run of identical settings is one step and its reading is its LAST rating of the site. Carry-over is told apart from pain drifting over the visit by which leg came first: carry-over lowers the falling reading in both orders, drift flips its sign with the order. Live RCS08 (30 sheets from the implant date on): ONE visit, 2026-09-16, rated the same current on both legs, 8 currents, and every fall came after its rise, so the record cannot separate carry-over from drift. On that visit, down minus up on the overall (NRS) score: left ladder -0.75 over 4 currents (0.5, 1.5, 2.5, 3.5 mA: -1.5, -0.5, 0.0, -1.0), right ladder +1.00 over 4 (+1.0, +1.5, +1.0, +0.5); read by hand from the sheet rows. No left-leg or back pair exists. A setting held and rated again: overall, stimulation on, median 1 minute later, 62 times on 13 visits, -0.06 (-0.26 to +0.27; lower 17, higher 9, the same 36); off, median 14 minutes, 10 times, -0.10 (-1.11 to +0.58). The stored titration ladders' settled band power (decision 213), the page's 22 bands less those on the stimulator: time-domain R 0-3+ (2 runs, 14 bands) +8.9% on the way down at the median band, higher in 11 of 14; L 1-3+ (1 run) +25.3%, higher in 14 of 14; again every fall after its rise. What would answer it: a ladder that starts at the top and steps down first, beside one that steps up first. Aggregates only are saved; nothing feeds a recommendation. Host 1565 / 2 / 0, container 757 / 0 (both run the package), jest 251 of 251 across 40 suites; chunk 646.3ca6dfcc carries the card's text; workers reloaded.
273. No pain rating is saved under a label that ignores it (CLAUDE.md section 8 rule 5; found by reading the code for the speed-up report, 2026-09-25). The Closed-Loop page's saved inputs held the design matrix -- the settings periods with their pain ratings -- under the recording set, the two constants and a rule version, and the joined table's in-process memo left the pain frame it merges in out of its label: a report filed without a new recording would not have reached the band-to-pain and current-to-pain readings (E2, E3) until a recording arrived. Measured first, live RCS08: the entry on disk (written 2026-09-25 08:47 UTC, after the newest report, 2026-09-23 18:07 UTC) matched a fresh build in every value -- 92 settings periods, 3,220 fields, the only difference the rating-day lists stored as an array against a tuple, 92 of 92 the same days -- so the fault was latent, not yet a wrong number. Now the entry holds the settings stream in place of the design matrix (rule v4) and the design matrix is built on every request through the Stim Optimizer's own matched table, which is keyed on the settings AND the pain-report snapshot; the joined-table memo's label includes the pain frame. Live, the Closed-Loop report L 1-3+ 24.5 Hz, before and after, store writes blocked: 59,169 fields in common, 3 differing (timing), 0 added, 0 removed. With a scratch store, alternating before / after / before / after: first request 119.4 / 130.5 / 119.5 / 115.6 s, a second request in the same process 25.0 / 23.5 / 23.9 / 24.1 s -- no cost. Three tests RED first (a new report reaches the design matrix with the recordings unchanged; no pain column is saved under the recording key; the joined-table memo tells two pain frames apart).
274. numba's type-checking log silenced below warnings (found proving 273). numba writes its own type checking at DEBUG and the server logs at DEBUG, so the first design-rule fit in each worker wrote 38,760 lines of it (decision 269's compiled filter); `design_rule` sets numba's logger to WARNING where it loads numba, so its warnings and errors still show. One test, RED first in the container once it set the root logger to DEBUG as the server does (a first version passed without the fix because plain pytest logs at WARNING).
275. The regression-to-the-mean check, saved (report 1 of the 2026-09-25 options; the synthesis's and the science critique's first question about decision 253). `ControlAnalyses/regression_to_mean.py` (18 host tests, RED first, a planted shared swing reads p above 0.9 and a planted setting-specific swing below 0.01) and the saved analysis `regression_to_mean`, fourth on the Stim Optimizer page's control-analysis card (2 jest tests). On the group decision 253 flagged (55 Hz, 60/160 us, left leg): the most-delivered setting, 1.6/1.2 mA, 8 of 19 settings periods, reads +1.53 / +0.54 / -0.05 across the three blocks of time (253's 1.51 / 0.53 / -0.06; heterogeneity p 0.00064). Is it specific to that current pair? A group of 8 drawn from the same 19 swings at least as much in 84.6% of all 75,582 ways to draw it (as far the same way in 43.0%); a swing that size appears in 33.8% of the 68 overlapping runs of 8 settings periods elsewhere in all the data we have; the first block sat +4.4 standard errors above the long-run average (+1.53 against +1.00). So the swing is the group's own pattern over those weeks, not something about 1.6/1.2 mA: decision 253's sentence 'the setting did not change and the patient's response to it did' reads better as 'the whole group's readings moved over those weeks', whether by regression to the mean or a shared calendar effect, which this check cannot tell apart. Descriptive; nothing reads it.
276. Decision 262's 'p 0.04' for the bands with the current taken out is replaced by that reading's own shuffle (the synthesis and report 5: the number came from a scratch probe and was read against the PLAIN reading's shuffled-data range). The diagnostic now refits the SAME current-removed model on each rotation of the pain label (`confound_diagnostic`, the same rotations, one more fit each; offline only -- the control analyses are its one caller) and the saved analysis `current_explains` carries its p. Live RCS08, the page's 60-minute matching, six sensing pairs at 30 and 60 s, 12 combinations: L 1-3+ p 0.030 at 60 s (187 ratings) and 0.035 at 30 s (201); the other ten 0.21 to 0.86. Corrected for the 12 (Benjamini-Hochberg, as the grid is corrected) both L 1-3+ readings are about 0.21; Bonferroni 0.36. A lead, not a finding (METHODS section 7). The plain readings reproduce 262 exactly (L 1-3+ 60 s: 0.708 / 0.743 / 0.683, shuffled 95th 0.673). Tests RED first in the container.
277. One harmonic check for both modules, in the PI's advisory wording (handoff item P-01, found 2026-09-25). The Stim Optimizer's `titration_plan.harmonic_avoidance` folded only |250 - rate| and the 1/2, 1/4 and 3/4 sub-harmonics, while the Biomarkers `analytics.harmonic_landings_hz` folds every whole multiple of the rate by the device's 250 Hz sampling; it now takes its folded landings from that one home and says a band 'carries a folded multiple of the stimulation rate', never that it 'measures the stimulator' (the PI's correction of 2026-09-06). At 55 Hz the fourth multiple (220 Hz) lands at 30 Hz and the fifth (275 Hz) at 25 Hz, so every centre from 22.5 to 29.5 Hz carries a folded multiple -- including 24.5 Hz, which decision 236 called 'the one clean band to watch'; 9 of 22 centres stay clear (8.5-10.5 and 16.5-21.5 Hz). At 60 Hz the landings are 10 and 20 Hz (fourth and eighth multiples) and 30 Hz (half the rate). Decision 236's watch lists now read: none of the watched bands clear. The titration card lists every landing the server sends. Live RCS08, store writes blocked: the Stim Optimizer main request 10,893 fields in common, 54 differing, 47 added, 17 removed, the two-stage request 57,569 in common, 55 differing, 47 added, 17 removed -- every one a harmonic field or its sentence (the clear and flagged lists, their reasons, the exploratory plan's watch lists, two readiness cells' harmonic notes), the response key (code digest) and one timing. Tests corrected RED then GREEN; jest pins pass.
278. The recording-set identity worked out once per request (speed-up report proposal 1). `recording_set_signature` reads three columns of the recordings (`values_list`) instead of whole joined rows and is held for the request in the scope of decision 267; a new recording is seen by the next request. Live RCS08, the Closed-Loop report L 1-3+ 24.5 Hz, store writes blocked, two rounds: 59,169 fields in common, 3 differing (timing), 0 added, 0 removed. The whole report, alternating before / after / before / after: 46.8 / 41.8 / 38.3 / 35.9 s. One test RED first (one database read per request; a new recording seen once the request ends).
279. The heat-map grid's medians in one computation (speed-up report proposal 2). numpy's `nanmedian` over rows falls back to one row at a time; `availability._whole_matrix_nanmedian` sorts the whole matrix once and reads each row's middle, reproducing it bit for bit, the infinity and all-excluded cases included (7 container tests, RED first, one negative control). Live RCS08, the grid, two rounds: 34,129 fields in common, 19 differing (timing), 0 added, 0 removed; alternating before / after / before / after 12.5 / 8.9 / 11.2 / 7.8 s.
280. Two page fixes from the handoff scan and the stale record corrected. (a) P-02: the Closed-Loop ROC panel labelled its cut-point in 'oriented log-power units' though the feature is raw power since decisions 202 and 204; it now reads 'oriented, standardized band power units'. (b) P-04: the sign-off card now says the mixed-effects check leaves out the first three weeks of the whole record, counted from the first recorded sample (not from each setting change), with the count the server sends; its own small component, because one more branch inside the card tripped the installed hooks lint rule's path counting. (c) Dated correction notes in four 2026-09-24 research documents that quoted decision 240's withdrawn numbers, decision 246's withdrawn R 1-3+ daily cycle or 262's 'p 0.04'; the digest's Part 2 'item 15' line (the sign-off card has been last since 258; three June audit items remain) and Part 1's one-band sentence (0 of 50 usable under 217); the device document's live composed constant (72.16, not 73.63). Jest 257 of 257 across 42 suites (two new files, each watched RED); host 1588 / 2 / 0, container 784 / 0 (277-280 together); chunk 32.a6dd8adf carries both sentences. HELD, not in the tree: P-13, a clinic-sheet cell with a written correction misread (saved as a patch; its live check on the sheets approved by the PI 2026-09-25, to run next).
281. A clinic-sheet cell with a written correction keeps what was delivered (handoff item P-13; the PI approved the live check on the sheets, 2026-09-25, counts only). A cell such as a pulse width with a correction written beside it in brackets was misread, the correction dropped and the previous step's value carried; `clinic_pain` now reads the delivered value and records that a correction was written; rule v2, so the stored table rebuilds at the next daily sheet sync (decision 182). Live RCS08, the committed parser against the new on all 30 sheets: 524 rows each, 0 added, 0 removed, 2 rows differing, both in the left pulse width, on one visit; the settings the fit reads go from 259 to 260 (one split in two). No cell content was printed. Tests RED first; host 1592 / 2 / 0, container 784 / 0.
282. Two saved control analyses on the Stim Optimizer page (the revised plan's A4 and A5; the science critique's M2 and M1). (1) Rating persistence: a day's mean rating correlates with the next day's at +0.62 on NRS (388 days, 186.8 independent, 48%), +0.57 VAS, +0.47 left leg, +0.52 back, +0.79 McGill; in the 0 mA stretch of 2025-07-16 to 08-22, 22.7 of 33 NRS days are independent. Report 02's targets of 52-98 independent days become 52-132 calendar days depending on how many bands are tested. (2) Stepped current, every band: on the time-domain route the bands far from pain (below 12 Hz, above 32 Hz) change 0.58-0.73 times as much per mA as the 21.5-27.5 Hz family (R 0-3+ 15.9% against 11.3%; L 1-3+ 5.2% against 3.8%; L 0-2+ 9.9% against 5.7%); an electrical effect predicts near 1, a family-specific signal well under 1, so this decides neither way; the device's own band-power route has too few bands to read. 18 host tests and 3 jest; both runs saved. Proof for 282-288 together, the committed tree against this one on RCS08, store writes blocked: Closed-Loop L 1-3+ 55,716 fields and R 0-3+ 55,729, 21 differing each, all timing; Stim Optimizer 10,911 and 57,587 fields, differing only in the response key (code digest), a manifest-written flag and one timing; heat-map grid 34,129 fields, 19 differing, all timing; 0 added and 0 removed anywhere. Host 1626 / 2 / 0, container 817 / 0, jest 267 of 267 across 44 suites. [2026-09-26: the stepped-current run was re-run on the full tiles (04:08:01Z) and every ratio above reproduces exactly; its run points of 2026-09-25 08:48 UTC predate the short copy.]
283. Speed-up proposals 4, 5 and 6 of the 2026-09-25 speed-up report, each exact: the CL-DBS simulation's controller loop, the design rule's local-level filter and the robustness replay compiled with numba in decision 269's pattern (same operations in the same order, numpy's pairwise sums, no on-disk cache, the numpy loop kept where numba is absent, equality tests in the container: the simulation's 56,619 compared values, 0 differing), and the joined table's time-block labels computed as one column instead of 11 million calls. Closed-Loop L 1-3+, alternating before / after / before / after: the whole report 36.1 / 24.6 / 36.7 / 25.1 s; simulation 5.9 / 2.4 / 6.1 / 2.4 s; design rule 2.3 / 1.1 / 2.4 / 1.2 s; robustness 1.2 / 0.4 / 1.3 / 0.4 s. Proof for 282-288 together, the committed tree against this one on RCS08, store writes blocked: Closed-Loop L 1-3+ 55,716 fields and R 0-3+ 55,729, 21 differing each, all timing; Stim Optimizer 10,911 and 57,587 fields, differing only in the response key (code digest), a manifest-written flag and one timing; heat-map grid 34,129 fields, 19 differing, all timing; 0 added and 0 removed anywhere. Host 1626 / 2 / 0, container 817 / 0, jest 267 of 267 across 44 suites.
284. The Closed-Loop stability panel says when its answer was assembled (June audit item [15], P-03 of the handoff scan, one of its three parts): read from the report's own freshness line, printed only when the test ran. Three jest tests (not watched failing on the old panel). The other two parts -- the per-block odds ratio has no interval, one rating can be split across two time blocks -- are still open. Proof for 282-288 together, the committed tree against this one on RCS08, store writes blocked: Closed-Loop L 1-3+ 55,716 fields and R 0-3+ 55,729, 21 differing each, all timing; Stim Optimizer 10,911 and 57,587 fields, differing only in the response key (code digest), a manifest-written flag and one timing; heat-map grid 34,129 fields, 19 differing, all timing; 0 added and 0 removed anywhere. Host 1626 / 2 / 0, container 817 / 0, jest 267 of 267 across 44 suites.
285. Handoff items P-05, P-06, P-08, P-09, P-07 and P-15, and the burn-in note. P-05: the per-recording PSD files' version name is computed from the 10% missing-sample limit (today's limit gives the same name, so nothing rebuilds). P-06: the uncalled `_assemble_psd_rows` deleted. P-08: the programmed switching values read in one query. P-09: the analytics test file's broken run-as-a-script block removed. P-07: a read-only counter of old PSD files by naming generation (`psd_rows_cache_generation_report`), not yet run, deleting nothing. P-15: the 8-30 Hz adaptive band range has one home in `DecodeCommon/device_ranges.py`, read by the Stim Optimizer and Biomarkers. Burn-in (the comment above the three-week exclusion, re-measured after decision 260): on L 1-3+ at 24.5 Hz it drops 13 samples in 2 weekly blocks (482 -> 469 samples, 42 -> 40 blocks); with it the mixed model reads OR 0.809 (0.570-1.148, p 0.235); without it lme4 fails to converge and returns a degenerate interval 0.0024 wide, so the exclusion stays. 13 new tests. Proof for 282-288 together, the committed tree against this one on RCS08, store writes blocked: Closed-Loop L 1-3+ 55,716 fields and R 0-3+ 55,729, 21 differing each, all timing; Stim Optimizer 10,911 and 57,587 fields, differing only in the response key (code digest), a manifest-written flag and one timing; heat-map grid 34,129 fields, 19 differing, all timing; 0 added and 0 removed anywhere. Host 1626 / 2 / 0, container 817 / 0, jest 267 of 267 across 44 suites. [2026-09-26, decision 289: measured on the short tile copy; on the full tiles the stability test on L 1-3+ at 24.5 Hz, NRS, again has 482 samples in 42 blocks; the mixed model's odds ratio was not re-run.]
286. Handoff item P-14 resolved without a change of arithmetic: the 'proven better' check did not omit a covariance term; it carried a fourth copy of the difference-of-two-estimates arithmetic that `routines/resolution.py` was built to hold once (2026-09-04), and now calls it. No verdict moves. Proof for 282-288 together, the committed tree against this one on RCS08, store writes blocked: Closed-Loop L 1-3+ 55,716 fields and R 0-3+ 55,729, 21 differing each, all timing; Stim Optimizer 10,911 and 57,587 fields, differing only in the response key (code digest), a manifest-written flag and one timing; heat-map grid 34,129 fields, 19 differing, all timing; 0 added and 0 removed anywhere. Host 1626 / 2 / 0, container 817 / 0, jest 267 of 267 across 44 suites.
287. Following 277: the titration card and plan no longer say a band near a folded multiple of the stimulation rate 'is struck'; the plan never dropped such bands, so the card now says they are flagged and analysed either way, in the PI's advisory wording of 2026-09-06. Jest pins. Proof for 282-288 together, the committed tree against this one on RCS08, store writes blocked: Closed-Loop L 1-3+ 55,716 fields and R 0-3+ 55,729, 21 differing each, all timing; Stim Optimizer 10,911 and 57,587 fields, differing only in the response key (code digest), a manifest-written flag and one timing; heat-map grid 34,129 fields, 19 differing, all timing; 0 added and 0 removed anywhere. Host 1626 / 2 / 0, container 817 / 0, jest 267 of 267 across 44 suites.
288. Handoff items P-17, P-20, P-21: the two Dockerfiles install R 4.3.3 and the lme4, lmerTest and emmeans packages at the versions the running server container has (the image installed no R, so a rebuild would have silently lost every mixed-effects fit); requirements' pins and notes aligned with the container; the settings ensure the empty static folder exists (the W004 warning). No image was built; nothing in the container changed. Proof for 282-288 together, the committed tree against this one on RCS08, store writes blocked: Closed-Loop L 1-3+ 55,716 fields and R 0-3+ 55,729, 21 differing each, all timing; Stim Optimizer 10,911 and 57,587 fields, differing only in the response key (code digest), a manifest-written flag and one timing; heat-map grid 34,129 fields, 19 differing, all timing; 0 added and 0 removed anywhere. Host 1626 / 2 / 0, container 817 / 0, jest 267 of 267 across 44 suites.
289. The saved 3-second tiles are read or written only for the one input set every page uses, and nothing before the implant date enters them (the PI, 2026-09-25 night). The saved copy was filed under the participant's database rows and constants, not under the recordings a caller handed in; the Stim Optimizer handed in the time-domain recordings alone (no survey or montage recordings) and built the patient-event PSDs without the sensing-pair index, so when it ran first after a daily ingest it wrote a short copy that every page then read: on 2026-09-25 at 20:03 UTC, 293,108 TD pieces where the full set is 303,321; the 10,213 missing equal the count of montage pieces, the recordings it did not hand in. The store's own record shows the same twice before, 2026-09-11 20:08 to 09-12 05:15 UTC and 2026-09-17 20:06 to 09-20 08:12 UTC. Now every caller asks through one helper (`_raw_lsb_cache_canonical`); a request with any other input set builds in memory, neither reads nor writes the saved copy, and is counted and logged; the saved copy's key carries the implant date, and the Closed-Loop `inputs` key carries the tile key. Live RCS08, 2026-09-26: the saved copy written at 03:35 UTC holds 303,321 TD pieces (261,636 indefinite stream, 31,472 BrainSense streaming, 10,213 montage) and 6,075 PSDs (4,512 patient-event, 1,563 montage), none before 2025-07-16 18:06 UTC; a build in memory without the cut gives the same counts, so on RCS08 today the cut removes nothing and stands as a guard. At the page's 60-minute NRS matching L 1-3+ matches 201 / 187 ratings at 30 / 60 s and R 0-3+ 459 / 439 (the short copy: 161 / 108 and 304 / 205). Before/after proofs made on the short copy stand (both sides read the same tiles); absolute numbers measured on it tonight are re-measured in 290-301 and corrected where they were written (Part 2 names what could not be re-run).
290. E2 read the ratings of the settings period BEFORE each piece's own, since the module was built (found 2026-09-25 night, fixed). The Closed-Loop joined table matched each piece's position in the settings table (counted from 0) against the ratings' period number (counted from 1): on RCS08, L 1-3+ at 24.5 Hz, 42,568 of 42,568 rated pieces carried the previous period's ratings, checked against the recording and report times, since `8fbe11ba` (2026-09-03). Only E2 (band power against pain) and its current-removed reading read those columns; E1, E3, the thresholds and the stability card do not. The ratings are now matched on the piece's own period number (`adapter._attach_setting_pain`), and a pain frame naming one period twice raises. Live RCS08, full tiles, 24.5 Hz, the old join against the new: L 1-3+ NRS 0.562 (0.426 to 0.691, p 0.41, 31 pain reports) -> 0.564 (0.438 to 0.678, p 0.29, 43 reports), current out 0.470 -> 0.553; L 1-3+ Left Leg VAS 0.773 (0.604 to 0.914, p 0.004, established, 22 reports) -> 0.641 (0.480 to 0.784, p 0.072, not established, 30 reports), current out 0.629 -> 0.551, and the verdict line from "1 of 3 intervals span zero" to "2 of 3"; R 0-3+ NRS 0.430 (0.302 to 0.576, 48 reports) -> 0.401 (0.288 to 0.509, 59), current out 0.388 -> 0.431; R 0-3+ Left Leg VAS 0.516 (0.344 to 0.675, 38) -> 0.445 (0.296 to 0.600, 44). The only E2 this page ever called established was the wrong period's ratings. Corrected, dated 2026-09-26: digest 242, 260, 268; full log 147, 175, 200 (and 242, 260, 268 there); `artifacts/established_point_sign_2026-09-13.md`; the 2026-09-22 D report, D panel and synthesis (the 0.559 baseline). Pinned by `test_pain_joined_on_own_setting.py`.
291. Two of the three parts of handoff item P-03 (June audit items [0] and [22]; the PI approved 2026-09-25), the third being 284: (a) each stimulation state's odds ratio on the band-stability card carries a 95% interval, the measurements and the pain reports behind it; (b) one pain report is counted in one stimulation state and one elapsed week, those of its earliest matched sample, so a rating matched to samples on both sides of a setting change is no longer split across blocks (stability rule v5; stored answers rebuild). Live, full tiles, 24.5 Hz, NRS: L 1-3+ 3 reports had samples in two states and 0 in two weeks; R 0-3+ 6 and 1. The June audit's item 15 remainder is closed with this row.
292. The per-state intervals and the "behaves the same" check are clustered on the pain report (the CR1 sandwich, with its small-sample factor), so a rating counts once however many samples it was matched to (stability rule v6); and a pain-score dropdown on the Closed-Loop page, from the heat maps' own list with NRS the default, drives every band-to-pain reading there (E2, E3, the stability card and its odds ratios), the ratings joined per request and never into the saved inputs (273). Live, full tiles, 24.5 Hz, odds ratio per standard deviation of band power (95% interval, pain reports): L 1-3+ NRS "cannot tell" (p 0.37; 482 measurements), off 0.89 (0.45 to 1.75, 43), low current 0.55 (0.25 to 1.20, 39), high 0.68 (0.41 to 1.13, 108); L 1-3+ Left Leg VAS "behaves differently" (p 0.0002; 289), off 13.1 (2.8 to 60.5, 18), low 1.66 (0.64 to 4.31, 28), high 0.82 (0.52 to 1.30, 68); R 0-3+ NRS "cannot tell" (p 0.056; 1,236), off 2.68 (0.93 to 7.72, 57), low 0.71 (0.54 to 0.92, 280), high 0.55 (0.33 to 0.90, 118); R 0-3+ Left Leg VAS "behaves differently" (p 0.042; 814), off 12.0 (1.7 to 83.6, 28), low 1.03 (0.79 to 1.35, 189), high 0.53 (0.26 to 1.08, 75). E3 (pain per mA): L NRS -0.155 (-0.270 to -0.039, p 0.0085, 90 settings), L Left Leg VAS -3.11 (-5.14 to -1.08, p 0.0027, 75), R NRS -0.192 (-0.358 to -0.027, p 0.023, 91), R Left Leg VAS -2.95 (-5.80 to -0.10, p 0.042, 75). Verdict: left "supported (point signs only; 2 of 3 intervals span zero)", right "blocked", under both scores. Not watched on screen.
293. (a) The deployment summary prints its area under the curve read again with the stimulation current taken out, beside the plain one (answer 6 of 2026-09-25; `routines/deployment_current.py`: the summary's own rows, E2's estimator, a straight line, descriptive only). Live, L 1-3+ 24.5 Hz, Left Leg VAS, tertile, 60 min: sheets off 0.617 (0.458 to 0.725, 39 pain reports) plainly and 0.580 (0.423 to 0.713) with the current out, over 5 distinct currents; sheets on (207 clinic-sheet ratings added) 0.554 (0.374 to 0.678, 80 reports) and 0.549 (0.396 to 0.677), 9 currents; verdict "candidate (mixed-effects n.s.)", 2 of 7 gates passed either way. The same values were read on the short copy (the summary reads no tile); decision 254's 0.623 (2026-09-23) and 258's 0.564 (2026-09-24) were read on the record as it then stood, before the implant-date cut (260). (b) The "Choose a band" card reads only stability answers computed under the rule in force (v6); an older answer reads "not tested", never borrowed.
294. Decision 253's block-of-time mark, a dagger, beside every current the Stim Optimizer page recommends whose pain map "moves between blocks of time" (answer 4 of 2026-09-25), with one note per card worded after 275, not 253's first sentence; the maps pooled across pulse widths (222, 255 and the back site's copies) now carry the same check, and on a pooled map "the setting delivered most often" includes the pulse widths. The folded across-rates table is not checked, on purpose, and says why: a stretch held out of a fit pooled across rates is also a set of rates held out (on RCS08 the REDCap 60/160 us fit's first stretch holds every 10 and 165 Hz epoch). Live, the two-stage request: 24 fitted maps carry the check; 4 read "moves between blocks of time" (left leg 55 Hz per pairing; REDCap pooled 55 Hz; clinic pooled 10 Hz; back site's clinic pooled 145 Hz), 13 "thin data", 5 "calibrated", 2 "too confident within blocks". No recommendation, verdict or value moves. The daggers were not watched on screen.
295. The calibration check's held-out folds are refitted side by side, in worker processes at one linear-algebra thread each (`STIM_OPTIMIZER_LOO_JOBS`; serially where no pool can start), every value unchanged: 0 of 2,104 held-out predictions differing. The leave-one-epoch-out fold, alternating before / after / before / after: 7.5 / 0.6 / 8.0 / 0.7 s and 11.6 / 0.9 / 11.6 / 0.85 s in the two measurements tonight's agent reported (the PI, 2026-09-25: "vectorize it"; not re-run for this record). The fold's own arithmetic is not batched: each fold's optimiser path depends on its rows. Its timings read no tile.
296. The dated settings history is kept per session file: a raw store kind, `therapy_settings_by_file`, holding the rows each stored session file contributed, filed under the file's uid and content hash (never its name), so a new file costs one parse, not 582 (a cold build decrypts 4.1 GB of JSON, 34.4 s). One new file, alternating before / after / before / after: 34.5 / 0.17 / 35.2 / 0.17 s; 67,950 cells of the stream, 0 differing (as tonight's agent reported; not re-run for this record). An unreadable file is never remembered as "no rows".
297. The research band detector, two versions, saved as control analyses on the Biomarkers page (the PI's rulings 5a-5c of 2026-09-25; `ControlAnalyses/band_detector.py`): the research version predicts the pain number from every band of a pair together on held-out blocks of time (rank correlation, 0 is chance), plainly and with the current taken out; the device-shaped version reads one band at the device's timing (3 s averages, 30 s onset, 15 s start-up) against two pain groups. Re-run on the full tiles 2026-09-26 (runs 04:08:29Z and 04:10:31Z; NRS, REDCap ratings): research, L 1-3+ 30 s (201 ratings, 151 days) every band +0.200 (+0.038 to +0.325, p 0.020, q 0.040), with the current out +0.187 (p 0.025, q 0.050), current alone +0.006; 60 s (187, 139 days) +0.243 (+0.081 to +0.376, p 0.010, q 0.040), current out +0.251 (p 0.005, q 0.020); R 0-3+ 30 s (459) +0.095 (p 0.12), current out +0.015 (p 0.51); 60 s (439) +0.105 (p 0.10), current out +0.013 (p 0.46). Device-shaped: L 1-3+ best band 19.5 Hz 0.784 (0.467 to 1.000, p 0.020, q 0.175), current alone 0.662, 0 of 22 bands q < 0.05; R 0-3+ best 0.543 (p 0.35). THE SHORT COPY HAD REVERSED THE RESEARCH VERSION: its runs of 02:11:05Z and 02:13:10Z (kept) read L 1-3+ +0.144 (p 0.10) and +0.179 (p 0.085) on 161 and 108 ratings, and R 0-3+ +0.240 (p 0.010) and +0.254 (p 0.015) on 304 and 205; the device-shaped version reads the same on both copies (it reads the device's own readings). Two first-version runs (02:06:55Z, 02:08:10Z) were moved out of the saved folder into `_agent_bridge/_superseded_band_detector_runs/`. A lead, not a finding (METHODS section 7).
298. TD and PSD, one vocabulary on the Biomarkers page (the PI, 2026-09-25): TD for band power from the time-domain recording in 3 s pieces, montage recordings included; PSD for the device's own 30 s snapshots; "spectrum" out of the page text of the three pages; "TD and PSD band power"; "the all-band scan". The heat-map square's split by source is printed as text above its scatter, computed on the server with the cell's own routine, interval, outlier rule and matching (`correlation_by_recording_source`); it selects nothing. Live, full tiles, daily defaults, 30 s, 24.5 Hz: L 1-3+ cell r -0.06 (201 reports), "TD values: r +0.00 (-0.16 to +0.16), 94 reports · PSD values: r -0.17 (-0.34 to +0.02), 107 reports"; R 0-3+ cell -0.19 (465), TD -0.04 (-0.24 to +0.18), 95 · PSD -0.23 (-0.32 to -0.13), 370.
299. A montage recording's 3 s pieces are labelled "Montage" instead of the fallback "time-domain" (the PI, 2026-09-26; tile rule v3): 10,213 pieces relabelled, 0 values differing (tonight's proof); the pieces are still cut from the time-domain signal and sit in the TD family, and no reader groups by the label. Live: the saved copy of 03:35 UTC carries 10,213 pieces labelled "Montage".
300. Handoff items P-20 and P-07. P-20: the beta-peak detector's two saved classifiers, fitted under scikit-learn 1.6.1 and loaded under the pinned 1.5.2, gave the same predictions under both (0 of 109,794 values differing, tonight's agent's count) and were re-saved under 1.5.2; a test fails if the pin moves without re-saving them. P-07: the old per-recording PSD files counted by naming generation: 1,620 files, 8.0 MB, all under the current naming; nothing to delete.
301. Records: the visit protocol revised to the PI's rulings (`artifacts/protocol_2026-09-25_rate_swap_and_down_first_ladder.md`); the 18 June mock-up images and pages at the repository root removed; the two calibration figures kept in `docs/exported_artifacts/`; the house rules' vocabulary row reads "say PSD for the device's own 30 s snapshot and TD for band power from the time-domain recording". Commit `4590af6a` carries 289-301: host 1705 / 2 / 0 and container 888 / 0 (bridge run 03:54 UTC), jest 424 of 424 across 55 suites.
302. The Closed-Loop page is one decision card plus the separate evidence cards (the PI, 2026-09-26: "one simple, streamlined card"; "the text is overwhelming"). The sticky header, "What would change this", "Full parameter recommendation", the band identity card and "Deploy-to-Percept review" become one card: one status line (decision 242's single verdict); red bullets when the device refuses ("Unmet:"/"Unchecked:" plus the rule's own label, at most four words, `short_label` on all 52 device rules) and yellow ones for evidence that did not run ("Untested:"); the values to enter only when the device allows them, with a "Programmed today" column; "Sign and print"; one Details fold, closed when allowed and supported. Evidence cards stay separate (supersedes 258(a)'s "sign-off card last"). The device-units panel no longer prints a value to program (its percentile cut-point read 141.7 and 143.0 LSB against the table's 241.14 and 213.48, 242 programmed). Found live: the result cache served an earlier band's report under a new band's name (header L 1-3+ 24.5 Hz, report L 0-2+ 23.5 Hz); a report for any band but the chosen one is now withheld from every card and named, with Recompute offered. Text at least 11 px including conditional and SVG sizes; pass/fail inks #00755A and #A84300. Live words on the page (document text, RCS08): 4,284 before, 2,025 after. Closed-Loop report 73,463 fields in common, 1 differing (timing), 32 added (the labels); summary 0 differing.
303. The Stim Optimizer page follows the same pattern (the PI, 2026-09-26: "yes to all six, build them"; amends 243's order, 243(b)/(d) and 245(b) now folded, the 2026-09-12 two-cards ruling, 245's legend). A status line ("Keep today's setting on both sides; closed loop cannot start.") with red and yellow bullets of five words or fewer, each with a glyph; readiness rows on the two pairs the device allows today, the other 19 folded; repeats said once (no current, closed loop cannot start, the ceiling); the home schedule inside the next-visit card; the current map blue-light grey-orange; one word per thing (stretches, proven better, minimum, rate) and no decision numbers in page text; chart text at least 4.5:1; bar states drawn by shape. Server wording changed in four files: live, the two responses 10,893 and 58,349 fields in common, 31 and 128 differing, every one a sentence with no number inside it changed, plus the response key. Live words: 10,473 before, 3,249 after.
304. The Biomarkers page laid out by the design review (the PI, 2026-09-26; amends 229, 235(c), 246(d)). Pain-score selector first; no box inside another; the stored-results and memory lines folded; one build action, Recompute; the 2026-09-21 search lines in their own fold; the heat maps open on one status line ("Allowed pairs today: L 1-3+, R 0-3+. Left Leg VAS, 5-min window: past the 22-band correction, 1 band rises with pain and 33 fall with it") and a red "4 of 6 pairs refused" bullet with each refused thumbnail marked; the calibration card shows its two constants in one open line; 37 sizes under 11 px and 22 greys under 4.5:1 fixed. Live words: 2,136 before, 1,618 after.
305. The device's sensing-pair rule has one home, `DecodeCommon/sensing_rule.py` (decisions 217, 243, 247); the Stim Optimizer's old names delegate to it; the Closed-Loop D52 rule, its settings reader and the control analyses call it; a test finds one definition of each function. The Biomarkers grid response carries `sensing_rule` from the newest stored setting on each lead, added after the stored grid is read, never in its key or payload. "Binarize" became the high / low split, "KMeans (legacy)" "Two clusters (older rule)", "Max LSB samples per pain rating" "Samples per pain rating, at most"; the timeline's frequency labels drawn in darker variants of their hues (all at least 4.5:1). Live, the last commit against this tree, store writes blocked: Closed-Loop report 73,463 in common, 1 differing (timing); heat-map grid 43,657 in common, 19 differing (timing), 28 added (the block). Host 1730 / 2 / 0, container 903 / 0, jest 530 of 530 across 60 suites.
