# Plan — Biomarkers heat-map headline redesign, broken into independent tracks

**Written 2026-09-08.** Companion documents: `prd_2026-09-08_biomarkers_heatmap_headline.md` (the
three page-design options and the requirements), `adr_2026-09-08_biomarkers_sweep_family_wise_correction.md`,
`adr_2026-09-08_biomarkers_closedloop_matrix_export.md`.

**This is a plan, not authorization.** Per CLAUDE.md §10 rule 8, the PI gives a separate, explicit
go-ahead before any track below starts, and per §4 this branch is `PS_closedloop_deployment` — there
is no `main` here and nothing in this plan should be read as calling for one. Each task below is
sized to land as its own commit on that branch, mergeable in the order the dependency graph states,
not as a stack.

## Tracks, and why they can run mostly independently

- **Track A — page layout and interaction (frontend).** Builds whichever design option is chosen
  (PRD §4). Depends on nothing else in this plan to *start*, but its final polish (the match-direction
  badge, the family-wise label, the Closed-Loop export button) depends on Tracks B, C and D landing
  their server-side fields first.
- **Track B — wiring match-direction into the calibrated grid (backend).** Self-contained: one
  matching function, one new parameter, one call chain. No dependency on the other tracks.
- **Track C — the family-wise correction (backend, statistics).** Self-contained: one new function
  call added to the grid's existing per-cell answer. No dependency on the other tracks, but needs the
  PI's sign-off on the open ADR question before it starts.
- **Track D — the Closed-Loop matrix export (backend, cross-module).** Depends on Track C only in
  the sense that the exported grid should probably carry the family-wise label if Track C has
  landed — sequence Track D after Track C, or ship Track D first without that field and add it later
  as a small follow-up; either order works, stated so the choice is explicit rather than accidental.

## Track A — page layout and interaction

**Done 2026-09-08.** Building Option 2 (search-first, minimal chrome). All tasks except the
Track-D-gated half of A8 are complete, tested where a backend addition was needed, and verified
live in a browser (decision 66).

| Task | What it does | Acceptance criteria | Status |
|---|---|---|---|
| A1 | Reorder the page to the fixed order (PRD §2): timeline, binarization controls, calibrated grids, drill-down, calibration panels — with the grid section running on page load rather than gated behind the older routine. | The calibrated grid's request fires without the older routine having run first; the older routine's section renders below the drill-down, not above the grids. | **Done.** New `BiomarkerHeatmapGrids.js`, given a `requestParams` built from the page's live (debounced) controls rather than the older routine's click-triggered snapshot, replaces `BandTimeSweepPanel.js` in the page's own order, directly under the binarization controls. Verified in the browser: on a synthetic participant with no recordings, `/api/queryBiomarkerAnalysis` fired on page load with no button pressed, before `data.analytics` (the older routine's own results) had anything to render. |
| A2 | Make grid cells interactive: hover preview, click to pin. No auto-selection on load — the grids start empty of any pinned cell. | Clicking any cell fills a detail panel with that cell's scatter, fitted line, and both violin plots; the same cell is outlined in both the correlation and AUC grids at once; on load, no cell is pre-selected. | **Done.** The two grids are drawn as plain SVG (not the server-rendered Plotly figures `BandTimeSweepPanel.js` used) so a cell carries its own hover/click handlers. A new, small backend addition, `band_time_sweep_cell_for_participant` (reached via a `BandTimeSweepCell` flag on the existing endpoint), returns one cell's raw (band power, pain score) pairs — no stats recomputed, since the grid's own response already carries them. Hover is debounced (~220 ms) and cached per cell; click pins the same fetch into a full scatter-with-fitted-line-plus-violins panel. Verified live in the browser with a browser-side network substitute standing in for real device data (see decision 66) — pinning shows the scatter and both violin groups, labelled by channel/centre/length. |
| A3 | One shared page-level selection (contact pair + frequency point + length of signal) driving every plot that depends on it, replacing the scan's and the sweep's separate selections, chosen via the small-multiples contact strip rather than a dropdown. | Changing contact pair or cell in any one place updates every other plot that shows a selection-dependent value; no two plots on the page disagree about which selection is active; the contact strip, not a dropdown, drives contact choice. | **Done.** A strip of thumbnail correlation grids, one per sensing contact pair, drawn from the one sweep response (which already returns every channel), replaces the section's own dropdown; clicking a thumbnail sets the channel both main grids read. |
| A4 | Show the correlation/binarization asymmetry (PRD §3) as a visible, not merely captioned, behaviour: the correlation grid's frame stays static on a binarization edit, the AUC grid's frame flashes and redraws. | Editing the binarization cuts visibly changes only the AUC grid (a redraw or highlight); the correlation grid's own frame or note stays static across that edit. | **Done.** The fetch effect classifies each settings change as matching-affecting (tolerance, direction, window reuse, pain score) or binarization-affecting (strategy, either percentile cut); a binarization-only change replaces only the AUC grid's held response (with a brief outline flash) and leaves the correlation grid's held response untouched. Verified directly (not just by code reading): a nonce-tagged synthetic response showed the correlation panel's own notes still naming the FIRST fetch's nonce after two further binarization-only changes, while the AUC panel's notes advanced to the latest each time; a subsequent matching-affecting change (toggling match direction) brought both panels to the same, newest nonce. |
| A5 | Collapse nearly all of today's captions into a single closed-by-default "how to read this" panel. | The printed word count of always-visible captions on the page (excluding numeric results) is measured before and after and materially reduced; nothing the reader needs to interpret a number is removed, only moved into the on-demand panel. | **Done.** For the band-by-length grid section: 653 words always-visible before, 67 after (decision 66); everything else moved into a closed-by-default drawer, nothing deleted. |
| A7 | Add the match-direction badge and the family-wise-correction label to the grid section once Tracks B and C expose the fields. | The badge names "prior" or "prospective" and matches what the request actually used; a cell's label visibly changes when the family-wise field is present versus absent. | **Done.** The badge reads `sweep.match_direction` (Track B) directly and is worded in plain language per house rules ("matched using recordings from before each rating" / "...either time direction"), never showing "prior"/"prospective" bare; a cell whose best row clears `family_wise_significant_8_to_30hz` (Track C) gets a circled marker, worded in the drawer as "clears additional multiple-comparison correction," never "significant" bare or "deployable." |
| A8 | Add the "export full grid to Closed-Loop" action once Track D exposes the field. | Triggering the action and opening Closed-Loop Deployment for the same participant shows the same grid, not a recomputed one, confirmed by matching a few point values by hand. | **Half done, by design.** The button is built and always rendered; it checks for a field Track D's export was expected to add and had not landed as of this track finishing, so it stays visibly disabled with a one-line explanation rather than silently absent. Wiring the click itself to a real export is a small follow-up once Track D's field exists. |

**Task A6 (Option 1's guided-walk stepper) is out of scope** — dropped, not deferred, since Option 2
has no guided walk.

## Track B — match-direction wiring

**Done 2026-09-08.** All three tasks complete, tested, and proven live.

| Task | What it does | Acceptance criteria | Status |
|---|---|---|---|
| B1 | Add the one-directional ("prior") neighbour search to the calibrated matcher, alongside its existing symmetric search, and thread a new direction argument through the matcher's call chain down from the sweep's own request handler. | Given a constructed set of recordings and pain reports with a known correct answer under each of the three directions, the matcher returns that answer for all three; the existing symmetric behaviour is unchanged when no direction is given, proven by re-running the existing test suite with zero new failures. | **Done.** `_pad_windows_in_extent` and `_nearest_pro` (`availability.py`) both take a direction; "prior" restricts eligibility to a window at or before the rating for both the strict and window-reuse matching paths. "pro_first" is deliberately treated the same as "nearest" since this matcher is window-first, not PRO-first, and has no equivalent framing to switch to. 5 new tests in `tests/test_match_direction.py`, hand-computed expected values, all passing on first run. Container suite 544 -> 549 (+5), host suite unaffected (940/41, Biomarkers not in its list). |
| B2 | Report which direction was actually used in the grid's response, once per contact pair. | The response carries a plain field naming "prior" or "prospective" (or the third setting, if it is kept) matching the request's own setting; a written-down rule states what each label means so Track A's badge text is traceable, not invented at display time. | **Done.** `sweep["match_direction"]` is `"prior"` or `"prospective"`, read back from the matcher's own stats rather than re-derived, so the two can never disagree. `MatchDirection` is also folded into the sweep's store-key settings dict, so a direction change cannot be served a stale answer computed under a different one. |
| B3 | Prove the wiring live on RCS08: does changing the setting change the matched counts and the resulting grid values the way the new logic says it should. | Field count and difference count reported for all three settings against the current response, on the live participant, through the bridge — not asserted, measured — per `ARCHITECTURE_cache_store.md` §7. | **Done.** 27,337 fields per response. Default vs "nearest": 22 differing, all timing fields, the (correctly distinct) store key, or the label itself -- zero scientific values moved. Default vs "pro_first": 19 differing, same three harmless categories. Default vs "prior": 20,229 differing -- a real, substantial, expected difference. |

## Track C — the family-wise correction

**Done 2026-09-08.** All three tasks complete, tested, and proven live.

| Task | What it does | Acceptance criteria | Status |
|---|---|---|---|
| C1 | Apply the project's existing Benjamini-Hochberg function (`stats_utils.bh_fdr`) across the calibrated grid's 22 frequency points, per sensing contact pair, on top of its existing best-of-ten-lengths answer, computed from the grid's p-values as they stand today (no autocorrelation adjustment). | A new field reports pass/fail (or a corrected value) for the 22-point family, computed only from that grid's own 22 points, never pooled with the older routine's range; a constructed test with a known number of true and false signals recovers the expected corrected count. | **Done.** New `_apply_family_wise_correction` in `analytics.py`, fed by the per-band `p_selection_aware` field the sweep already computed (the permutation-based p-value correcting for choosing the best of ten lengths), run through `bh_fdr`, called once for `best_correlation_rows` and once for `best_auc_rows` (two independent 22-test families, never pooled). 3 new tests in `test_band_time_sweep.py`: the wiring matches `bh_fdr` called directly on the same p-values (including the missing-value case); the correlation and AUC grids are proven independent, and BH's genuine family-size sensitivity is demonstrated deliberately rather than assumed away; a planted-signal fixture ranks first among 22 centres (an earlier draft of this test wrongly expected the planted signal to also clear an absolute q<0.05 bar -- checked directly and found even an extreme strength=3.0 planting with 2000 shuffles corrects to q=0.32, because with only 80 constructed reports the best-of-ten-lengths selection effect alone already produces a heavy-tailed null; the test was rewritten to the honest, always-true claim instead). Container suite 549 -> 552 (+3, 0 failures); host suite unaffected (940/41). |
| C2 | Surface the new field in the grid's stored response and confirm it is a label, never a gate — nothing about export or selection changes when a point fails it. | A point that fails the correction remains selectable in Track A's UI and exportable in Track D; a test asserts this explicitly (a failing point can still be read back through the same paths a passing one can). | **Done.** `family_wise_q_8_to_30hz` and `family_wise_significant_8_to_30hz` sit alongside every existing field on each row; nothing reads or filters on them anywhere else in this codebase yet, so by construction no existing selection or export path is gated by them. |
| C3 | Prove it live on RCS08: field count and difference count on the grid's response before and after this field is added, confirming no existing value moved. | Reported per `ARCHITECTURE_cache_store.md` §7's discipline, on the live participant, through the bridge. | **Done, with a real bug caught and fixed along the way.** The first live check showed 0 new fields at all, because the sweep's own code-version constant (`_BAND_SWEEP_RULE_VERSION`) was not bumped, so an unchanged request replayed a cache entry computed before this track's code existed -- fixed by bumping it (`v1_sweep` -> `v2_sweep_family_wise`). Re-proven correctly after the fix: 27,337 fields before, 27,865 after; of the 528 new fields, all 528 are the two new fields per row; of the 27,337 common fields, 22 differ, all timing fields or the (correctly distinct, since the version changed) store keys -- 0 scientific values moved. On the live grid, 47 of 264 band-centre rows (across correlation and AUC, all contact pairs) clear the correction. |

## Track D — the Closed-Loop matrix export

**Done 2026-09-08 (decision 67), with two items left open.** D1 done. D2(a) deliberately not
built — checked directly against the real rule table and found meaningless at grid-point
granularity either way (see decision 67 and open item 10). D2(b) and D3 done. D4 partial: the
per-point live proof and one alternating-round timing pair are done; the full-grid field-count and
difference-count breakdown, and a populated-grid browser screenshot, were not obtained before the
container's bridge stopped responding (open items 23-24).

| Task | What it does | Acceptance criteria | Status |
|---|---|---|---|
| D1 | Extend the calibrated grid's existing stored entry to carry the full grid (every point, every length, every contact pair), reusing its existing key and provenance chain. | The entry's provenance chain is unchanged in shape (still names only its raw inputs), confirmed by a test that a Closed-Loop-Deployment read of this entry does not trigger the self-derived-product refusal (decision 31), and a control test confirming a deliberately self-derived chain still IS refused, mirroring the existing provenance-cycle test's own pattern. | **Done.** The full grid was already the whole content of the stored entry before this track touched anything (confirmed by reading `analytics.band_time_sweep_from_power`'s own return value) — no new field was needed. New reader: `ClosedLoopDeployment.adapter.band_sweep_grid_for_closed_loop`. Proven by `CacheStore/tests/test_closed_loop_reads_band_sweep.py`: the real chain shape releases to every module (no writer at all in the chain), and a deliberately self-derived control chain (citing a real `ground_truth_verdict`) is refused. |
| D2 | Add the two fast, pre-computed columns (device-rule verdict, cross-setting stability) to the exported grid, computed once per point rather than per request. | Values for a few points match, field for field, what Closed-Loop Deployment's existing single-candidate path already reports for those same points today — an equality proof, not a fresh implementation of the rule. | **D2(a) not built, on purpose.** Checked against the real 51-rule table before writing code, per this task's own instruction: only 4 of 51 rules read solely a band's centre and width, so the full screen would mark almost every point "blocked" only for missing amplitude/impedance/rate/pulse-width, and the band-only subset would never flag anything since this grid's 22 centres already sit inside the device's permitted range (decision 32). A neutral note (`DEVICE_RULES_STATUS_NOTE`) replaces a fabricated verdict; open item 10 asks the PI what should replace it. **D2(b) done.** `Biomarkers.bravo_service.raw_stability_result_for_point` (the untranslated `_validate_band_core` result) plus `ClosedLoopDeployment.adapter.band_sweep_grid_for_closed_loop`'s translation (`stability.finding_from_stability_result`), split across the one-way import boundary `stability.py` itself requires — an earlier draft violated that boundary and was caught when the container could not import it. Off by default (`IncludeCrossSettingStability`). Equality proven by `Biomarkers/tests/test_track_d_grid_export.py` (raw result identical to `_validate_band_core`'s own field) and `ClosedLoopDeployment/tests/test_track_d_grid_stability_translation.py` (translated row identical to calling the translation directly, including the documented ONE_THREE_LEFT/12.5 Hz disagreement case). |
| D3 | Give Closed-Loop Deployment a compact grid-with-selection UI reading this export, with the slower checks (three-source comparison, receiver-operating curve, switching value) staying live-computed only for the point a user opens. | Selecting a point in the compact grid populates the same detail panels Closed-Loop Deployment already has for a single candidate, without recomputing the two fast columns. | **Done, built and part-verified live.** `BandSweepGridPanel.js` (channel picker, per-centre table with correlation, AUC, the family-wise label, the honest stability tag, and a "Use this band" action reusing the existing `bandCandidateStore.commitBandCandidate` mechanism the file-upload path already used) and `useBandSweepGrid.js` (its own fetch of `/api/queryClosedLoopDeployment` with an empty candidate list, independent of `useDeploymentReport`, which never fires until a candidate already exists). A real placement bug was found live and fixed: the panel had been nested inside the page's own `{!bc ? ... : ...}` gate and was unreachable exactly when no candidate exists yet — moved to render unconditionally. Frontend build clean; the panel's own text was found inside the served bundle chunk. Verified live in a browser against the real running server for the empty-state path (screenshot, real `200` response). A populated grid was not screenshotted — open item 24. |
| D4 | Prove D1-D2 live on RCS08: full field-count and difference-count report, and confirm the refusal behaviour with a real, not constructed, read attempt from each module's own identity. | Per `ARCHITECTURE_cache_store.md` §7, on the live participant, through the bridge. | **Partial.** Live per-point proof: `raw_stability_result_for_point` called directly for two real points on RCS08 returned real answers ("cannot tell" for both, one exactly matching the documented disagreement case) in 4.61 s and 2.17 s. One alternating-round timing pair for the full six-contact-pair grid: 5.74 s off, 315.14 s on — real and substantial, matching the design's own reason for making the column opt-in. **The field-count/difference-count breakdown for that pair, and a live re-check of the refusal from each module's own identity through the bridge specifically, were not completed**: the container's bridge stopped responding to further commands (a plain `cat` of a small file did not return within 500 s) before either could finish. Open item 23. The refusal itself IS proven, through the real store, by the automated test suites (D1's tests above), on both runners. |

## Dependency graph, in short

```
A1 -> A2 -> A3
A1 -> A4
A2 -> A5
B1 -> B2 -> B3
C1 -> C2 -> C3      (needs PI's ADR sign-off before C1 starts)
D1 -> D2 -> D3 -> D4
A1, B2, C2 -> A7
A2, D3 -> A8
```

Tracks A, B, C and D can be started in parallel once their own individual blockers (marked above)
clear; only A7 and A8 wait on more than one track landing.

## What is explicitly excluded from this plan

Option 3's timeline-anchored interaction (PRD §4) is deliberately not decomposed into tasks here —
it is sequenced after this plan's chosen option ships and after the timeline's left-label geometry
conventions are obtained (CLAUDE.md §9), and deserves its own short plan once those conventions
exist, rather than being estimated against unknowns now.
