avro==1.12.0
boto3[crt]==1.40.21
cryptography==44.0.0
django==5.1.3
djangorestframework==3.15.2
django-cors-headers==4.6.0
filelock==3.16.1
blosc2==3.3.3
numpy==2.1.3
scipy==1.14.1
scikit-learn==1.5.2
statsmodels==0.14.6
# Logistic mixed-effects (glmer via R lme4) for the exploratory band inference. pymer4 0.8.2 is
# the lightweight branch (needs only lme4/lmerTest/emmeans + seaborn; NOT the 0.9 easystats/report
# stack). rpy2 must come from the system package (apt python3-rpy2) — the PyPI sdist fails to link
# its C extension in this image. R + r-cran-lme4/lmertest/emmeans are installed at the OS level.
pymer4==0.8.2
seaborn==0.13.2
pandas==2.2.3
spectrum==0.8.1
specparam==2.0.0rc2
requests==2.32.3
pymysql==1.1.1
psutil==7.0.0
mysqlclient==2.1
whitenoise==6.7.0
waitress==3.0.2
setuptools==75.8.0
pywavelets==1.8.0
nibabel==5.3.2
PyCap==2.6.0  # REDCap PRO pull (modules/Biomarkers/routines/redcap_client.py)
# StimOptimizer's interactive figures. The module builds Plotly figure JSON for the browser to
# draw and never renders images server-side, so this is the pure-Python package ONLY — kaleido and
# a headless browser are deliberately not needed, which is why routines/plots.py draws its static
# PNGs with matplotlib instead of exporting the Plotly figures.
#
# Its absence was SILENT and cost the Stim Optimizer page every one of its figures. _plotly_figures
# imports plotly inside the function, so the ImportError was caught by the caller, which set an
# empty figure dict plus a figures_error string; the page then had nothing to draw. Nothing failed
# loudly, no test covered it because the figure builders are exercised on the host where plotly IS
# installed, and the module had been producing good figures as saved PNGs all along — so the
# natural conclusion was that the interface had never been wired for them, which was wrong.
plotly==7.0.0
