server {
    listen 80;
    client_max_body_size 500M;

    # API + websockets proxied to the Django dev server.
    location /api {
        proxy_redirect off;
        proxy_set_header host $host;
        proxy_set_header X-real-ip $remote_addr;
        proxy_set_header X-forward-for $proxy_add_x_forwarded_for;
        # Long timeout: full-resolution biomarker computations over many months of dense
        # power-domain data can take a while (the calculations are intentionally not decimated).
        # Match gunicorn's --timeout 600 so a slow window returns instead of a 504.
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        proxy_pass http://localhost:27286/api;
    }

    location /socket {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header Origin $http_origin;
        proxy_cache_bypass $http_upgrade;
        proxy_pass http://localhost:27286;
    }

    # Serve the SPA (mounted ./Client/build) directly so index.html + static stay consistent.
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}
