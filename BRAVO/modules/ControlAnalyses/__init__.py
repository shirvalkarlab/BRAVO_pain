"""Control analyses: checks run offline on one participant's record and SAVED, dated, for the page
they belong to (the PI, 2026-09-24). They feed no recommendation and are never recomputed on a
page load; a new run adds a new saved result and keeps the old ones."""
